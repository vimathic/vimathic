## MATHEMATICAL_ACCURACY.md


# VIMATHIC — Mathematical Accuracy Report

**Scope:** All 192 formulas across 12 collections, shared helper functions, the
6 volume vector fields, and — since round 5 — the 38 GPU displacement branches
in `computeMode()`. Those 38 were outside every earlier revision of this
document, which is how two of them came to render nothing at all and how
SCIENCE.md came to name four modular forms of which one was implemented.
**Date:** 2026-05-10
**Method:** Per-formula classification by numerical method, comparison of stated formula vs implementation, audit of failure modes (truncation, modulation, approximation error).

---

## Executive Summary

| Tier | Count | Definition | Marketing-defensible? |
|------|------:|------------|:---------------------:|
| **A** — machine precision | **122** | Closed-form analytic expressions evaluated at IEEE 754 double precision (~10⁻¹⁰ to 10⁻¹⁴ accuracy). | ✓ Yes |
| **B** — bounded approximation | **35** | Polynomial fits, finite-converged series, well-behaved iterative methods, real PDE/ODE simulations on adaptive grids. Documented error ≤ 10⁻³ to 10⁻⁷. **Per row, read at the factory sliders — see the rule below — and not a single ceiling over the whole tier.** Two rows say so in their own text: `manifoldCurvature` 2.8×10⁻³ on its stencil at freq 3.5 and 3.24 world units over a third of the plate at the audio envelope, and `ornsteinUhlenbeck` documents 17 % on the two statistics that define its process. | ✓ Yes, with that qualifier |
| **C** — visualization-grade | **35** | Truncated chaotic iterations, decorative modulations, simplified models. Qualitatively faithful but not numerically exact. | Conditional |
| **D** — defects | **0** | All previously identified defects fixed and verified by automated tests. | n/a |

**Tier A + B = 157 formulas with verifiable numerical accuracy.**

**Round 1 — D-tier defects fixed (3):** `tinkerbell`, `dragon`, `jacobian` — all moved up to A or B with regression tests.

**Round 2 — C-tier formulas rewritten with canonical implementations (11):**
- `bessel1` — finite-difference replaced with Numerical Recipes J₁ polynomial. C → **B**, ~10⁻⁷ accuracy.
- `polygamma` — single asymptotic term replaced with full Bernoulli series + recurrence. C → **A**, ~10⁻¹⁰ accuracy.
- `dawson` — naive Riemann sum replaced with Taylor (|x|<3.5) + asymptotic series (|x|≥3.5). C → **A**, ~10⁻⁷ accuracy verified against mpmath.
- `landauLevels` — hardcoded `1-r²/2` replaced with proper generalized Laguerre L_n^0 recurrence. C → **A**, exact for any n.
- `atomicOrbitals` — was mislabeled "sp³" with broken angular structure. Renamed to honest "sp² (xz-plane)". C → **A**. (Round 8: the "3-lobe geometry at 120°" claimed here is not what the entry draws, and the label named the amplitude where the kernel returns its square — the caption and the row now both read \|ψ_sp²\|². See its row below.)
- `radonTransform` — decorative rotated Gaussian replaced with analytic Radon transform of two Gaussians (closed form). C → **A**.
- `cauchyIntegral` — Gaussian peak replaced with numerical contour integral, N=24 quadrature. Verified to give f(z₀) inside contour, 0 outside. C → **B**.
- `windingNumber` — `sin(2θ)` replaced with numerical winding count via `∮dz/(z-z₀)/(2πi)`. Returns ~n_loops inside, ~0 outside. C → **B**.
- `conway3D` — 1D Wolfram rule replaced with real 3D B5-7/S6 simulation on 18³ grid, 3–5 generations. C → **B**.
- `excitableMedia` — sin-spiral replaced with FitzHugh-Nagumo PDE on an optimised internal grid, bilinearly interpolated onto the display mesh. C → **B**.
- `reactionDiffusion` — threshold-of-oscillators replaced with Gray-Scott reaction-diffusion on an optimised internal grid, bilinearly interpolated onto the display mesh, with configurable F/k regimes. C → **B**.

**Round 5 — the 2026-08-13 audit.** Every entry below was re-derived against an
independent reference (Gauss–Legendre quadrature of the defining integral, a
second algorithm, or a canonical constant), not against the implementation.

- `erf` — the Abramowitz & Stegun §7.1.26 Horner fit was rated A while its own
  error bound is 1.5·10⁻⁷; measured 1.394·10⁻⁷ at x = 0.045. Replaced by the
  all-positive series 2/√π·e^{−x²}·Σ2ⁿx^{2n+1}/(2n+1)!!, which has no
  alternating cancellation. Now 7.7·10⁻¹⁵ over the reachable domain. Stays **A**,
  and now earns it.
- `dawson` — Taylor below |x| = 3.5, five-term asymptotic above: 1.9·10⁻¹² at
  x = 3.4 against **3.2·10⁻⁵** at x = 3.5, a seam inside the domain the entry
  reaches at the default wave intensity. The asymptotic series for F is
  divergent, so no term count closes it. Replaced by Rybicki's lattice sum,
  ~3·10⁻¹⁵ over |x| ≤ 24. Stays **A**.
- `clausen` — twelve terms of Σ sin(kθ)/k², which converges like 1/N and had no
  accuracy at the ends of the period. Replaced by the log-sine expansion
  θ − θ ln θ + θ·Σ ζ(2n)/(n(2n+1))·(θ/2π)^{2n}. Exact to 10⁻¹⁶ at Catalan's
  constant and at Cl₂(π/3). B → **A**.
- `airy` — no forward march of y″ = xy survives, whatever the step size: the
  growing Bi solution takes over. Measured, Ai came back **negative** from
  ξ ≈ 4.88, inside the ξ ≤ 5.25 the default wave intensity reaches, where the
  true Ai is positive everywhere. Marching replaced by the Maclaurin series on
  |x| ≤ 8 and the standard asymptotics beyond (u₀…u₅), and cheaper than the RK4
  loop it replaces. C → **B**, absolute error ≤10⁻¹³ over the default window
  |x| ≤ 5.25 and ≤10⁻⁸ over the whole reachable |x| ≤ 24.

  *Second pass.* This entry was first written up here as C → A on a claimed
  10⁻¹⁴. An adversarial re-derivation — Decimal series at 130+ digits, a contour
  integral through the saddle, and the Bessel identity
  Ai(−y) = √(y/3)·(J_{1/3}+J_{−1/3}), all three agreeing — showed that is not
  what the series delivers: it alternates, so it loses digits exactly where |Ai|
  is smallest. At x = 8 the largest partial is 1.34·10⁶ against |Ai| = 4.7·10⁻⁸,
  leaving about 2.5 significant digits, and the measured worst absolute error is
  1.1·10⁻¹⁰ at the positive seam and 1.2·10⁻⁸ at the negative one. Invisible on
  a mesh (10⁻⁸ against a frame ~3 units high) — but the tier and the number in
  this document have to be the ones that were measured, so both are corrected.
- `zeta` — 14–22 terms of Σn^{−s} is 85 % below ζ(1.05); shifting the window to
  start at 1.05 removes the divergence and nothing else. Euler–Maclaurin closes
  it in the same budget, ~10⁻¹⁰ across the window. C → **B**.
- `hypergeometric` — the early exit at 10⁻⁸ never fired (the twelfth term at
  z = 0.875 is 2.5·10⁻²), so the truncation was hard at twelve terms and two
  orders outside tier B. Euler's transformation plus a 120-term cap brings the
  worst reachable point to 6.5·10⁻⁵. Stays **B**.
- `chebyshev` — the ±(1−10⁻⁹) guard inside acos cost 2.5·10⁻⁸ on the entire
  saturated rim, not at one point. Removed; the argument was already clamped.
  Stays **A**.
- `gamma_fn` — drew 0.12·ln|Γ(n)| under the caption Γ(n) = (n−1)!. Over the
  window it shows, Γ runs 4.591 → 0.8856 → 4.694 and fits the frame directly,
  so the log was not even buying headroom. Now plots Γ. Stays **A**.
- `hydrogenS` — |ψ₁₀₀|² was multiplied by cos²(l·θ + 0.3t) with l = 0, i.e. by
  cos²(0.3t): the 1s orbital blinked out completely every 21.8 s of wall clock,
  peak falling from 3.623 to 4.9·10⁻¹¹. An s state is spherically symmetric and
  stationary. The angular factor is now applied only for l ≥ 1. Stays **A**.
- `feynmanPath` — the eighth entry reading the session clock as a physical age,
  missed by round 4 because that fix worked from a hand-written list. Amplitude
  1/√(0.5+0.05t) fell to 0.107 of boot at thirty minutes and 0.038 at four
  hours. Now folded to a 24-unit period; t = 0 is bit-identical. Stays **A**.
- `catenoid` — exact and unwatchable: peak |y| of 8.2·10¹ at the **default**
  wave intensity against a frame ~3 units high, 5.1·10¹² at the top of the
  range. Output clamped to ±1.5, which leaves the neck and the zero level set
  untouched. Stays **A**. *(Round 8 withdrew that letter: the fold that replaced
  the clamp covers 52.16 % of the plate at grid 81 and 51.90 % at 161 at the
  factory sliders — the 52.1 % is the grid-90 count, and 90 is not a lattice the
  app lays down — so half of what is drawn at boot is a tanh image of
  a·cosh(z/a). See its row.)*
- `legendre2` / `sinc` — captions corrected to the surface actually drawn
  (Pₙ with n ∈ {3,4,5}; the radial sinc, not sinc of x).

Volume fields: `fluidVortex` was described as incompressible and had
∇·v = −amp·0.1·freq·sin(y·freq+t) — its vertical component is now driven by the
cylindrical radius, so ∇·v ≡ 0 exactly. `magneticDipole` applied its
regularising ε to the numerator as well as the denominator, which made the
axis-to-equator ratio −1.667 instead of the −2 a dipole gives at any radius;
the true r² is restored and the ratio is now exact. `lorenzField` scaled its
three components by three different constants, so the direction of the vector
was wrong everywhere; one scale now, and β = 8/3 rather than 2.667.

<!-- This counts tests/math-validation.test.js alone. `npm test`
     runs every other suite in tests/ as well. -->
Test suite: the validation file passes in full, including the regression tests that fail
on the pre-round-5 code and pass on this one.

### A note on grid resolution

Formulas labelled as running "on a 64×64 grid" or similar in this document refer to the **internal simulation grid** used by heavy CPU formulas (cellular automata, reaction-diffusion PDEs, etc.). These internal grids are bilinearly interpolated onto the main display mesh, whose resolution is adaptive:

- The plane carries `planeSegs` 80 on mobile and 160 on desktop
  (`src/main.js`, `planeSegs: isMobile ? 80 : 160` in the CFG literal) — **81 and
  161 vertices per side, and no other value.** The
  tier bump in `RenderEngine`'s constructor raises `planeSegs` to
  `max(planeSegs, 160)` on tier `high`/`ultra`, which is a no-op on desktop and
  never fires on mobile, where `_detectPerformanceTier()` returns `low` on its
  first line for every mobile UA. There is no 120, no 200, and no
  integrated-GPU branch; the only two `PlaneGeometry` calls that carry a
  formula — the boot mesh in the constructor and `_buildShapeGeo`'s
  `case 'plane'` — take that same `planeSegs`. (Cited by symbol, not by line:
  the four render.js line numbers this bullet used to give were all displaced
  by round 10's own edits, and the file is still moving.)
- **A formula is not evaluated on `planeSegs` at all.** `MathVisualizer` takes
  `gridSize = round(√(vertex count))` of whatever shape is selected and hands
  that to `generateSurfaceFromFormula`. It is computed in five places in
  `src/math-visualizer.js` — `setFormula()` twice (once to size the blend
  buffer, once to set `_gridSize`), `onShapeChange()`, `tick()` (which compares
  the live count against the stored `_gridSize` and bumps the worker
  generation when they differ), and `_snapshotBasePositions()`. Cited by
  method rather than by line on purpose: the previous revision of this bullet
  gave four line numbers, round 10 inserted code above all four, and every one
  of them then pointed at something unrelated. `grep -n 'Math.round(Math.sqrt'
  src/math-visualizer.js` finds the five and cannot go stale.
  Over the twenty shapes in `#shape-sel` that is
  **22 distinct grids — 3, 5, 8, 9, 10, 13, 15, 18, 21, 24, 43, 45, 52, 63, 65,
  74, 81, 83, 100, 114, 161, 198** — thirteen odd and nine even. Round 10 moved
  three of them: closing the mesh of `cone`, `pyramid` and `pyramid-smooth`
  (see below) raised their vertex counts, so grids **20, 42 and 82 are no
  longer produced at all** and **21** joined; 43 and 83 were already reachable
  through `disc` and `cylinder`. On desktop only three of the twenty (plane,
  sphere, solar) have a vertex count that is a perfect square.
- **What the height field is, since round 10.** It used to be indexed straight
  into whatever buffer the shape laid down — value *i* to vertex *i* — which is
  the lattice only for the rotated plane. It is now sampled at each vertex's own
  (x, z) and added to the vertex's pristine Y, so the plate is a plate over the
  domain on all twenty. Measured through the real `setShape` at desktop
  `planeSegs` 160 and the real `applyHeightField`, probe field
  f = sin(1.3x) + cos(0.9z) + 0.2xz on each shape's own grid, the drawn
  displacement correlates **1.000000** with f(x, z) — that is, r ≥ 0.9999995,
  which is what rounds to six nines; only `plane` and `octahedron` read a bit-
  exact 1.000000000 — on **eight** of the twenty, and **≥ 0.997** on **ten**
  more. (Two earlier revisions of this bullet disagreed about which count was
  which, because each was measured in a sandbox and none of them had a home in
  the repository. This one does: `tests/surface-field-on-shapes.test.js`, "the
  census MATHEMATICAL_ACCURACY.md publishes is the census this tree produces",
  which fails if either count moves.) Two fall short: `star` reads **0.954** and
  `tetrahedron` **0.407**. Neither is a surviving permutation — it is the
  field's own lattice, because those two draw at grid 5 and grid 3 and what the
  correlation measures there is the bilinear interpolant. Holding the shape
  fixed and refining only the field lifts them to 0.99998 and 0.99996 at grid
  21 and to 1.000000 at grid 81; the old index identity does not converge that
  way at all — on `tetrahedron` it reads −0.361, 0.055, 0.204, 0.206, 0.197,
  0.182 at grids 3, 21, 41, 81, 161, 641 — which is what separates an
  interpolation error from a permutation. `tests/surface-field-on-shapes.test.js`
  is where these are re-derived: its "the drawn displacement is the field at the
  vertex own (x,z)" test drives the same `setShape` and the same probe field and
  fails on any shape that drops below 0.99 — `star` and `tetrahedron` excepted by
  name, since 26 and 12 sampled vertices cannot support a correlation, and both
  are covered instead by the tearing and coverage stencils in the same file — and
  its CONTROL replays the old identity so the stencil is shown able to fail.
  Coincident vertices take equal heights, and no vertex past gridSize² is left
  pinned at zero. `gridSize` still sets the *resolution* of the sampled field,
  which is why the grid list above still matters.
- The boot shape is `pyramid-smooth` (`RenderEngine`'s constructor), so the
  first plate any formula draws is grid **43 on mobile and 83 on desktop** —
  both odd since round 10, where both were even before it. Grid 43 carries a
  vertex at exactly x = 0; grid 83 misses it by 4.44·10⁻¹⁶ (see the
  `pseudosphere` and `landauLevels` rows), so "odd" and "samples the origin"
  are not the same statement here.

The interpolation ensures smooth visual output at the display mesh's full resolution regardless of the internal simulation resolution. The internal grid sizes (40×40 to 64×64) were chosen as the sweet spot where simulation accuracy meets real-time performance — doubling them would exceed the frame budget on mid-range hardware.

**Which lattice a percentage was counted on.** The plane itself is laid down at
`planeSegs` 80 on mobile and 160 on desktop (`src/main.js`), that is **81 and 161
vertices per side**, while the validation suite counts fold coverage on a 90-vertex
plate. Grid 90 is not a lattice the app draws, and it is even, so it has no vertex on
the origin and none on the lines x = 0 or z = 0 — which is why several entries below
read differently on it. `pseudosphere` is the clean case: its peak is 0.800000 — the
`soften` ceiling, reached because ln tan(T/2) → −∞ at r = 0 — on every odd grid the
app can draw and at all five reference settings, against 0.40032 at grid 42 (the
mobile boot shape), 0.54609 at 82 (the desktop boot shape) and 0.68129 at 198 at the
factory sliders; the even-grid figure is not one number but climbs with resolution,
and 0.5639 at grid 90 sits in the middle of that run. **Round 10:** grids 42 and 82
were the boot grids when those two figures were taken, and the catalogue no longer
produces either — closing the mesh of the three cones moved the boot grids to the
odd **43 and 83**, where this entry reads the ceiling 0.800000 like every other odd
grid. The even-grid shortfall is still real, but it is no longer what a viewer meets
at boot on either platform. `scherkSurface` is not that
case, and round 8 read a coincidence as a mechanism: 1.4867 at grid 81 and 1.4867 at
161 is one measurement rather than two, because grid 81 is a bit-exact subset of grid
161 — 7/80 is exactly 2·(7/160) in float64 and all 81 of its coordinates land on a
grid-161 vertex — while the other odd grids the app draws give 1.0589 (43), 1.1006
(65) and 1.1402 (83) and the even 74 and 198 give 1.3398 and 1.3431. Its peak jitters
with resolution and does not track parity at all. Every fold percentage in the
rows below is therefore given twice — the grid-90 figure the suite compares against,
and the same count on the two meshes the app lays down — and a figure that moves with
the clock is given as a range over one turn of it rather than as its value at t = 0.

---

## Tier Definitions

### The settings a letter is read at, and the rule that reads them

Three slider settings are named by number all through the rows below, and until
this revision none of them was written down anywhere in this file — which made
every headline percentage unreproducible. They are:

| Name | amp | freq | comp | Where the numbers come from |
|------|----:|-----:|-----:|------------------------------|
| **Factory sliders** | 0.70 | 1.00 | 0.50 | The `amp` and `waveInt` defaults in `src/params.js` with the audio path silent. This is what the app boots into. |
| **Audio envelope** | 1.05 | 1.30 | 0.90 | The loudest a track drives the sliders with nobody touching them. `src/math-visualizer.js` passes `amp × (1 + bass·0.5)`, `waveInt × (1 + treble·0.3)` and `comp = 0.5 + mid·0.4`, and the three band levels are capped at 1. |
| **Slider maxima** | 2.25 | 4.55 | 1.00 | Every slider at its top carried by those same gains: amp 1.5 × 1.5, waveInt 3.5 × 1.3. `comp = 1` is above anything the app can produce — the audio path is comp's only writer and stops at 0.9 — so this column is deliberately an over-reach rather than a reachable corner. |

The amplitude slider's floor is **0.2** (`src/params.js`), which is where the
low-amplitude figures in `romanSurface` and `crossCap` are read. A fourth
setting exists and is easy to confuse with the first: the validation suite's
general baseline is `{amp: 1, freq: 1, comp: 0.5}` (`tests/math-validation.test.js:58`),
amp **1** rather than the app's 0.7, and a few rows quote that corner by name.
Amp is a linear scale on most kernels, so the two differ by more than rounding
wherever a fold, a clamp or a threshold is involved. The plate is
laid down at `planeSegs` 80 on mobile and 160 on desktop (`src/main.js`,
`planeSegs: isMobile ? 80 : 160`),
that is **81 and 161 vertices per side**, while the validation suite counts fold
coverage at **90** — a lattice the app never draws. That is why every percentage
below names its grid, and why the same entry legitimately reads two different
numbers in two places.

**The rule that decides the letter.** A letter is a claim about the expression
an entry evaluates, so it is read **at the factory sliders**: take the drawn
plate there and compare it against the same kernel with `soften()` replaced by
the identity. If nothing has moved, the fold is asleep at boot and the letter is
whatever the entry's own numerical method earns — 10⁻¹² for A, 10⁻³ for B, C
otherwise. If something has moved, that deviation counts against the entry's
ceiling like any other error, which is why `manifoldCurvature` states its own
deviation instead of hiding behind a percentage. (`dragon` stood here with a
single 1.8×10⁻³ vertex until round 11 rebuilt it around a normalised density,
which folds nothing at all.)

**What the rule costs.** Boot is one point of a three-dimensional box, and the
rule reads only that point. Measured at grid 81 against the identity-`soften()`
copy (grid 161 agrees to a fraction of a point; `manifoldCurvature` takes no
clock and reads the same to the digit at every instant of one, but `complexSin`
carries a t·0.3 phase, so its two percentages are the band a 7 680-instant sweep
of one turn reaches — 13.47 % and 81.09 % are its t = 0 instants, and its two
worst-deviation figures round to the same value across that sweep):

| | factory | audio envelope | slider maxima |
|---|---|---|---|
| `complexSin` — stays **A** | nothing moves at all | 13.41…14.24 % of the plate past 10⁻³, worst 0.262 | 80.59…82.34 %, worst 967 |
| `manifoldCurvature` — stays **B** | nothing moves at all | 34.57 % past 10⁻³, worst 3.24 | 81.33 %, worst 2033 |

Both factory zeros are the fold being asleep and not the comparator being blind:
not one vertex of 6 561 moves by any amount, and halving the knee lifts the same
counter to 3.72 % and 0.55 % respectively. But one slider step away — at a
setting a listener reaches without touching anything — a third of
`manifoldCurvature`'s plate and an eighth of `complexSin`'s is a tanh rescaling
of the named function rather than the named function, and the letter does not
say so. That is the price of reading the rule at one setting. It is paid
knowingly, and it is why every entry that folds states its envelope and maxima
figures in its own row.

### What a caption is allowed to leave unsaid

A caption is the string the app renders under the surface — `info.textContent =
f.formula` — and the entry's `name` sits in the `<option>` directly above it.
Both are read as promises. Until this revision the catalogue had no rule for
which of a kernel's factors a caption had to name, and it had drifted into
disagreeing with itself in a way no reader could have caught by eye:
`hydrogenS` was forgiven for absorbing its 1/π into the display scale while
`maxwellBoltzmann` was charged, in the same round, for absorbing
4π(m/2πkT)^{3/2}. Those look identical on the page. They are not, and the
difference is measurable:

> **A caption may omit a factor if and only if `drawn ÷ named` is one positive
> constant over the whole plate and over the whole reachable slider box.**

| entry | `drawn ÷ named` | verdict |
|---|---|---|
| `hydrogenS` | 12.8177 at every vertex and every setting | omission forgiven |
| `feynmanPath` | constant to nine digits | omission forgiven |
| `gaussian` | 1.033984 at comp 0.5 → 1.199422 at comp 0.9 | must be named |
| `maxwellBoltzmann` | 0.548200 at comp 0.5 → 0.895630 at comp 0.9 | must be named |

The eye cannot see a slider dependence in a picture whose height is normalised
before it is drawn. The fit can, and that is the whole of clause 1: a fixed
display scale and the amplitude gain are part of the instrument, while a
cancelled normalisation and a comp-dependent gain are part of the mathematics.

**Clause 2. A caption must state anything that offsets, saturates, wraps,
blanks, or changes sign.** Those operations move zeros and orderings, and zeros
and orderings are what a reader reads off a surface — where it crosses, which
lobe is taller, where it stops. A tanh knee that compresses a peak is a display
choice; a −0.3 that moves the zero contour is a different function.

**Clause 3. A ±k % time gain is forgiven under clause 1 only where the named
object is not required to be time-independent.** A stationary state, a conserved
quantity, a fixed function and a coefficient spectrum must not breathe. This is
not a new position: it is the ruling already written into `math-collections.js`
when cos²(0.3t) was cut out of `hydrogenS` for exactly this reason. A caption
that names a bifurcation diagram or an orbit density, by contrast, may move as
much as it likes.

**The name is judged too.** Four entries carried a claim in the `<option>` rather
than in the formula — "Scherk Minimal Surface", "Catenoid (Minimal)",
"Pseudosphere (Tractricoid)", "Random Phase Fourier" — and a reader had no way
of knowing that one of the two lines was meant less seriously than the other.
Round 9 renamed all four, and `index.html` carries a second hand-maintained copy
of every name that has to move with it (`tests/math-validation.test.js:2273`).

**What the rule costs.** Three things, paid knowingly.

It is read on the *reachable* slider box, comp ∈ [0.5, 0.9], not on the maxima
column this document quotes elsewhere. A caption can therefore be true
everywhere a listener can drive the app and still fail at comp = 1.0, which no
audio path produces. Where an entry is measured at comp 1.0 its row says so.

It is mechanical only where the named quantity has a closed form to divide by.
For a caption naming an *object* — a Klein bottle, a minimal surface — there is
nothing to form a ratio against, and the entry is judged by the property that
defines the object: mean curvature for a minimal surface, the sign of the
Gaussian curvature for a pseudosphere, the identification for a Klein bottle.
Those are stated per row rather than by rule.

And it forgives motion that a stricter reading would charge. `doubleAngle`,
`bessel0`, `bessel1`, `sierpinski` and `cantorDust` all carry a small clock gain
over a named function that does not require one to be still; under clause 3 they
are clean, and under a strict contract they would each need a sentence. The
catalogue is an instrument that is watched as much as it is read, and a rule
that made every entry recite its envelope would be honest and unusable.

### What a number in a comment has to carry

The rule above governs the strings a viewer reads. This one governs the numbers
the *next author* reads, and it exists for the same reason: the catalogue had
no rule, and drifted into disagreeing with itself in a way no reader could have
caught by eye.

Round 10 put dozens of specific measurements into source comments —
correlations, coverage percentages, world-unit jumps, millisecond costs.
Eleven turned out to be false when someone re-measured them. The pass that
corrected those eleven introduced seven more while doing it. Not through
carelessness: each figure had been read in a private sandbox, under conditions
the comment did not record, by an author who was no longer there to be asked,
so every re-measurement was a fresh guess at what had been measured. A number
whose conditions are unwritten cannot be checked, only re-taken — and a re-take
under different conditions is indistinguishable from a correction.

> **A number in a comment must be re-derivable by a reader who has only this
> repository. It states the conditions it was read under, and it is either
> produced by a committed test or probe, or it is not written as a number at
> all.**

Conditions, concretely. Every row below has silently moved a number in this
repository at least once:

| condition | why it moves the number | measured example |
|---|---|---|
| **which tree** | a claim about what the code *used to* do is a claim about the code *and the geometry* of that commit | the old index identity reads **four** negative correlations on the pre-round-10 catalogue (`c629b53`) and **eight** on the repaired one — same rule, same probe, different bodies |
| **which shape** | the twenty geometries differ in vertex count, vertex order and coincident corners | on the same probe field the drawn/named correlation runs 1.000000 on `plane` and 0.407047 on `tetrahedron` |
| **which grid** | `gridSize = round(√vertexCount)`, so the lattice follows the shape *and* the platform, and its parity decides whether x = 0 is sampled at all | "0 for 100 of 192 formulas" reads 100 at grids 21, 83, 90 and 198 and 99 at 43, 81 and 161 |
| **which sliders** | amp, freq, comp — and the factory 0.70 and the validation suite's 1.00 are different corners, not rounding | the same per-frame drift is 0.49461 at amp 0.70 and 0.70659 at amp 1.00 |
| **which probe field** | a mechanism can only be measured with a field that has no symmetry the defect could hide in | `sin(1.3x) + cos(0.9z) + 0.2xz` for correlation, `f = x` for tearing, a constant for coverage |
| **which machine** | wall-clock cost, and nothing else | 4.85 ms and 5.22 ms for the same call on the same device, in two sessions |

**A number needs a home.** "Produced by a committed test or probe" means a
reader can run one command in a fresh clone and watch the figure appear. A path
into a reviewer's notes directory is not a home — `wave2/numbers/m9-grid-guard.txt`
was cited from `sampleHeightField` and is not in this repository and never was.
The three habitable forms are: a test that prints the number in its own failure
message, a test whose name states the property the number quantifies, and a
comment that gives the recipe in enough detail to be re-run by hand.

**What to do when a number cannot meet the bar.** Deleting it is usually wrong:
"the double write was expensive" tells the next author less than a wrong
millisecond does, because it does not even say what to measure. In descending
order of preference:

1. **Give it a home.** If a figure is worth stating it is usually worth a test,
   and the test then carries the conditions in its own setup where they cannot
   drift away from the number. The plane-and-sphere jump quoted in
   `_restorePristineToMesh` is printed verbatim — 0.9795 and 0.9640 — by
   `tests/blend-from-state.test.js` the moment the clear it documents is removed.
2. **Name the conditions inline**, where a test would be disproportionate, and
   name them completely enough to rebuild the measurement: tree, shape, grid,
   sliders, probe field.
3. **Quote the ratio rather than the reading**, where the absolute value belongs
   to the machine and the ratio belongs to the code. A fraction of the call it
   rides along with survives a change of device; a millisecond does not.
4. **Say the shape of the thing without a digit** — "one frame of the formula's
   own motion", "the whole range of the field", "inside the unchanged tree's own
   spread" — when even the conditions are not stable. A qualitative claim that is
   true beats a quantitative one that is true only in a sandbox nobody kept.

**Never replace a vague statement with a crisp false one.** That trade reads as
progress in a diff and is the most expensive mistake this document has recorded.
Where a figure will not resolve, the honest edit is the one that says which
question is still open.

**Cite by symbol, not by line.** `src/render.js:1116` was right when it was
written and pointed at unrelated code one round later. Round 10 displaced eight
such citations in the grid bullets of this file alone — four into `render.js`,
four into `math-visualizer.js` — and replaced them with symbol names for exactly
this reason; repairing a rotted citation by writing a fresh line number only
resets the clock. Name the function, the method, the `case`, the test —
`RenderEngine`'s constructor, `_buildShapeGeo`'s
`case 'plane'`, `tests/surface-plumbing.test.js`'s "a field that is not
gridSize² still writes a number, never NaN" — or give the grep that finds it
(`grep -n 'Math.round(Math.sqrt' src/math-visualizer.js`). A line number is
acceptable only where nothing else identifies the site, and then it is written
together with the text it points at, so a reader can see at a glance that it has
moved.

**What the rule costs.** Three things, paid knowingly.

Comments get longer, and the longest are on the smallest functions — the
`grid < 2` guard in `sampleHeightField` is one line of code under a paragraph
of conditions. That is the right ratio when the line exists because of a
measurement, and it would be absurd on a line that does not.

It discourages cheap measurement. A figure taken in five minutes now has to
earn either a test or a paragraph, and the honest consequence is that some true
things go unquoted. Clause 4 is there so that the alternative to a homeless
number is a true sentence rather than silence.

And it cannot be enforced mechanically. No test can tell a conditioned number
from an unconditioned one, so this section is read by people, like the caption
contract above it — which is why both are written as rules a reviewer can apply
rather than as taste.

### Tier A — Machine Precision (Exact)

The formula is implemented as a closed-form analytic expression, or as an iterative method that converges to machine precision well within the iteration budget. Validation: direct comparison against reference implementation (scipy, mpmath, Wolfram) yields agreement to ~14 significant digits for non-pathological inputs.

Examples: `gaussian` (direct PDF formula), `chebyshev` (cos(n·acos(x))), `lambertW` (Halley iteration converges quadratically — 6 steps gives ~10⁻¹⁴).

### Tier B — Bounded Approximation

The formula uses a numerical method whose error is documented in the literature and bounded within stated parameter ranges. Truncated series, polynomial fits (e.g. Abramowitz & Stegun), midpoint integration, well-converged iterative maps, PDE simulations on adaptive grids with bilinear interpolation to display resolution.

Examples: `besselJ0` (Numerical Recipes polynomial — max error 1.5×10⁻⁷), `erf` (Abramowitz Horner — max error 1.5×10⁻⁷), `henon` (20-iteration map, attractor reached), `reactionDiffusion` (Gray-Scott PDE on optimised internal grid, ~10⁻³ accuracy limited by grid discretisation and Euler integration).

### Tier C — Visualization-Grade

The formula is qualitatively faithful — it produces visually correct shape and structure — but cannot be numerically validated against canonical references because of one or more of:
- Iteration budget too low for chaotic system convergence (e.g. Lorenz attractor with 8 Euler steps).
- Audio modulation alters core formula parameters (e.g. magnetic dipole moment `m = amp·sin(t)`).
- Output is decorated by an envelope or threshold function that breaks direct correspondence.
- Simplified model that captures qualitative behaviour but omits canonical normalization or higher-order terms.

These formulas are honest **artistic interpretations** of mathematical structures, not measurements of them.

### Tier D — Defect

Either the implementation does not match the stated formula at all, or there is a numerical bug. Must be fixed before any accuracy claim is made. Currently zero.

---

## Per-Collection Breakdown

Tier ratings shown as: 🟢 A · 🔵 B · 🟡 C · 🔴 D

### 1. Fractals & Chaos (16) — 0 A · 6 B · 10 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `mandelbrot` | Mandelbrot Escape | 🟡 C | maxIt = 6–16. Canonical fractal needs 100–10000 iter for crisp boundary. |
| `julia` | Julia Set (animated) | 🟡 C | Same as above + time-varying c. |
| `burningShip` | Burning Ship | 🟡 C | Same iteration budget issue. |
| `lorenz` | Lorenz Attractor Density | 🟡 C | Round 8: eight Euler steps of dt = 0.004 is T = 0.032, so the entry drew the flow map of a plane of initial conditions — least-squares plane R² 0.9788, non-planar residual 6.1e-3 rms against a 0.174 peak-to-peak — and `lorenzY` took `t` without ever reading it, so the plate at t = 17.3 was the plate at t = 0 bit for bit. Same repair as `rossler` and `chua`: one RK4 orbit (σ = 10, ρ = 28, β = 8/3, not the truncated 2.667; dt = 0.01, 12 000 steps) splatted as a density in the (x, z) projection through `createCachedHeavySampler`, drawn as the square root of the cell occupancy so the fold does not swallow the band. The orbit agrees with scipy DOP853 at rtol = atol = 1e-12 to 3.6e-4 over T = 0.5, 1.1e-5 of its own radius; past the Lyapunov time no integrator agrees with another and only the invariant measure is reproducible, so that is what is checked — the drawn density matches a T = 960 reference orbit to correlation 0.969, where reference segments of the same 12 000 samples score 0.961 [0.955, 0.968]. Plane R² → 0.0020, peak 0.644, 0.7–1.2 ms per 90-plate beside `rossler` at 0.7–1.1 and `chua` at 1.1–1.8. Stays C: a vertex height is a 12 000-sample density estimate, not a value good to 1e-3. `freq` no longer reaches the entry — the cached-sampler trade the other heavy entries already make. |
| `rossler` | Rössler Attractor | 🟡 C | Round 6 replaced twelve Euler steps of dt = 0.003 — T = 0.036, about a two-hundredth of one loop, so the plate was the flow map's own linearisation (least-squares plane R² = 1.000000, non-planar residual 3.9e-5 rms against a peak of 0.0192) — with one RK4 orbit (a = b = 0.2, c = 5.7, dt = 0.02, 8 000 steps) splatted as a density in the (x, y) projection through `createCachedHeavySampler`, drawn as the square root of the cell occupancy (sd 0.166 linear against 0.189 with the root). Round 7 froze c at 5.7: on a slider it made every rebuild a fresh realisation of a different invariant measure — 39 % median plate change between consecutive rebuilds with nothing playing. Round 9 re-measured it at grid 90, factory sliders: plane R² 0.018183, peak 0.633696, and the 48² accumulator's lit-cell count and maximum are identical at all five reference settings, so neither `freq` nor `comp` moves the plate — the cached-sampler trade the other heavy entries make. Stays C: a vertex height is an 8 000-sample density estimate, not a value good to 1e-3. |
| `newtonFractal` | Newton Fractal z³−1 | 🟡 C | 4–12 iterations. Inside basins converges fast; near boundary needs much more. |
| `sierpinski` | Sierpiński IFS | 🔵 B | Math is exact; depth 2–6 limits resolution. |
| `lyapunov` | Lyapunov Exponent Map | 🟡 C | Round 6 fixed three things. The parameter window ran to 5.6, where the logistic orbit escapes to −∞ and the exponent is not an exponent of anything — 73.6 % of the plate sat pinned at the +0.8 clamp, i.e. most of the picture was the clamp. It is now [2.6, 4.0]. The average began at the first iterate with no transient discarded, which put the wrong SIGN on 10.2 % of vertices (order reported as chaos); 48 iterations are now burned in first. And `if (isFinite(lam)) n++` counted a step whose own term was infinite; the guard is now on the term. Clamp → `soften(0.5, 0.9)`. Round 8: 48 iterations at comp = 0 and 96 at comp = 1 cannot pin a chaotic Lyapunov average to a bounded error — the ~1/√n scatter puts 10 % of the plate above 1.5e-2 in drawn units against a limit computed by mpmath quadrature of the exact invariant density (control: λ(r=4) = ln 2 to 8.8e-23). Truncated chaotic iteration is the definition of tier C in this document's own table. **Round 8 — the a = 4 edge.** Moving the window to [2.6, 4.0] put its right edge exactly on the degenerate value: x₀ = 0.5 is the critical point of the logistic map, 4·½·½ = 1, then r·1·(1−1) = 0, and the orbit sits on the repelling fixed point 0 for ever, so every \|f′\| is exactly r and λ collapses to the closed form ½(ln a + ln b). No burn-in escapes an exact fixed point. The corner returned ln 4 = 1.3862944, exactly twice the true ln 2 = 0.6931472 the tent-map conjugacy gives, and all 21 vertices of the x = 3.5 edge (25 of 25 on a 25² plate) were wrong by more than 0.05 drawn units, median 0.137. Seed is now x₀ = 0.3 — a generic point of the interval, and dynamically identical to 0.7 since f(1−x) = f(x), so the choice is not a lucky orientation. Measured against the limit itself at 40 dps (2×10⁵ iterates after 2×10⁴ burn-in, three unrelated seeds, controlled to 1×10⁻³⁷ against ln\|2−r\| and ½ln\|4+2r−r²\| and to 1.9×10⁻⁵ against ln 2), over whole plates at the factory sliders, excluding the 5.4–5.6 % of vertices where the limit is itself basin-dependent: x = 3.5 edge median 0.137 → 0.004–0.008 and vertices over 0.05 on that edge 21/21 → 0/21 and 25/25 → 0/25; p90 over the plate 0.0295 → 0.0105 (21²) and 0.0170 → 0.0084 (25²); worst vertex 0.331 → 0.072 and 0.527 → 0.189; sign disagreement with the limit 4.20 % → 2.10 % (21²) and 3.51 % → 0.92 % (25²), and at comp 1 on the 25² plate 2.95 % → 0.55 %. The corner now reads λ = 0.6634 / 0.6823 / 0.6923 at comp 0 / 0.5 / 1 against ln 2 = 0.6931. Median error is the one number that does not improve (0.00035 → 0.00015 at 25², but 0.00012 → 0.00011 at 21²): x₀ = 0.5 is the critical point and therefore lands on a periodic attractor faster than a generic seed, so at a minority of vertices inside periodic windows it was exact and 0.3 is still in transient after 48–96 iterates. Seeds near 0.5 were measured for that reason and rejected — 0.499, 0.49, 0.48, 0.46, 0.51, 0.52 all leave a worse plate (worst vertex 0.463, sign disagreement 2.0–4.2 %) and 0.499 still leaves edge vertices over 0.05, because escaping the neighbourhood of 0 also takes iterations. Peak, non-finiteness and grid behaviour are unchanged: peak 0.579 at amp 0.7 and 0.899 at amp 2.25, zero non-finite vertices over the whole slider box, and the 90² and 161² peaks are bit-identical before and after. The tier is unmoved — 48–96 iterates still cannot pin a chaotic average, and the survivors are exactly that: long transients into periodic windows, where the 40-dps limit is unambiguous (seed spread 0) but the drawn average has not arrived. **Round 8 — the cost of that seed change, which the paragraph above states nowhere, measured on the lattices the app actually lays down.** Every per-grid statistic above is a 21² or 25² plate, and the plane the app renders is 81² (mobile) or 161² (desktop); re-measured there against a 10⁵-iterate limit from four seeds that are neither 0.5 nor 0.3 (controls: ln\|2−r\| at r = 2.6/2.9 to 2×10⁻¹², ½ln\|4+2r−r²\| at r = 3.2/3.4 to 3×10⁻¹², ln 2 at r = 4 to 6×10⁻⁷; sensitivity control: flipping the limit's sign on a quarter of the plate moves the disagreement statistic to 29 %), at amp 0.7: **grid 81** — 291 vertices worse by more than 0.01 drawn units against 521 better at comp 0.5 (405/598 at comp 0, 257/472 at comp 1), 169 vertices newly on the wrong side of zero against 136 rescued, and the plate-wide sign disagreement moves the *wrong* way, 4.53 % → 5.03 % (4.62 → 5.37 at comp 0, 4.15 → 4.68 at comp 1); **grid 161** — 1161 worse against 2137 better at comp 0.5, 551 newly wrong-signed against 665 rescued, sign disagreement 5.66 % → 5.22 %. So the change is not free and the 25²-plate figure of "21 worse against 44 better" understates the count on a shipped plate by more than an order of magnitude. It is still the right change: on both shipped grids the p90 error halves (0.0252 → 0.0197 at 81², 0.0307 → 0.0147 at 161²), the worst vertex falls (0.423 → 0.266 and 0.672 → 0.593), and the a = 4 edge the seed was changed for goes from every vertex wrong by more than 0.05 to three of 81 and four of 161, median 0.137 → 0.005. The damage is speckle in the periodic windows; the repair is the whole right-hand edge. **Round 8 — the fold, which no revision of this row had stated.** The fold covers a measured **0.2 %** of the plate at the factory sliders and 6.8 % at the slider maxima, both counted at grid 90; on the meshes the app lays down the same counts read 0.15 % and 6.86 % at grid 81 and 0.08 % and 6.82 % at 161. The kernel takes no `t` and the plate is bit-identical at t = 0.25, 3.7, 11.3 and 17.9, so there is no cycle to average over. The factory figure is ten vertices of 6561 and twenty-two of 25921 — 0.08 / 0.15 / 0.21 % across grids 161 / 81 / 90 is a ratio of 2.5 on a span of 0.13 pp, which is what a count of ten vertices is worth. It does not touch the letter: measured against the same kernel with `soften()` replaced by the identity, 0.03 % of the plate is drawn further than 10⁻³ from the un-compressed value at the factory sliders, 0.52 % at the audio envelope and 5.01 % at the maxima — the letter is C for the chaotic average above, not for the fold (control: halving the knee lifts the factory figure to 0.56 %, so the comparator would have seen a fold had one been awake). |
| `dragon` | Dragon Curve Density | 🔵 B | Heighway IFS, f₁(z) = (1+i)z/2 and f₂(z) = 1 − (1−i)z/2, drawn as the chaos-game density of ONE orbit on a 128² grid over [−0.55, 1.40] × [−0.55, 0.90], normalised on the 95th percentile of its non-empty cells and read bilinearly; 60k…180k points as `comp` moves, cached per point count, the attractor centred in frame with `freq` as the zoom. **Round 11 — the kernel this replaces was not drawing the set at all.** It ran a private 8–14 step chaos game at every vertex, seeded from that vertex's own quantised coordinates, and at λ₁ = 0.421 twenty steps stretch the 0.0787 grid spacing by e^{8.42} ≈ 4.5×10³ — so neighbouring vertices sampled independent points of the attractor. Measured on the shipped kernel at grid 90: 83.1 % of the plate exactly 0, neighbour correlation 0.027 across x and 0.088 across z, plate σ 0.0278. What a viewer met was static speckle on a radial ramp, at every zoom. The distribution was right — KS 0.0206 at p = 0.64 against 20 000 true attractor points — which is exactly what a per-vertex sampler earns: the correct measure, one point at a time, and never the set. Controls for the replacement, against a dragon built with no IFS at all (the paper-folding sequence at 16 levels): raster Jaccard 0.971, box [−0.3330, 1.1663] × [−0.3332, 0.6665] against the canonical [−1/3, 7/6] × [−1/3, 2/3]. The first run of that control read 0.179, because the reference was mirrored in y — the oracle, not the kernel. Bounds: the density is in [0, 1] by construction, so the drawn height is at most amp·1.2 — 0.8369 at the factory sliders, 2.6737 at the slider tops — and the round-8 compression at knee 0.9, ceiling 1.8 is gone together with the unnormalised sum of Gaussians it was guarding. No clock, as before: the plate is bit-identical across the sweep. |
| `chua` | Chua Circuit Attractor | 🟡 C | Round 6 replaced ten Euler steps of dt = 0.003 — T = 0.030, plane R² 0.999839 with a 6.3e-4 rms non-planar residual against a peak of 0.0118 — with one RK4 orbit (α = 15.6, β = 28, m₀ = −1.143, m₁ = −0.714, dt = 0.01, 12 000 steps) splatted as a density in the (x, z) projection through `createCachedHeavySampler`, drawn as the square root of the cell occupancy. The scroll is drawn in (x, z) because y spans only ±0.389 on the attractor against ±2.263 and ±3.638 for x and z. Round 9 re-measured it at grid 90, factory sliders: plane R² 0.000614, peak 0.663719; against an independently written RK4 carrying the same constants the drawn density correlates 0.9857, while moving α to 15.0 gives 0.9027, β to 27 gives 0.8419, the diode slope to −0.75 gives 0.6564 and removing the diode's kink altogether destroys the double scroll. The 48² lit-cell count and maximum are identical at all five reference settings, so neither `freq` nor `comp` moves the plate. Stays C: a vertex height is a 12 000-sample density estimate, not a value good to 1e-3. |
| `cantorDust` | Cantor Dust | 🔵 B | Base-3 decomposition exact; depth 2–6 limits resolution. |
| `ikeda` | Ikeda Map | 🔵 B | 8 iterations adequate. |
| `logistic` | Logistic Map Bifurcation | 🟡 C | 40–80 iterations sufficient for attractor, but output `exp(-50·(x-target)²)` is decorative envelope, not direct map value. **Round 9 — the seed.** x₀ = 0.5 is the critical point of the map and the r window's right edge is r = 4 exactly, where 4·½·½ = 1, then r·1·(1−1) = 0 and the orbit sits on the repelling fixed point 0 for ever, so the chaotic edge column drew a single bump: 28 of 81 vertices above 1e-3 against 81 of 81 at its neighbour x = 3.4125, and one distinct iterate over the 24 sampled against 24 spanning [0.000789, 0.999803]. The seed is now x₀ = 0.3, the substitution round 8 already made in `lyapunov`, generic and dynamically identical to 0.7 since f(1−x) = f(x). The cost is not one column, and the small-plate figure that says so understates it: 1341 of 6561 vertices move by more than 1e-3 at the factory sliders on grid 81 and 5790 of 25921 at grid 161, of which 80 and 159 sit on the r = 4 edge — a subset of the chaotic count below, not a bucket beside it. **Round 9 tail — the whole population, not most of it.** The movers split by the ATTRACTOR of their own column, the period found by cycle closure to 1e-12 after 2×10⁵ iterates (control: r = 3.2 → period 2, 3.5 → 4, 3.5644 → 16, 3.8320 → 3, and 3.9 and 4.0 aperiodic; no column below r∞ = 3.5699 is misread as aperiodic). At grid 81 and the factory sliders 1128 of the 1341 fall in the 19 aperiodic columns — the chaotic band, where both seeds sample the same attractor at different points and only the speckle changes — and the other 213 fall in PERIODIC columns, where the burn-in (40 + ⌊40·comp⌋, 60 at this comp) has not reached the cycle: six columns r = 2.9500–3.0438 astride the period-1 → 2 bifurcation at r = 3, r = 3.4375 beside the period-2 → 4 one at r = 3.44949, and the period-16 and period-10 windows at r = 3.6625 and 3.9063 **above** r∞. The earlier accounting — 1143 of the 1341 at r ≥ 3.5699, all of it called chaotic band — therefore both left 198 movers unexplained and counted 15 window vertices as chaos. Grid 161 at the same sliders: 5790 = 4718 chaotic + 1072 periodic, the periodic ones at r = 2.9406–3.0438, 3.4281–3.5406, 3.6625 and 3.9063. Those periodic movers are transient and not a difference of attractor: 5000 extra iterates for **both** seeds take 213 → 0 and 1072 → 0 (worst \|Δ\| 7.0×10⁻⁵), while the chaotic count does not collapse under the same 5000 — 1128 → 1128 at grid 81 and 4718 → 4816 at 161 — so the probe is able to fail. The transient part is small and nearly lit-neutral: worst \|Δ\| 0.0119 at grid 81 with every one of those columns keeping its lit count (577 → 577), and worst 0.0937 at grid 161, where r = 3.4844 (period 4) is the only column to lose real ground, 153 → 148 lit, the periodic columns together going 2429 → 2420. The split holds across the settings box as total = chaotic + periodic — grid 81: 1442 = 1178 + 264 at amp 1, 1373 = 1195 + 178 at the audio envelope, 1561 = 1293 + 268 at the reachable over-drive and 909 = 825 + 84 at the floor; grid 161: 6291 = 4971 + 1320, 6028 = 5071 + 957, 6960 = 5557 + 1403 and 3931 = 3441 + 490. Lit share 82.81 % → 83.62 %, mean height 0.172804 → 0.177029, per-column lit count moving 0.65 vertices on average, peak unchanged at 0.35 (1.125 over the whole settings box), no non-finite vertex. Tier unmoved: the height is still a 24-iterate density estimate. |
| `duffing` | Duffing Oscillator | 🟡 C | Round 8: 15 Euler steps of dt = 0.01 is T = 0.15, a fortieth of one drive period, so the plate was the affine free-drift sheet — plane R² 0.999905, and deleting the whole restoring term −x + x³ moved it by 1.20 % of its own frame, the cubic alone by 1.74 %, the drive by 0.24 %. Same defect and same repair as `rossler` and `chua`: one long RK4 orbit splatted as a density in the (x, ẋ) plane through `createCachedHeavySampler`. Plane R² 0.999905 → 0.007436, peak 0.391 → 0.634 at the factory sliders, 1.21 → 1.81 ms per 90-plate and 5.74 → 2.46 ms per 200-plate. The constants are frozen at Holmes's two-well set δ = 0.25, γ = 0.30, ω = 1 (measured largest Lyapunov exponent +0.133); γ used to ride `comp`, which is the chaotic-parameter-on-a-slider fault FIX(r7) named for `rossler`. Still C, but now for the sample count rather than the horizon: the integration is B-grade (RK4 at dt = 0.01 tracks DOP853 to 1.1×10⁻⁸ over a drive period and a half), while 24 000 splats estimate the invariant measure to a correlation of 0.729 against 370 000 DOP853 samples — where 24 000 DOP853 samples themselves reach only 0.769 ± 0.03. Trade: like the other thirteen cached entries it no longer responds to Wave Intensity, and with γ frozen it no longer responds to Compression. |
| `henon` | Hénon Map | 🔵 B | 20 iterations on canonical attractor. |
| `tinkerbell` | Tinkerbell Map | 🔵 B | 12-iteration map on canonical Tinkerbell attractor. Post-loop `isFinite` guard added — no longer returns `Infinity`. |

### 2. Special Functions (16) — 10 A · 6 B · 0 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `bessel0` | Bessel J₀ | 🔵 B | Numerical Recipes polynomial fit, max error ~10⁻⁷. |
| `bessel1` | Bessel J₁ | 🔵 B | Numerical Recipes J₁ polynomial fit, max error ~10⁻⁷. Replaced finite-difference approximation. |
| `legendre2` | Legendre Pₙ Surface | 🟢 A | Closed-form polynomials P₀–P₆. n = round(1 + comp·4), so the reachable comp range renders P₃–P₅. |
| `gamma_fn` | Gamma Function | 🟢 A | Lanczos g=7, ~10⁻¹⁴. Computes Γ(n) over n ∈ [0.2, 3.8], including the minimum 0.8856 at n = 1.4616 — it used to plot 0.12·ln\|Γ(n)\| under this caption. Round 9: the HEIGHT is the affine image 0.22·Γ(n) − 0.6, not Γ itself, and that is now in the caption. Γ is strictly positive on the whole window, so the −0.6 puts 82.72 % of the plate below zero (67 of the 81 columns, identically at every setting measured) and invents crossings at n = 0.3276762316 and n = 3.3156166911 where Γ has none — mpmath findroot, with the control that the same routine returns the known roots of Γ(n) = 1 as exactly 1.0 and 2.0. The letter does not move: the omitted −0.6 is one constant over the whole reachable slider box (drawn ÷ (0.22Γ − 0.6) is amp to 1.000000000000), unlike `gaussian` and `maxwellBoltzmann`, whose omitted factors ride `comp`. |
| `erf` | Error Function | 🟢 A | All-positive series 2/√π·e^{−x²}·Σ2ⁿx^{2n+1}/(2n+1)!!, measured 7.7×10⁻¹⁵. Replaced an Abramowitz & Stegun Horner fit whose own bound is 1.5×10⁻⁷ — a tier-B number under a tier-A rating. |
| `zeta` | Riemann Zeta (real axis) | 🔵 B | Euler–Maclaurin: 15 direct terms, integral tail, two Bernoulli corrections. ~10⁻¹⁰ across the window. Replaced a 14–22 term Σ 1/n^s that was 85 % below ζ(1.05) — the domain shift to [1.05, 5.05] removed the divergence and left the convergence rate untouched. `comp` now stretches the s-window instead of setting the term count. |
| `airy` | Airy Function Ai(x) | 🔵 B | Maclaurin series from the exact (Ai(0), Ai′(0)) seed, six-term DLMF 9.7 asymptotics beyond, with the handover at \|x\| = 7 on the right and 10 on the left. Worst absolute error against mpmath: 1.8e-13 over the default window \|x\| ≤ 5.25, 1.2e-11 on the right branch, 3.3e-8 on the left. Round 8: one handover served both signs, and they fail in opposite directions — Ai decays on the right, so the alternating series loses everything to cancellation (relative error 1.1e-3 at x = +8), while on the left it oscillates at O(1) and the series still beats the asymptotic out to \|x\| = 10. That single threshold left 1.26e-7 under a row claiming ≤10⁻⁸. **Round 9 — the ±0.8 clamp, which this row never mentioned.** It does not bind at the settings the letter is read at: 0 of 6561 grid-81 vertices touch it at the factory sliders (drawn range [−0.2050, 0.2623]), at amp 1, at the audio envelope or at the amplitude floor. It binds only at the reachable over-drive 2.25/4.55/0.90 — 7 of 6561 vertices sit exactly on +0.8 — so the saturation is real but confined to a corner, and clause 2 is satisfied by stating it here rather than in a 24-character caption. |
| `hypergeometric` | ₂F₁(a,b;c;z) | 🔵 B | Euler transformation (1−z)^{c−a−b}·₂F₁(c−a,c−b;c;z), 120-term cap, relative early exit at 10⁻¹². Worst reachable point 6.5×10⁻⁵ — that is the series, not the plate. Round 9: the ±0.8 output clamp sits OUTSIDE the z-envelope, so it draws a literal flat tabletop, and it binds within the reachable slider box — 0 of 6561 grid-81 vertices at factory, amp1, audio and the amplitude floor — though the ARGUMENT clamp z = ±0.95 does bind at the audio envelope, but 32 of the 81 columns of the z = 0 row and 790 of 6561 plate vertices at amp 2.25 / comp 0.9 (918 at the unreachable comp 1.0), understating the right edge by 1.613× (drawn 0.800000 at x = +0.875 where mpmath hyp2f1 gives 1.290265). The caption now states the clip. Control for the census: `dawson` carries the identical clamp shape and never reaches it at any setting. The previous 12-term cut never reached its 10⁻⁸ exit: the twelfth term at z = 0.875 is 2.5×10⁻². |
| `laguerre` | Laguerre L_n | 🟢 A | Closed-form three-term recurrence, and n = round(1 + comp·4) so the reachable comp range renders L₃–L₅. Round 9: the ±0.7 output clamp never binds at the factory or audio sliders (0 of 6561 grid-81 vertices) and replaces the L₅ trough on 20 of the 81 columns of the z = 0 row and 566 of 6561 plate vertices at amp 2.25 — drawn −0.700000 at x = +3.15 where 0.15·amp·L₅(5.7) = −1.326145 is due, 1.894×. The caption now states the clip and the degree range. (L₅(5.7) = −3.92931725 by mpmath; L₅(6) = −3.8, confirmed by mpmath, scipy, sympy and an exact-rational sum, and the deepest DRAWN vertex is at xv = 5.7, not at the right-hand edge.) |
| `chebyshev` | Chebyshev T_n | 🟢 A | Direct cos(n·acos(x)), \|x\| ≤ 1. Exact on the rim now: the ±(1−10⁻⁹) guard inside acos cost 2.5×10⁻⁸ across the whole saturated edge, and the argument was already clamped. |
| `sinc` | Cardinal Sinc (radial) | 🟢 A | The **normalised** sinc, sin(πr)/(πr), with r = √(x²+z²)·freq·2 — the "sombrero", which is what it always drew. (The caption said sinc(x), and did not say which of the catalogue's two sinc conventions this is; both now say.) Measured against mpmath `sincpi` at 40 dps over the reachable slider box: worst \|Δ\| **3.7×10⁻¹⁶**. Round 8 removed the +10⁻⁸ that was added to r: it was the entire error of the entry — 8.22×10⁻⁹ at amp 1, at r ≈ 0.66 where \|d sinc/dr\| is largest, and −6.0×10⁻⁹ on the ring r = 1 where sinc is exactly zero — and it bought nothing, since sin(πr)/(πr) is 0/0 at r = 0 and nowhere else. That single vertex is now special-cased at 1. |
| `ellipticK` | Elliptic K(k) | 🔵 B | Midpoint rule N=16. ~10⁻⁴ accuracy for k<0.95. The k floor of 0.01 bites only the leftmost column of the plate and costs 1.77×10⁻⁵ there — inside this row's tier, so round 8 left it alone. |
| `dawson` | Dawson F(x) | 🟢 A | Rybicki lattice sum, one algorithm for the whole line, ~3×10⁻¹⁵ over \|x\| ≤ 24. Replaced a Taylor/asymptotic pair with a 3.2×10⁻⁵ step at their |x| = 3.5 seam, inside the reachable domain. |
| `clausen` | Clausen Cl₂(θ) | 🟢 A | Log-sine expansion θ − θ ln θ + θ·Σ ζ(2n)/(n(2n+1))·(θ/2π)^{2n}, exact to 10⁻¹⁶ at Catalan's constant. Replaced a 12-term Fourier sum, which converges like 1/N and so had no accuracy at the ends of the period. |
| `polygamma` | Digamma ψ(x) | 🟢 A | Recurrence lifting x to ≥ 12, then the Bernoulli asymptotic through B₁₀. Worst error 3.6e-15 against mpmath over the drawn range (control: ψ(1) = −γ and ψ(½) = −γ−2ln2 to 40 digits). Round 8: the lift stopped at 8 with four terms, leaving the first dropped term B₁₀/(10·8¹⁰) = 7.06e-12 — and the measured error was 6.77e-12, that term and nothing else, under a letter promising ~10⁻¹⁴ while this row's own prose said ~10⁻¹⁰. The left 3.7 % of the plate (x ≤ −3.245) still rests on the ±0.6 display clamp, since ψ(0.2)·0.2 = −1.06. |
| `lambertW` | Lambert W(x) | 🟢 A | Halley iteration converges quadratically — 6 steps → machine precision. |

### 3. Probability & Statistics (16) — 9 A · 5 B · 2 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `gaussian` | Gaussian Bell | 🔵 B | Not a PDF: the wrapper is exactly σ√(2π)·0.55, which cancels the 1/(σ√2π) the formula string names. The omitted factor is not a constant either — σ rides `comp`, so it runs 1.034 → 1.199 (+16 %) across one sweep of the mid band. Same criterion this file already applies to `studentT` and `vonMises`. |
| `bivariate` | Bivariate Gaussian | 🟢 A | Closed-form 2D Gaussian with correlation. |
| `cauchy` | Cauchy | 🟢 A | 1/(π(1+x²)). |
| `laplace` | Laplace | 🟢 A | (1/2b)·exp(-\|x\|/b). |
| `maxwellBoltzmann` | Maxwell–Boltzmann | 🔵 B | v²·exp(−v²/2a²) — the kernel, not the density: the wrapper is exactly 0.6·a³·√(π/2)... i.e. the √(2/π)/a³ of the distribution is cancelled. The omitted factor rides `comp` too, 0.548 → 0.896 (+63 %). |
| `poisson` | Poisson PMF | 🟢 A | Log-domain stable computation. **Note**: output multiplied by `(k%2===0?1:-1)` for visual contrast — sign-flipped, not the PMF. |
| `randomWalk` | Brownian Motion (seeded) | 🟡 C | LCG-driven walk — statistically not Wiener process realisation, just a deterministic path with similar shape. |
| `ornsteinUhlenbeck` | Ornstein–Uhlenbeck | 🔵 B | Round 6: there was no noise in what was drawn. The seed advanced ~8 per vertex out of a 65536 period read through `&0xffff`, so every vertex on the row got the same twenty increments and what varied was the initial condition relaxing smoothly — total variation over span 1.09, the signature of a monotone curve. x is now the time axis of one sample path integrated from the left edge, so neighbouring vertices share history: total variation over span 9.91, stationary variance 1.84e-2 against σ²/(2θ) = 1.78e-2, autocorrelation at lag 1.0 of 0.194 against e^{−θ} = 0.223. B rather than A because explicit Euler at dt = 0.05 with uniform increments is what leaves those two gaps. Round 8: the generator is sound — the hash32 increments pass a KS test against U(−1,1) at p = 0.86 — but explicit Euler at dt = 0.05 does not reproduce the process exactly: stationary variance 2.17e-2 against the AR(1) value 1.85e-2 and lag-1 autocorrelation 0.246 against 0.210, both about 17 % out. That is the discretisation, not the noise, and it is what tier B here means. |
| `chiSquare` | Chi-Squared | 🟢 A | Closed-form via gamma, on the window x ∈ [0.01, 10] — the window is part of the entry, not a rounding guard: the density is x^{k/2−1}e^{−x/2}/(2^{k/2}Γ(k/2)), which is +∞ at x = 0 for k = 1 and 0·log 0 there for k = 2, so the origin is genuinely outside the domain being drawn. Round 8 checked the alternative and left it alone: dropping the floor makes the k = 2 column NaN. The cost of the floor is confined to the single leftmost column of the plate and is a statement about where the window starts, not about the closed form, which is exact everywhere inside it. **Round 8 — that cost was quoted at one k.** `comp` sweeps k = round(1 + 7·comp) over 1…8, so the floor costs a different amount at every slider position, and 2.98×10⁻⁴ is the k = 5 value, which is where the factory `comp` = 0.5 happens to land. Measured against `scipy.stats.chi2.pdf` at amp 2.25, as \|drawn(x = 0.01) − f(0⁺)\|: k = 2 → 5.6×10⁻³, k = 3 → **8.9×10⁻²** (the worst, 300× the quoted figure, and the whole left column reads 0.0893 where the density is 0), k = 4 → 5.6×10⁻³, k = 5 → 3.0×10⁻⁴, k = 6 → 1.4×10⁻⁵, k = 7 → 6.0×10⁻⁷, k = 8 → 2.3×10⁻⁸. At k = 1 the difference is not finite at all — the density diverges at the origin — and the ±0.8 output clamp takes over, so that column reads 1.8 at amp 2.25. The range over the slider is therefore 2.3×10⁻⁸ … 8.9×10⁻², plus an unbounded k = 1. |
| `studentT` | Student's t | 🔵 B | Missing normalization constant Γ((ν+1)/2)/(√(νπ)Γ(ν/2)). Shape exact, scale off by const. |
| `entropyLandscape` | Shannon Entropy | 🟢 A | Standard binary entropy, measured **4.4×10⁻¹⁶** against `scipy.stats.entropy` over the reachable box (control: H(½) = 1, H(¼) = 2 − ¾log₂3 = 0.8112781244591328). Round 8: p used to be clamped to [0.001, 0.999], which is not a pole guard — H is defined at both endpoints, p log p → 0 — so the two end columns of the plate drew H(0.001) = 0.0114 bits instead of 0, worth 1.05×10⁻² at amp 2.25 under this row. What the clamp was really avoiding is the NaN from 0·log 0, and a branch on the endpoints avoids that without moving the abscissa. |
| `mixtureGaussians` | Gaussian Mixture | 🔵 B | Sum of normal PDFs with wᵢ = 1, not weights summing to 1, so the height is n times a mixture density: measured multiplier 1.600000000 at n = 4 and 2.000000000 at n = 5 (= 0.4·n, spread ≤2.9e-15). `comp` crossing 0.7 adds a component and lifts the whole surface 25 % — the same missing-normalisation criterion the file already applies to `studentT` and `vonMises`. |
| `pareto` | Pareto | 🟢 A | α·xm^α/x^(α+1). |
| `kernelDensity` | KDE | 🟢 A | Sum of fixed-kernel evaluations. |
| `vonMises` | von Mises | 🟢 A | Round 6 restored 1/(2πI₀(κ)) — and the point is that it is not a constant: κ = 1 + comp·4 with comp riding the mid band, so the omitted factor ran 30.7 at comp 0.5 and 120.0 at comp 0.9 and the surface changed height by ×3.9 with the music while calling itself a density. It also stood 3.5 world units tall at the factory sliders against a ~3-unit frame. Now the density itself, whose peak e^κ/(2πI₀(κ)) grows only as √(κ/2π). I₀ by ascending series, twenty terms. |
| `metropolisWalk` | MCMC Metropolis | 🟡 C | 40-step deterministic Metropolis + decorative output `exp(-3·(v-x)²)` — not direct sample density. |

### 4. Linear Algebra (16) — 10 A · 4 B · 2 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `eigenField` | Eigenvector Field | 🟢 A | Round 6: A(t) = R(0.3t)·diag(1+comp, −1)·R(0.3t)ᵀ and the height is (v × Av)/\|v\|, the signed distance from Av to the line through v. It is exactly zero on the eigenvectors and nowhere else, so the surface is the statement `Av = λv` rather than a decoration of it. Previously a fixed linear functional of Av, and the old matrix was identically zero at t = 5π + 10πk — the plate went flat once every 65 s. |
| `determinant` | Determinant | 🟢 A | ad-bc exact. **Round 9 — A was never named and the clock term is a rigid offset, which clause 2 charges.** The height is (x·freq·z·freq − sin 0.4t·cos 0.3t)·amp·0.3 and the second term carries neither x nor z: on grid 81 at the factory sliders y(t = 3.7) − y(t = 0) is −0.092994 at every one of the 6561 vertices, standard deviation across the plate 2.2·10⁻¹⁵ (control: the same statistic on `eigenField`, whose clock is not an offset, spreads by 3.196707). Sup \|sin 0.4t·cos 0.3t\| = 0.951428 over the 20π common period, so the offset reaches ±0.199800 at the factory sliders against a ±2.572500 saddle term — 7.77 % of the picture, 4.60 % at the audio envelope, 0.375 % at the reachable slider tops — and it moves the zero contour and not merely the level: up to 14.80 % of the grid-81 vertices carry the opposite sign to the offset-free saddle, worst at t = 11.31 (control: 0.00 % with the offset removed). The caption therefore names A = [[freq·x, sin 0.4t],[cos 0.3t, freq·z]] and writes its determinant out. Drawn ÷ det A is then exactly amp·0.3 — 0.210000 / 0.300000 / 0.315000 / 0.675000 / 0.060000 at the five settings, relative spread ≤ 1.0·10⁻¹³ over grids 81 and 161 at t ∈ {0, 3.7, 7, 29} — where the old `det(A(x,z))`, read literally as a function of (x, z) alone, gives a ratio running −11.94 … +12.36 and changing sign. Peak \|y\| scanned over the clock is 2.772 at the factory sliders, 3.960 at amp 1, 6.821 at the audio envelope and 171.826 at the reachable slider tops; the t = 0 readings 2.5725 / 6.5213 / 171.1838 are not bounds. |
| `svdSpectrum` | SVD Singular Value | 🟢 A | Closed-form 2×2 singular value formula. |
| `trace` | Matrix Trace | 🟡 C | `cos(r)ⁿ` is exactly tr(Aⁿ) for a rank-one A of trace cos r — checked by multiplying an explicitly built matrix, not by quoting the identity: agreement 4.4·10⁻¹⁶ over the plate. C stands because the entry names no particular A, and the reading a reader reaches for first — A a rotation by r — gives 2cos(nr), which differs from the drawn surface by up to 1.13 against its own peak of 0.5. |
| `tensorField` | 2D Tensor Field | 🔵 B | u²+uw+w² with u = 0.7·freq·x, w = 0.7·freq·z — the sum of T's three distinct components, not a tensor norm and not frame-invariant, but a scalar functional of T. Round 9 put the 0.7·freq into the caption, because with a bare x the ratio carries freq² and stops being one constant over the slider box. |
| `hessian` | Hessian Determinant | 🔵 B | Analytic Hessian of sin(x)+sin(z). Exact. |
| `rotationMatrix` | Rotation Matrix Flow | 🟢 A | Rotation matrix exact. |
| `gram` | Gram–Schmidt | 🟢 A | Exact 2D Gram–Schmidt projection, measured **3.6×10⁻¹⁵** against LAPACK's Householder QR (`numpy.linalg.qr` of [v₁, v]; \|e₂\| is \|R₂₂\|) over the reachable box — the 2.7×10⁻¹⁵ this row used to quote is exceeded by that reference at amp 2.11 / freq 2.73. Round 8 removed the +10⁻⁹ in the normalisation: v₁ = (cos 0.3t, sin 0.3t) has \|v₁\| ≡ 1, so the guard could never fire — all it did was make e₁ a non-unit vector by one part in 10⁹, worth 1.79×10⁻⁸ of error. **Round 8 — the figure above is clock-blind and needs qualifying.** The QR reference is handed the same double `cos(0.3t)` the kernel used, so the rounding of `0.3*t` cancels out of the comparison and the number is the same at every clock value. Against a reference that keeps t exact at 40 digits — which is what the caption's v₁ actually names — the error is 1.1×10⁻¹⁵ at t = 0, 2.1×10⁻¹³ after an hour of uptime, 4.6×10⁻¹² after a day (t = 41 472 at 0.008 per frame, 60 fps) and 2.7×10⁻¹¹ after a week: the direction of v₁ drifts by an ulp of 0.3t and takes \|e₂\| with it. Same mechanism as `pythagorean`, and equally invisible under the Float32 buffer. |
| `quadraticForm` | xᵀAx | 🟢 A | Direct quadratic form evaluation. |
| `nullspace` | Nullspace Projection | 🟢 A | Exact orthogonal complement projection. |
| `spectralRadius` | Spectral Radius | 🟢 A | Round 6: ρ = (\|tr\| + √disc)/2 for a real pair and √det for a complex one — both branches closed-form and exact. The previous kernel returned √\|disc\|·0.3, which is 0.3·\|λ₁ − λ₂\|, the spread rather than the radius; the trace here is not zero (tr = x·freq·comp), so the ratio to ρ ran across [0.220, 0.600] and no constant could absorb it. The 0.8 clamp went with it — it was pinning 57.8 % of the mesh flat at the default slider. |
| `matrixExp` | Matrix Exponential | 🟡 C | `cosh(r·comp)·cos(r) - 1` is a stylized substitute, not general e^A. |
| `kronecker` | Kronecker Product | 🔵 B | Grid+sub-grid product structure correct conceptually. |
| `vectorField` | Curl ∇×F | 🟢 A | Closed form. F = (−sin(z·f), sin(x·f)) has curl f·(cos(x·f) + cos(z·f)), divided by f so a derivative-valued formula does not scale with the frequency slider. The field has to be named: the stencil that used to stand here was correct, but it was applied to a gradient field, whose curl is identically zero — the formula rendered a flat plate until the field was replaced. Round 6 removed the stencil itself: central differences with h = 0.01 give 4–5 correct digits, measured 3.3e-5 at the default slider and 6.9e-4 at the top of it, which is not the ~10⁻¹⁰ this row claimed. |
| `jacobian` | Jacobian Det | 🟢 A | Closed form for u = cos(f(x+z)), v = sin(1.3fx) + sin(1.9fz)/1.9 — which is the map the old stencil was in fact differentiating: it varied only the second occurrence of z in `sin(z·f·0.9 + z·f)`, so the derivative it formed was f·cos(1.9fz) rather than 1.9f·cos(1.9fz). The drawn surface is unchanged; it is now exact rather than 9.1e-5 out at the default slider and 3.9e-2 at the top. (Round 1 fixed an operator-precedence bug here; round 6 removed the stencil.) |
| `manifoldCurvature` | Gaussian Curvature | 🔵 B | Round 6: now the full K = (F_xx·F_zz − F_xz²)/(1 + F_x² + F_z²)², h = 0.05 central differences, worst deviation from the closed-form K 8.4e-5 over the plate. The denominator — named in the formula string — was absent, so the drawn quantity was the Hessian determinant, off by up to 1.39× in shape, not scale. The display constant rose 0.15 → 6.0 with it (the old one left the peak at 0.021 units against a ~3-unit frame) and the ±0.6 clamp became `soften(±1.2, ±2.6)`: K grows as freq⁴ where both slopes vanish, so the over-drive range is folded instead of cut. Round 8: the 8.4e-5 above holds at freq = 1 and only there — the h = 0.05 stencil is fixed while the surface it differentiates steepens with the slider, so the error on K is 4.0e-4 at freq 2 and 2.8e-3 at freq 3.5. Tier B is met across the range; the single number was not the whole story. **Round 8 — the fold, which decides nothing here and hides a great deal.** The fold covers a measured **0.0 %** of the plate at the factory sliders and 83.1 % at the slider maxima, at grid 90; on the meshes the app draws, 0.00 % and 83.34 % at grid 81, 0.00 % and 82.19 % at 161, and the kernel takes no clock. The factory zero is real and not a blind probe — the factory peak is 0.848 against the knee 1.2, and halving the knee lifts the same count to 0.55 % — but it is one slider step from meaning nothing: at the audio envelope (amp 1.05 / freq 1.3 / comp 0.9) 38.65 % of the plate is folded at grid 81 and 38.21 % at 161, and 34.57 % of it is drawn further than 10⁻³ from the un-compressed value. At the maxima that reaches 81.33 %, and the fold has become the thing it was introduced to remove: 42.68 % of the plate at grid 81 and 43.19 % at 161 sits bit-exactly on `fround(2.6)`, 54.3 % within 10⁻³ of the ceiling. The letter stays at B because the rule that sets it is read at the factory sliders, where the fold is asleep and the h = 0.05 stencil is the whole of the error; the maxima figures are disclosure, not a demotion, and they are stated here because a row quoting only "0 % folded" would be true and useless. |

### 5. Trigonometry (16) — 15 A · 0 B · 1 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `sinCos` | sin·cos product | 🟢 A | Trivially exact. |
| `pythagorean` | Double-Angle Wave −cos 2(r+t) | 🟢 A | sin²(r+t) − cos²(r+t) = −cos(2(r+t)) with r = 2·freq·√(x²+z²), measured **1.2×10⁻¹⁴ at boot** against mpmath `cos` over the reachable box. Round 8 removed the +10⁻⁹ on r: −cos(2(r+t)) is analytic at the origin and nothing here divides, so the guard could not be protecting anything, and it cost 2.03×10⁻⁹ — an order above the top of tier A. **Round 8, two labelling repairs.** (i) The caption read `sin²+cos²=1 → height = sin²(rx)−½`, which is a static function of a product `r·x` the kernel never forms; the kernel forms r = 2·freq·√(x²+z²), adds the clock, and returns (sin² − cos²)·amp·0.45, i.e. −cos(2(r+t))·amp·0.45. At (1.3, −0.4) with the factory sliders the drawn height is −0.209641, −cos(2r)·0.7·0.45 is −0.209641 and sin²(r·x) − ½ is −0.352072. Caption and row now both use r + t, one argument throughout. (ii) **The accuracy figure is a boot-time figure and needs the clock attached.** r + t is formed in double, so the rounding of the sum is set by the magnitude of t, and t is never rewound: at 0.008 per rendered frame a day of uptime at 60 fps is t = 41 472. Measured worst error over the box: 1.2×10⁻¹⁴ at t = 0, 2.3×10⁻¹³ after an hour, 7.2×10⁻¹² after a day, 5.6×10⁻¹¹ after a week — nearly three orders of magnitude between boot and a day, all of it argument reduction and none of it visible against the Float32 buffer. **Round 11 — renamed.** sin² + cos² = 1 is a constant; this kernel forms sin² − cos² = −cos(2(r+t)), which is the double-angle identity. The arithmetic is untouched and stays exact — PARI/GP at 40 digits gives −0.20964089841172726 at (1.3, −0.4) with the factory sliders, digit for digit with the kernel — so only the claim changed. A viewer reading "Pythagorean Identity" over a full-height radial wave has caught the app in a claim the identity itself refutes. |
| `sumAngle` | Sum of angles identity | 🟢 A | Validates identity sin(α+β)=sinα·cosβ+cosα·sinβ. |
| `doublAngle` | Double angle | 🟢 A | Exact. |
| `halfAngle` | Half-angle | 🟢 A | sin(x/2), and the identity the caption states is the same number: max \| \|sin(u/2)\| − √((1−cos u)/2) \| = 7.955e-15 over 2000 samples × four wave intensities. The old B asked that the caption name the side that is evaluated; applied consistently that demotes `doublAngle`, `productSum` and `chebyshevTrig` too, and all four state identities whose two sides agree with what is drawn. The one entry where they do not — `inverseTrig`, whose identity is the constant π/2 while the surface runs ±0.411 — is repaired in the caption instead. |
| `productSum` | Product-to-sum | 🟢 A | 2sinAsinB = cos(A-B)-cos(A+B), exact. |
| `tangentWave` | Tanh | 🟢 A | Built-in Math.tanh. |
| `lissajous` | Lissajous | 🟢 A | Exact. |
| `hyperbolicGeom` | Cosh²-Sinh² | 🟢 A | cosh(0.7·r) − 1, exact, r = freq·√(x²+z²) — round 9: the row said cosh(r); the kernel's argument carries a further 0.7, and drawn ÷ (cosh(0.7r) − 1) is a flat amp·0.25 at every setting (0.175000 at the factory sliders, 0.562500 at the slider tops). The 0.7 is dial-away — at the reachable freq = 1/0.7 = 1.428571 the drawn field equals (cosh r − 1)·0.25 to 2.8·10⁻¹⁴ — but the −1 is not: it misses cosh(r)·0.25 by exactly 0.250000 there, and the plate's minimum is exactly 0 at every setting where cosh's is 1. |
| `chebyshevTrig` | Chebyshev identity cos(nθ) | 🟢 A | The right-hand side of T_n(cos θ) = cos(nθ), instantiated exactly: the surface is cos(n·π·0.9·x)·0.45 to 6.3e-15. NOT the same formula as `specialFunctions.chebyshev` — that one applies acos to the clamped coordinate and draws the polynomial with its plateau rim; this one has no acos anywhere and is a plain sinusoid, the same kernel as `complexNumbers.moivre` up to constants. |
| `standingWave` | Standing wave | 🟢 A | sin(kx)·cos(ωt). |
| `travelingWave` | Traveling wave | 🟢 A | sin(kx-ωt). |
| `modeInterference` | Mode interference | 🟢 A | Σ_{n≤6} sin(n·k·x)·cos(nωt)/n, times the plate envelope e^{−z²/4}, exact to 0.000e+0 over the whole grid-81 plate at the factory sliders, the audio envelope and the slider tops — round 9: the row wrote an unbounded sum and six terms are computed. Against Σ_{n≤200} the median relative gap away from the jump is 12.0 % at the factory sliders, 13.1 % at the audio envelope and 12.9 % at the slider tops, and the worst is 0.364295 against the field's own sup of 0.335349, so the truncation is the entry's identity rather than an error — A is for the six-term sum the caption now states. |
| `circularFunctions` | sec·csc | 🟡 C | Outside the guard band the surface is clamp(sec·csc·0.04·amp, ±0.7) exactly — residual 0.000e+0 against an independent sec·csc, so it is genuine sec/csc there, not decorative. C is for the guard itself: `\|cos\|>0.1` returns 0 at the poles, where the clamp had already capped the value, so instead of saturated ridges the mesh gets a hole — 12.3 % of vertices sit at exactly 0 and the largest step between neighbouring vertices is 0.7000, the whole half-height, in one mesh step. |
| `atan2Field` | atan2 phase | 🟢 A | sin(3φ + 2r − t) exact to 0.000e+0, φ = atan2(z,x), r = freq·√(x²+z²) — round 9: the height is that three-armed rotating phase field and not φ. Walking a circle of radius 2 in 72 steps the drawn field changes sign six times where φ changes sign once, and a free-scale fit against φ leaves 99.90 % relative residual at the factory sliders, so no display constant reconciles the two. The letter stays A on the expression, which is what the caption now names. |
| `inverseTrig` | arcsin | 🟢 A | Math.asin clamped to exactly ±1. Round 6: the argument was held off ±1 by 1e-6 and then again by 1e-9, and asin(±1) = ±π/2 needs neither — the whole saturated rim sat 4.243e-4 world units below where it belongs, and the rim is 31.9 % of the row at freq 1.5, not one vertex. |

### 6. Complex Numbers (16) — 10 A · 2 B · 4 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `euler` | Re(e^iθ) | 🟢 A | cos(θ). |
| `eulerIm` | Im(e^iz) | 🟡 C | e^(−z)·sin(x), exact. Round 6 folded the display: e^{−z·freq} has no envelope, so the z = −3.5 edge stood 10.4 world units high at the FACTORY sliders — the largest such offender in the catalogue — and grew without limit across the slider range. `soften(0.9, 1.8)` leaves the oscillation exactly as computed wherever it is legible. Round 8: the fold is not a rim effect. The fold covers a measured **24.4 %** of the plate at the factory sliders and 48.1 % at the slider maxima, both counted at grid 90; on the meshes the app lays down it is 24.30 % and 47.77 % at grid 81 and 24.30 % and 47.90 % at 161, read at t = 0 — and t = 0 is the minimum of the cycle. Over one turn of the clock the factory figure runs 24.30…27.10 % at grid 81 (mean 26.1) and 24.30…26.94 % at 161, so the boot instant understates the average by 1.8 pp; at the audio envelope (amp 1.05 / freq 1.3 / comp 0.9) it is 35.4…36.2 %. **Round 8 — Tier A withdrawn.** Measured against the same kernel with `soften()` replaced by the identity, 22.59 % of the drawn plate at grid 81 and 22.44 % at 161 is further than 10⁻³ from the un-compressed value at the factory sliders — 25.35 % at the worst instant of the clock (t = 12), 34.02 % at the audio envelope, 47.19 % at the maxima — and the largest of those deviations is 8.63 world units against a frame about 3 units high. Tier A asks for agreement to about fourteen significant digits, and a quarter of the plate at boot is a tanh image of Im(e^iz) rather than Im(e^iz): that is the "output is decorated by an envelope … that breaks direct correspondence" bullet of Tier C. What argues against moving it, and does not win: the fold is strictly monotone, so the sign, the ordering and the zero set of the oscillation survive exactly, and the entry cannot be drawn unfolded at all — the z = −3.5 edge stands 10.4 world units high at the factory sliders, the largest such offender in the catalogue. That is a statement that the entry can do no better, not that the plate is the named function. Outside the fold the agreement is 1.9e-16, and that is what the other three quarters carry at boot. |
| `moivre` | De Moivre | 🟢 A | cos(nθ). |
| `complexPower` | \|z^z\| | 🟡 C | Direct via log/exp identity. Round 6: clamp → `soften(0.5, 0.85)`; exp is unbounded, so the cut left 10.2 % of the mesh flat at the default slider. Round 8: the `+1e-9` propping up ln r **was** the whole residual — against mpmath's `abs(mpc(x,z)**mpc(x,z))` at 50 dps the drawn value was out by 4.77×10⁻¹⁰, and against the same expression *with* the epsilon it agreed to 1.29×10⁻¹⁶. Removed; the residual outside the fold is now 5.6×10⁻¹⁶ at the factory sliders and 1.9×10⁻¹⁵ at the slider maxima. r = 0 is a removable point, not a pole (both terms of x·ln r − y·arg z vanish with r, so \|z^z\| → 1 from every direction — mpmath spread 9.2×10⁻² at \|z\| = 10⁻² down to 1.4×10⁻²⁸ at 10⁻³⁰), so the origin carries the limit 0.1·amp rather than the display bound; saturating it would restore the grid-parity needle round 6 took out of `complexLog`. The fold covers a measured **12.3 %** of the plate at the factory sliders and 36.8 % at the slider maxima, both counted at grid 90. The 12.4 % this row used to quote is no mesh's number at all: the three grids give 12.47 % (81), 12.35 % (90) and 12.26 % (161), and grid 41 gives 12.79 %, so the figure has to carry its lattice even though the spread is only 0.20 pp. The kernel takes no clock — the plate is bit-identical across a full sweep of it — and at the audio envelope the coverage is 21.34 % at grid 81 and 21.27 % at 161. **Round 8 — Tier A withdrawn.** Measured against the same kernel with `soften()` replaced by the identity, 11.54 % of the drawn plate at grid 81 and 11.37 % at 161 is further than 10⁻³ from the un-compressed value at the factory sliders (20.74 % at the audio envelope, 36.67 % at the maxima), and the largest of those deviations is 4.76 world units against a ~3-unit frame. The argument this row used to make — "a minority everywhere the sliders reach" — is literally true, 36.8 % being the maximum anywhere; it does not survive being written down, because "minority" is a 50 % threshold nobody stated, and applied consistently it would keep `mobiusTransform` at A on 22.6 % as well. An eighth of the plate at boot is a tanh image of \|z^z\| and not \|z^z\|, which is Tier C's envelope bullet. \|z^z\| outside the fold is unchanged and exact to 5.6×10⁻¹⁶, which is the figure above and the one a viewer can rely on for the other seven eighths. |
| `rootsOfUnity` | n-th roots of unity | 🟢 A | Sum of Gaussians at exact roots. |
| `complexLog` | Log(z) | 🟢 A | ln\|z\| exactly, outside a disc of radius 0.08; inside it, the quadratic meeting the logarithm in value and slope at the rim. Worst deviation outside the disc 1.1e-16 against mpmath. Round 6 replaced a +1e-9 regulariser that fixed the pole's depth rather than removing it — one vertex sat 4.14 units below a surface whose own peak is 0.58, on every odd grid size, and the app's grid floats 60–200 with the GPU. Round 8: its replacement, ln√(r²+0.08²), is not ln\|z\| anywhere — the bias ½ln(1+ε²/r²) is small but never zero, so this row's own claim was false on 100 % of the vertices and by 5.8e-2 at r = 0.07. |
| `riemannSphere` | Stereographic | 🟢 A | (r²-1)/(r²+1) — exact projection. |
| `mobiusTransform` | Möbius (az+b)/(cz+d) | 🟡 C | Direct complex division. Round 6: clamp → `soften(0.5, 0.85)`. The map has a pole at z = −d/c, which enters the plate for large |c|, so the cut was doing real work (10.8 % of the mesh flat at the default slider) — a fold does the same work without erasing the neighbourhood of the pole. Round 8, two findings. (i) The `+1e-9` on \|cz+d\|² was the entry's whole residual: against mpmath complex division at 50 dps the drawn value was out by 1.45×10⁻⁹ outside the fold and by 4.52×10⁻¹⁷ against the same division *with* the epsilon. Removed; the division is now exact to 5.2×10⁻¹⁶ (factory) and 1.4×10⁻¹⁶ (maxima). At the pole the epsilon had been choosing a finite height for an infinite value — the quotient came out **0**, punching a hole through a ridge whose two sides are ±0.85. It now returns the fold ceiling. **Round 8 correction — that branch is defensive, not a visible feature, and this row said otherwise.** `den2 === 0` is an exact IEEE test: it needs z exactly 0, which only an odd lattice has, and c·x·freq exactly −1, which at t ≠ 0 has to come out of `cos(0.3t)` by coincidence. The lattice is `round(√vertexCount)` of whatever geometry is on screen, which over the app's **twenty** shapes at both detail levels gives **3, 5, 8, 9, 10, 13, 15, 18, 21, 24, 43, 45, 52, 63, 65, 74, 81, 83, 100, 114, 161, 198 — thirteen of them odd** (this list was measured again in round 10: it had been quoted as eighteen shapes and twenty-three grids, missing 65, and round 10's mesh repair then retired 20, 42 and 82 and added 21). Sweeping 868 freqs × 107 comps at t = 0 across the eleven odd grids reachable at the time, the branch is taken on **grid 15 and grid 43 only** (56 and 139 placements), and never on the plane's own 81 or 161 — grid 21 entered the catalogue afterwards and is not in that sweep; over 450 000 frames of clock at the factory comp and freq — an hour of watching — the only hit is frame 0. The probe is not blind to the others: forcing freq = −1/(comp·x) onto a lattice vertex fires on every odd grid, 29 placements on 81 and 62 on 161, so the pole *can* be landed on anywhere if freq is dialled to the exact reciprocal — it is a measure-zero set of slider values, not a thing a viewer meets. What the repair buys is that when it is landed on, the height is the ceiling of the ridge it sits in rather than 0. (ii) **Tier A withdrawn.** The fold covers a measured **22.6 %** of the plate at the factory sliders, 37.2 % at amp 1, **98.3 %** with both sliders at their tops (amp 1.5 / freq 3.5, comp left at the 0.5 the audio path idles at) and **99.4 %** at the slider maxima — all counted at grid 90 and all at t = 0, which for this entry is the one thing they cannot be left as. **Round 8 correction — the 62.1 % this row used to quote for amp 1.5 / freq 3.5 is one instant of a plate that breathes.** It is that corner read at comp 1 with the clock at exactly 0: the lowest of the 924 (comp, clock) settings there, whose median is 97.8 % and whose fifth percentile is 91.9 %, and which the clock alone lifts to 66.9 % by t = 0.25 and 93.2 % by t = 1 — about two seconds of wall clock at 0.008 per frame and 60 fps. Over the comp window the audio path can actually reach, comp 0.5…0.9, the coverage at that corner never falls below 90.2 %. The breathing is this entry's main fact rather than a footnote: at grid 81 the factory coverage runs 15.79…42.72 % over one turn of the clock (mean 25.4, and 22.73 at t = 0) and the audio envelope runs 1.60…89.74 % (mean 44.0, and 17.45 at t = 0), so the boot instant is under half the cycle mean; at grid 161 the same two ranges are 15.54…42.36 % and 1.56…89.55 %. Measured against the same kernel with `soften()` replaced by the identity, 17.89 % of the drawn plate at grid 81 and 17.74 % at 161 is further than 10⁻³ from the un-compressed value at t = 0, rising to 34.35 % at the worst instant of the clock, and the deviation at t = 0 is 78.0 world units on both meshes the app draws, and 78.0 is that instant rather than a bound — over one turn of the clock, the 7 854 frames the app renders at 0.008 apiece across 20π, the largest deviation runs **0.089 … 9.70×10⁵** world units on both, median 40.4 at grid 81 and 76.6 at 161. What the number measures is how close the sweeping pole passes to a lattice vertex on the frame in question, which is why its top is four decades above its floor and why a coarse scan of the clock reads it low — the 9.19 this row used to quote is the grid-90 value, and 90 is not a lattice the app lays down. Where the fold bites, the drawn height is a monotone tanh rescaling of Re f and not Re f, so at the top of the reachable box a machine-precision claim would describe six vertices in a thousand. This is the "output is decorated by an envelope … that breaks direct correspondence" bullet of Tier C, and the factory measurement alone carries the letter — it does not need the 99.4 %. The division itself is still exact to 10⁻¹⁶ outside the fold — that number is above, and it is what a viewer can rely on for the 77 % of the plate that carries it at boot. |
| `cauchyRiemann` | Re(z²) | 🟢 A | x²-z² is harmonic by construction. |
| `complexSin` | sin(x)cosh(z/2) | 🟢 A | Re(sin(x+iz/2)) = sin(x)cosh(z/2), exact — the kernel is `Math.cosh(z*freq*0.5)`, so the imaginary axis is compressed by two, and freq cannot absorb it because freq multiplies both axes alike. Round 9: worst |drawn − caption| is 0.000e+0 at all five settings and three clock samples once the ½ is written in, against 1.021 world units at the FACTORY sliders with the caption as it stood, on a plate whose sup is 0.622 — and 0.800 at the amplitude floor, where nothing folds at all, so this is not the fold. The factor is this entry's alone: `cauchyRiemann`, `complexLog`, `complexPower`, `mobiusTransform`, `blaschke` and `riemannSphere` all map the plate into ℂ with z·freq unscaled. **Round 8 — the ±0.7 clamp is now `soften(·, 0.7, 1.4)`**, the round-6 treatment with the knee at the old bound, so the picture at the factory sliders is bit-identical: 0 of 8100 vertices move at grid 90, because the factory peak is 0.6225 and the knee is 0.7. What the clamp was doing is on the rest of the slider — the share of the plate sitting bit-exactly on 0.7 was 18.17 % at the audio envelope and 84.25 % with every slider at its top, and both are 0.00 % now. The fold covers a measured **0.0 %** of the plate at the factory sliders and 84.2 % at the slider maxima, counted at grid 90; on the meshes the app draws it is 0.00 % and 83.95 % at grid 81 and 0.00 % and 83.95 % at 161, with the clock worth about 1 pp at the top (83.86…85.17 % at grid 81) and the audio envelope sitting at 18.20…19.17 %. The letter stands because the rule that sets it is read at the factory sliders: 0.00 % of the plate there is drawn further than 10⁻³ from the same kernel with `soften()` replaced by the identity, on every grid and at every clock sample, and that zero is the fold being asleep rather than the comparator being blind — halving the knee lifts it to 3.72 %. **What the letter does not say, and this row now does.** At the audio envelope 13.47 % of what is drawn is a tanh rescaling of Re sin(x+iz) rather than Re sin(x+iz), and at the maxima 81.09 %; and the fold did not make cosh gentle, it moved the tabletop from the old bound to the new ceiling — 54.56 % of the maxima plate at grid 81 and 54.06 % at 161 sits bit-exactly on `fround(1.4)`, 62.98 % and 62.78 % within 10⁻³ of it (control: the same counter against the un-folded kernel reads 0.00 %). |
| `juliaPotential` | Julia escape time | 🔵 B | Round 6: the 2⁻ⁿ that defines G(z) = lim log\|fⁿ(z)\|/2ⁿ was missing — the code returned log₂(log\|z_n\|), the logarithm of the potential plus the escape index. Against G run to convergence (200 iterations, escape radius 10⁵⁰) the old expression was off by up to 2.158 on a range whose own maximum is 1.612. Now 1.8e-3, which is the truncation left by stopping at twelve iterations. |
| `windingNumber` | Winding number | 🟢 A | Round 6: argument increments accumulated along the contour rather than 1/(z−z₀) integrated. The old form shared N = 48 nodes across n_loops traversals — 12–16 per loop — which is right deep inside and far outside but carries a ring of spurious poles at \|z₀\| = 1, where the value was set by whichever vertex landed nearest: peak 0.62 / 1.39 / 7.87 / 6.22 across grids 25 / 90 / 161 / 400. Now exactly n_loops inside and exactly 0 outside, identical at every mesh density. |
| `blaschke` | Blaschke product | 🟡 C | Direct iterative complex division. Round 6: the ±0.6 clamp swallowed the surface — 89.7 % of the mesh pinned flat at the bound at the default wave intensity and 99.6 % at the top of the slider, so the viewer got a small disc on a table. `soften(0.45, 0.85)`. Round 8, two findings, the first measurable without implementing anything: for \|aₖ\| < 1 a finite Blaschke product has \|B\| = 1 identically on \|z\| = 1, so the drawn height on the unit circle must be exactly amp·0.45 − 0.2. It was off by 3.65×10⁻⁹ at amp 1 — precisely the size of the `+1e-9` in the denominator — under a row claiming 10⁻¹⁴. (i) Epsilon removed: the circle is now exact to 2.2×10⁻¹⁶, the interior to 4.3×10⁻¹⁶ against mpmath at 50 dps. 1 − āₖz vanishes at \|z\| = 1/0.6 = 1.667, inside the plate at every reachable freq and an exact double zero; where the epsilon was in force that point drew **−0.2** (the product collapsed to zero), a pit at the one point where the surface is highest. It now returns the fold ceiling, which is also what the whole neighbourhood already reads. **Round 8 correction — like `mobiusTransform` above, that pit is a branch this row should not describe as drawn.** `d2 === 0` needs z exactly 0 and āₖz exactly 1 in IEEE double; over the same 868 freqs × 107 comps at t = 0, across all eleven odd lattices the app produces, it is taken **zero times**. The probe is not blind: putting freq = 5/(3x) on a lattice vertex makes d2 exactly zero on grids 13, 15, 43, 45, 63, 81, 83 and 161 (on 3, 5 and 9 it only reaches 1.2×10⁻³², so those cannot host it at all). So the pit is reachable in principle and unreachable in the box as swept; the branch is a guard, and the epsilon's real cost is the 3.65×10⁻⁹ on the unit circle above, which is drawn everywhere. (ii) **Tier A withdrawn.** The fold covers a measured **90.9 %** of the plate at the FACTORY sliders — 92.3 % at amp 1, 99.5 % at amp 1.5 / freq 3.5, 99.8 % at the slider maxima — all counted at grid 90, with deviations of **236 world units** at t = 0 at the factory sliders on both meshes the app lays down — an instant, not an upper bound: over one turn of the clock (10π, 3 927 rendered frames) the largest deviation runs **15.7 … 525.0** at grid 81 and **31.7 … 1387.4** at 161, medians 29.6 and 57.4. Nine tenths of what the viewer is shown at boot is a tanh rescaling of \|B\|, monotone and so qualitatively faithful, but not the product; that is Tier C by this document's own envelope bullet. Exactness outside the fold, and on the unit circle, is stated above. **Round 8 — two figures in the paragraph above needed their lattice, and one correction runs the other way.** (a) The 21.3 world units this row used to quote for the factory deviation is the grid-90 value; the same measurement is 236 at grid 81 and 236 at 161, and at the maxima the same t = 0 instant is 49.5 at 81, 97.0 at 161 and 135.5 at 90, and over one turn of the clock it runs 45.9…199.2 at 81, 60.3…199.2 at 161 and 44.0…146.8 at 90 — both app grids reaching their top at the same frame, t = 18.064 — 90 is not a lattice the app draws. The coverage itself is grid-stable and barely breathes: 90.66…91.02 % over a full turn of the clock at grid 81 and 90.66…90.80 % at 161 at the factory sliders, 95.74 % at the audio envelope, 99.68 % (81) and 99.73 % (161) flat at the maxima, so 90.7 % is the honest app number for the 90.9 % above. (b) The stronger complaint is that the fold has become the tabletop it was introduced to prevent: at the factory sliders 77.55 % of the plate at grid 81 already sits within 10⁻³ of the ceiling 0.85 (1.71 % bit-exactly on `fround(0.85)`), and at the maxima 99.62 % within 10⁻³ and 99.41 % bit-exactly — at the top of the sliders the plate is one flat disc. Control: the same counter against the same kernel with `soften()` replaced by the identity reads 0.00 % at both settings, so it is counting the ceiling and not the top of whatever it is handed. |
| `complexHeat` | Heat kernel ℂ | 🟢 A | exp(-r²/4t)/(4πt) exact. |
| `argandField` | sin(arg(zⁿ)) | 🟢 A | sin(n·θ), exact. Round 8: the in-app caption said `arg(z^n)` while this column already said sin(n·θ) — the sine is two-to-one in the phase and spans ±0.45, where the argument itself spans ±π, so the caption now names the sine. |
| `riemannZetaStrip` | ζ on the critical line | 🔵 B | Round 9 replaced the truncated Dirichlet series, which **diverges** on Re(s) = ½ and does not improve with terms, by Euler–Maclaurin at the same term budget — N−1 direct terms, the integral tail and four Bernoulli corrections, i.e. the repair `zeta` took in round 5 carried over to complex s. Against mpmath at 40 dps on the z = 0 row plus five off-row vertices, grid 81: worst 1.9×10⁻⁸ world units at the factory sliders (rms 4.1×10⁻⁹ against an oracle rms of 0.1238), 4.1×10⁻⁹ at the audio envelope, and 9.7×10⁻⁴ at the worst corner of the reachable box — freq 4.55 with comp 0.5, where |T| reaches 83.25 on 20 terms. The partial sum it replaces measured 0.95 and 2.46 at the same points, an rms 113.5 % of the oracle's, and put 15 of 86 sampled vertices on the wrong side of zero; the patched kernel puts none. What is drawn is Re(e^{i·freq·z}·ζ(½+iT)) with T = 30(freq·x+3.5)/7 — the critical **line** along x, turned by z; σ is hard-coded to ½ and nothing off it is sampled, which is why the entry is renamed from "Critical Strip". The display constant stays at 0.15: the factory plate falls from ±0.797 to ±0.299 because the left edge was the divergence itself, and ±0.299 is where this collection's neighbours already sit. |

### 7. Fourier Series (16) — 15 A · 1 B · 0 C · 0 D

This is the cleanest collection — every wave is a real truncated Fourier series, exhibiting genuine convergence behaviour including the Gibbs phenomenon.

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `sineWave` | Fundamental sine | 🟢 A | sin(2πx/L + t)·e^{−0.3z²}, L = 1/(0.3·freq) — exact as a sine, and the letter is unaffected. Round 9: this row used to print the caption's static f(x), which is neither of the two transformations the kernel applies. The clock is a pure translation (max\|y(x,z,3) − y(x+1.5915,z,0)\| = 5.97e−16, ridge speed 0.5305 world units per formula-time unit — 0.2546/s of uptime at 60 fps — at freq 1) and the z-window keeps 2.54 % of the amplitude at the plate rim (e^{−0.3·3.5²}); both are now in the caption. |
| `squareWave` | Square wave Fourier | 🟢 A | Truncated 4/π·Σ sin((2k-1)(x+t))/(2k-1). Round 6 moved the clock inside the harmonic index: added as a common phase it made the sum cos(t)·(series) + sin(t)·(conjugate series), and the best achievable correlation with a square wave fell 0.986 → 0.000 at t = π/2 while the peak swung ×2.18 per cycle. The surface is now a rigid translate of itself — asserted as an identity, not a tolerance. |
| `sawtoothWave` | Sawtooth Fourier | 🟢 A | 2/π·Σ(-1)^(k+1)sin(k(x+t))/k. Same round-6 repair; this was the worst case — at t = π/2 the sawtooth was replaced outright by (2/π)ln(2cos(u/2)), correlation 0.000, peak ×1.62 per cycle. |
| `triangleWave` | Triangle Fourier | 🟢 A | 8/π²·Σ(-1)^k·sin((2k+1)(x+t))/(2k+1)². Same round-6 repair; softest case, since this conjugate series converges absolutely — correlation 1.000 → 0.976 mid-cycle, peak ×1.32. |
| `pulseWave` | Pulse with duty cycle | 🟢 A | Standard pulse Fourier formula, with the round-6 repair. The common phase cost the duty cycle itself: correlation with a pulse of the stated duty fell 0.985 → 0.735 at t = π/2. |
| `gibbsPhenomenon` | Gibbs ~9% overshoot | 🟢 A | Real Gibbs constant ≈ 0.0894 at first overshoot — exhibited correctly. |
| `heat2D` | Heat equation Fourier | 🟢 A | Σ_{n≤N} bₙ·sin(nπ(freq·x+½))·e^{−n²π²τ}, bₙ = 4/(nπ) for odd n and 0 for even, truncated at N = 5 + round(6·comp), at τ = 0.01 + 0.005·(t mod 30) — the diffusion time is replayed, not the session clock this row used to print (FIX #5, r4; the uptime note below already records the replay). What the replay costs, measured at the factory sliders on grid 81: max\|y(30.1) − y(29.9)\| = 0.210046 against sup\|y(29.9)\| = 0.073858, so the plate jumps by 284 % of its own height once every 30 formula-time units (62.5 s of uptime at 0.008 per frame, 60 fps), where the same 0.2-unit step away from the seam moves it by 0.001895. The arithmetic is exact against the named series at that τ. **Round 9 — the Σ was bare, here and in the caption, and the kernel truncates.** N = 5 + round(6·comp) takes exactly the values 8, 9 and 10 over the reachable comp ∈ [0.5, 0.9] (comp = 0.5 + mid·0.4, so comp = 1 is not reachable): 8 below comp ≈ 7/12, 9 from there to comp = 0.75, 10 at 0.75 and above — swept at 400 001 points of the mid slider, the set is {8, 9, 10} and nothing else. The caption prints that integer range rather than the expression because 6·comp lands on an exact half at comp = 0.75, which mid = 0.625 reaches, and there JS `Math.round` returns 5 while round-half-to-even returns 4 — two different pictures out of one written formula, the trap `trace` was caught by this round. The range is the same set under either convention; the expression is not. Read as a bare Σ the caption fails clause 1: drawn ÷ the same series carried to n ≤ 400 is not one constant but spreads 3.03·10⁻⁴ at the factory sliders (grid 81, t ∈ {0, 3.7, 7}), 5.80·10⁻⁶ at the audio envelope and 5.19·10⁻⁶ at the reachable slider tops — the boot instant τ = 0.01 is the worst case, the tail shrinking as τ grows — against 5.9·10⁻¹⁶ once the kernel's own N is supplied to the same probe. What the truncation costs the picture is nevertheless small, which is why a range and not a single N is enough: max\|y(N=8) − y(N=10)\| is 1.34·10⁻⁵ against sup\|y\| = 0.279759 at the factory sliders, and 0.0048 % of the height at all five settings (control: N = 1 against N = 8 is 18.76 %). |
| `parseval` | Parseval spectrum | 🟢 A | \|cₙ\|² for square wave. |
| `wavelets` | Haar wavelet | 🟢 A | ±1 indicator on dyadic intervals. |
| `dct` | DCT-II | 🔵 B | Full DCT-II, X[k] = Σₙ x[n]cos(π(n+½)k/N) over N=8 samples of a two-harmonic test signal with f₀ = 1 + comp·3. Exact to float64; kept at B because the signal is synthetic rather than a canonical function. **Round 9 — the bin clamp binds, and the caption now says so.** The bar under a column is picked by `clamp(Math.round((x+3.5)/7·N), 0, N−1)`; round() returns 8 for x ≥ 3.0625 and the clamp folds it into bin 7, so the rightmost bar is the widest on the plate. Columns per bin: [5, 10, 10, 10, 11, 9, 10, 16] at grid 81 and [10, 20, 20, 20, 21, 19, 20, 31] at 161 — 6 columns of 81 and 11 of 161 absorbed, the last bar 1.60× and 1.55× an interior one (grid 90, the suite's, reads [6, 11, 11, 11, 12, 11, 11, 17], 1.545×). It is not a float64-only reading: the Float32Array that reaches the vertex buffer carries one identical height across the last 16 columns at grid 81 and the last 31 at 161. The map contains no slider, so these are grid facts alone, and bin 7 is strictly the widest on every grid from 12 up (only 5, 6, 10 and 11 tie, and nothing draws them). Control: the sibling bar chart `parseval`, whose clamp does not bind, has 0 columns moved by it at either grid; and replacing the round with `Math.min(N−1, Math.floor(…))` gives eight near-equal bands, [10, 10, 10, 10, 10, 10, 10, 11] at grid 81 — a different picture, which is why the repair was a caption and not a kernel. Replaced a sum of the basis vector alone, which is the transform of the constant 1 and therefore exactly 0 for every k ≥ 1. |
| `convolution` | (f*g) | 🟢 A | Midpoint rule, 32 nodes, over a window that follows the evaluation point. Round 6: the window was fixed at τ ∈ [−2, 2] while x·freq runs to ±15.9, so for most of the plate the Gaussian kernel sat entirely outside the interval being summed — measured error 106 % of the peak at the default slider and 259 % at freq 2. Worst error now 1.4e-10 across the whole slider range. |
| `spectralLeakage` | Hann + DFT | 🟢 A | Real Hann window + DFT magnitude. |
| `harmonics` | Σ aₙ sin(nx) | 🟢 A | Standard harmonic sum. |
| `stochasticFourier` | Weyl-Phase Fourier | 🟢 A | Σ_{n=1}^N cos(n(2·freq·x+0.3t)+φₙ)/n, exact to float64 and exactly reproducible. Round 9 renamed the entry: φₙ = ((n·2654435761) mod 2³²)/(2³²−1)·2π and 2654435761/2³² = 0.6180339868 is the golden-ratio conjugate, so the phases are a Weyl sequence — equidistributed, which is the opposite of random, and the pattern the r6 `hash32` note was written to replace. Sorted circular gaps give sd/mean = 0.3057 with 4 distinct gap lengths — three, up to the single-unit offset that dividing by 2³²−1 rather than 2³² adds to the wrap gap, which is the three-distance theorem's ceiling; against 4000 draws of 15 uniform phases the median is 0.8952 and 0 of 4000 are as even, while `hash32` through the same statistic scores 0.8503 with 15 distinct gaps. The letter is unaffected — the sum is exact whichever phases it carries. |
| `fejerKernel` | Fejér kernel | 🟢 A | (sin(Nx/2)/sin(x/2))²/N, measured **2.0×10⁻¹⁴** against the defining sum (1/N)\|Σ_{k<N} e^{ikx}\|² in mpmath — a different expression, not the closed form re-typed — with the singular points deliberately sampled. Round 8: x used to be shifted by +10⁻⁶ to step around sin(x/2) = 0, at a cost of 9.32×10⁻⁶, four orders above the top of tier A. The singularity is real (x = 0 is a column of the mesh on eleven of the twelve odd grids the app can reach — grid 83 is the exception, its centre column sitting at 4.44·10⁻¹⁶ because `−3.5 + 41·(7/82)` does not round to zero; the closed form is well conditioned there and draws the same 0.378 at the factory sliders and 2.025 at the reachable over-drive as grid 81 does through the branch) but removable, F_N(2πk) = N, so the shift is now that value, taken only where sin(x/2) is exactly 0 — and x is folded into [−π, π] first, because the removable point is at **every** 2πk and the closed form is unusable near all of them: at x = π, freq 1 it returned −12.0 where the defining sum gives 17, since (N+½)x rounds at 7×10⁻¹⁵ while sin(x/2) is 1.2×10⁻¹⁶. Branching at zero alone would have been a regression there, measured 4.0 world units. |
| `dirichletKernel` | Dirichlet kernel | 🟢 A | sin((N+½)x)/sin(x/2), measured **5.6×10⁻¹⁴** against the defining sum 1 + 2Σ_{k=1..N} cos kx in mpmath, singular points included. Same +10⁻⁶ shift (cost 2.48×10⁻⁵), same repair and same 2π fold as `fejerKernel`: D_N(2πk) = (2N+1)·cos((2N+1)πk)/cos(πk) = 2N+1 for every k. Without the fold the error at the non-zero removable points reached 6.5 world units. |

### 8. Differential Equations (16) — 12 A · 1 B · 3 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `simpleHarmonic` | SHO | 🟢 A | A·cos(ωt+φ). |
| `dampedOscillator` | Damped oscillator | 🟢 A | exp(-γt)·cos(ωt). |
| `forcedOscillator` | Driven resonance | 🟢 A | Steady-state amplitude formula F/√((ω₀²-ω²)²+(2γω)²) — exact. |
| `exponentialDecay` | Exp decay | 🟢 A | x₀·exp(-λt). |
| `logisticGrowth` | Logistic growth | 🟢 A | K/(1+(K/x₀-1)·exp(-rt)) closed-form. |
| `predatorPrey` | Lotka–Volterra | 🟡 C | 5–25 Euler steps — does not conserve invariant H. Round 9: the O(dt) gap against an exact integrator is NOT a defect and the earlier claim of one is withdrawn — it converges first-order (|y − scipy| = 6.71e-2, 6.72e-3, 6.72e-4, 6.72e-5, 6.72e-6 for dt = 5e-2 … 5e-6), so comparing an Euler kernel with an exact one measures the method. What is a defect is the domain and the guard: the plate coordinates ARE the initial populations (px = 10 + 5·x·freq, py = 5 + 2·z·freq), so at the factory sliders 22.222 % of the grid-81 plate starts with a negative prey population and 14.815 % with a negative predator population — 33.745 % outside the model, 45.191 % at the audio envelope, 67.048 % at the slider tops. There the kernel's max(0,·) fires, and Lotka–Volterra contains no such guard: 14.815 % of the plate at the factory sliders draws at exactly −0.140000 (23.457 % at −0.210000 at the audio envelope, 41.975 % at −0.450000 at the slider tops), a dead-flat extinction shelf, permanent because once y = 0, dy = y(δx−γ)dt = 0. Control: the same Euler from IC(0,−2) gives −0.621092 unguarded and exactly 0.000000 guarded, while from IC(10,5) the two agree to every digit. |
| `heatEquation` | Heat 1D | 🟢 A | Truncated Fourier solution. |
| `waveEquation` | Wave 1D | 🟢 A | Truncated d'Alembert sum. |
| `laplacePDE` | Laplace solution | 🟢 A | Re(z²)=x²-z² is harmonic by construction. |
| `eulerMethod` | Euler method | 🟡 C | Demonstrates Euler — by definition O(h) error. Faithful demonstration of an inaccurate method. |
| `rungeKutta4` | RK4 | 🔵 B | Standard RK4, error O(h⁴). |
| `beamBending` | Euler-Bernoulli beam | 🟢 A | Round 8: the solution was exact and invisible. **What is drawn is a deflection in world units**, δ·sin(nπξ) with δ = 0.45·amp·e^(−0.3z²) and n = round(1 + comp·4) — the exact simply-supported solution of EI·y'''' = q̂ₙ·sin(nπξ) for the mode-scaled load q̂ₙ = EI·(nπ/L)⁴·δ. Every mode is therefore drawn under the load that gives it the same peak deflection, so the picture shows mode shape at constant deflection and **not** the 1/n⁴ softening a fixed load would give; the load amplitude rises as n⁴ (measured q̂₃/q̂₁ = 81, q̂₅/q̂₃ = 7.7160493827, both to 1e-15). Before: the load amplitude was a fixed 0.8 and the (nπ/L)⁴ denominator was never compensated, so with comp = 0.5 + mid·0.4 reaching n = 3..5 the tallest point on the whole plate was 5.5e-2 world units — 6.8e-4 inside the comp window the audio can actually reach, and a floor of 2.7e-5 — against 0.42..2.28 for twelve of the other fifteen entries in this collection (the remaining three peak at 8.7, 114 and 539, which is a separate complaint). Measured after: peak 1.012 over the reachable slider box (amp 0.7..2.25, freq 0.3..4.55, comp 0..1), 0.84 of the collection median of 1.203, floor 0.255. Verified against two independent solvers of EI·y'''' = q under y(0)=y(L)=y''(0)=y''(L)=0 — sympy dsolve with the four conditions fitted symbolically, and a finite-difference BVP solved as two tridiagonal systems in numpy — both first shown to reproduce the textbook 5qL⁴/384EI = 0.013020833333 for a uniform load (error 0 and 2.6e-9): drawn row against sympy 5.7e-16..2.1e-15, i.e. 2e-15 of the peak; load energy outside the named mode ≤ 2.1e-10 of q̂. Peaks on grids 25/90/161 agree to ×1.155 (worst corner freq 2, n = 4, where grid 25 misses the crest). Independent of t, as a static load on a static beam must be — the audio moves it through amp and comp instead. The beam spans \|x\| ≤ 3.5/freq, so above freq = 1 the rest of the plate is flat at zero deflection because there is no beam there: measured 23.2 % of the x range at freq 1.3 and 78.2 % at freq 4.55. |
| `schrodingerBox` | Particle in box | 🟢 A | √(2/L)·sin(nπx/L)·cos(Eₙt/100), Eₙ = n²π², n = 1+round(5·comp) — exact to a flat amp·0.45 once the plate envelope e^{−z²/4} is divided out (drawn ÷ named·e^{−z²/4} = 0.315000000 at the factory sliders over 6156 grid-81 vertices at every instant). Round 9: with ħ = 1 this is Re Ψₙ = Re(ψₙe^{−iEₙt}), which is why the letter stands — but the factor passes through zero and changes sign, so the plate's peak runs 0.445477 (t = 0) → 0.003715 (t = 1.0) → 0.445415 (t = 2.0), a factor of 119.9, flattening to 0.83 % of its height and returning INVERTED every 4.145 s at the factory sliders and every 1.842 s at the audio envelope. The caption now says Re Ψₙ rather than ψₙ, because a stationary state must not do that and Re Ψ must. |
| `reynoldsFlow` | Stokes/Poiseuille | 🟡 C | The parabolic profile (1−r²) is exact to 5.6e-17 in cross-section. Round 8: the sin(x·freq·0.5 + t·0.3) factor multiplying it makes the streamwise velocity depend on x, and for the unidirectional flow this row names that is a violation of incompressibility — ∂u/∂x reaches 0.225 against max \|u\| = 0.450, i.e. half the field. Exact profile, decorative modulation, and the modulation is what the viewer sees moving; C rather than a bounded-error claim about a flow the field does not satisfy. |
| `fishersEquation` | Fisher wave front | 🟢 A | Round 6: the logistic ansatz is not a travelling-wave solution of Fisher–KPP at any speed — substituting u = σ(kξ) into −cu′ = Du″ + ru(1−u) requires Dk² = 0. Measured residual 1.29 at the speed the code claimed and 0.48 at the best speed any logistic could have. Replaced by the Ablowitz–Zeppetella closed form u = (1 + e^{ξ√(r/6D)})⁻² travelling at c = 5√(rD/6), residual 6e-8 — measured on the SHAPE, with the display gain divided out, which is what the suite's own test does when it reads U = f(ξ,0,0,{amp:1})/0.5. Round 9: the drawn height is K·(1+e^{kξ})⁻² with K = amp·0.5·e^{−z²/4}, and Fisher–KPP is nonlinear, so a constant multiple of a solution is NOT a solution — sympy gives the residual of the caption's own u(1−u) form in closed form as r·g·(g−1)·U², zero iff g = 1, and the drawn plateau at z = 0 is 0.350000 at the factory sliders, 0.525000 at the audio envelope and 1.125000 at the slider tops, never 1. The caption now writes ru(1 − u/K) with K free, as `logisticGrowth` already does, and the drawn field satisfies it identically at every setting (drawn ÷ K against (1+e^{kξ})⁻² worst 2.2e-16 over the grid-81 factory plate; control, against the plain logistic round 6 removed, 0.993061). The test puts the drawn profile back into the equation rather than comparing it with another implementation. |
| `pendulumNonLinear` | Phase portrait | 🟢 A | Energy contour H = ½ω² - cos(θ) exact, and the height is sin(2H + 0.3t) — a many-to-one relabelling of correct contours, which is why the letter stands and the caption now says so. Round 9: 2H spans 52.993835 rad across the grid-81 plate at the factory sliders, 8.434 full turns of the sine, so about seventeen disjoint energies share every drawn height; at the slider tops it spans 1018.261114 rad, 162.061 turns, and the plate is 162 concentric ripples rather than a readable phase portrait. Drawn ÷ sin(2H + 0.3t) is a flat amp·0.35 (0.245000000 at the factory sliders); against H itself the ratio runs −0.106443…+0.489981 at t = 0 and −11.558260…+28.975194 at t = 3.7, so no scale relates the height to the energy. |

### 9. Integral Transforms (16) — 10 A · 4 B · 2 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `fourierTransform` | FT of Gaussian | 🟢 A | √(π/a)·exp(-ω²/4a) exact. |
| `fourierInverse` | F⁻¹ of rect | 🟢 A | The **unnormalised** sinc, sin(u)/u with u = Wx and W = 4·freq — a different convention from `specialFunctions/sinc`, which is sin(πr)/(πr); one word, two functions, and the row used to say only "sinc(x)". F⁻¹[rect_W](x) = sin(Wx)/(πx) = (W/π)·sin(u)/u, and the W/π is absorbed into the display scale, so the surface is normalised to amp·0.5 at x = 0 rather than to W/π; the e^{−0.3z²} is decoration. Measured against mpmath `sinc` at 40 dps (control: PARI/GP agrees to 30 digits): worst \|Δ\| **2.4×10⁻¹⁶**. Round 8 removed the +10⁻⁹ added to u, which cost 1.5×10⁻¹⁰ at the factory amplitude, 2.2×10⁻¹⁰ at amp 1 and 4.9×10⁻¹⁰ at amp 2.25 — the A/B seam rather than a broken tier, but bought nothing. The picture moved by at most 1.2×10⁻⁷ world units, below float32 mesh resolution. |
| `laplaceTransform` | L{1} | 🟢 A | 1/s on s ∈ [0.35, 5.1]. Round 6 moved the window off the pole: L{1} converges for every Re s > 0, so where the window starts is free, and starting it at 0.1 put the left edge of the plate at 1/0.1 = 10 — 3.5 world units at the factory sliders against a ~3-unit frame, purely because the window was pushed against the pole. |
| `laplaceDecay` | L{e^(-at)} | 🟢 A | 1/(s+a). |
| `zTransform` | Z{a^n} | 🟢 A | z/(z−a) direct, drawn on its region of convergence. Round 6 moved the plate: it used to start at Re z = 0.5 while a reaches 0.9, so part of the picture stood where Σaⁿz⁻ⁿ does not converge and the pole was crossed exactly (Im z = 0 is a row of the mesh). Peak measured 22.8 / 8.4 / 89.2 across grids 25 / 90 / 161 — ×10.6, a different picture per GPU. The plate now starts at \|z\| = a + 0.2. |
| `waveletTransform` | Morlet | 🟢 A | exp(-x²/2)·cos(ω₀x). |
| `hilbertTransform` | (f + H[f])/2 | 🟢 A | Both halves are exact — H[sin ωu] = −cos ωu, verified against the principal-value integral (residual halves with the cut-off: 7.75e-4 → 3.88e-4 → 1.94e-4 at T = 200π/400π/800π, so that is the reference truncating, not a disagreement). What is drawn is their mean, (f + H[f])/2 = sin(ωu − π/4)/√2, not the real part of the analytic signal: Re(f + iH[f]) is f itself. Agreement with (f+H)/2 is exactly 0.0 over all 841 nodes and all 12 sweeps. |
| `radonTransform` | Sinogram | 🟢 A | Analytic Radon transform of two Gaussians (closed form). Replaced decorative rotated Gaussian. |
| `hankelTransform` | "Hankel of f" | 🟡 C | Just J₀(ρ)·exp(-ρ·0.3) — that's the kernel evaluated, not the transform of any function. |
| `mellinTransform` | Mellin kernel | 🔵 B | x^(s-1)·e^(-x) is the integrand. Not the transform itself. |
| `stieltjesTransform` | Stieltjes | 🔵 B | Midpoint on the substitution t = u/(1−u), which carries [0, ∞) onto [0, 1), 64 nodes. Round 6: the integral runs to infinity and the sum stopped at t = 5 with h = 0.25 — worst error 1.6e-2 at z = 0.5, an order and a half outside this tier, and the bound failed at every reachable z. Now 2.0e-5 worst, 2.2e-9 at z = 1, checked against e^z·E₁(z). |
| `cauchyIntegral` | Cauchy formula | 🟢 A | Round 6: singularity subtraction, ∮f/(z−z₀)dz = ∮[f(z)−f(z₀)]/(z−z₀)dz + f(z₀)·2πi·n(z₀). The regular integrand is the polynomial z + z₀ for f = z²+c, so the quadrature never meets the pole, and the winding number is counted by argument increments. Plain quadrature broke as z₀ neared the contour and the reachable region crosses it (\|z₀\| reaches 2.47 against R = 2): peak 1.28 / 4.37 / 14.7 / 35.1 across grids 25 / 90 / 161 / 400, and 33 % low inside the contour. Round 9: not at every mesh density it was not — the winding number was counted by argument increments over 48 contour samples, and that unwrapping fails for z₀ within ~4.3e-3 of the contour (the 48-gon sagitta R(1−cos(π/48)) = 4.282e-3; worst measured hole 4.09e-3). Measured on the meshes the app draws: 48 of 25921 grid-161 vertices INSIDE the contour dropped to 0 at the factory sliders (worst gap 0.594686 world units), 40 at t = 3.7, 72 at the audio envelope with gaps to 1.765460 — and the set flickered, because the samples carry +0.1t. For a circle the winding number of z₀ IS |z₀| < R, so the count is now the definition: 0 holes at grids 81 and 161 × factory / audio / maxima × t = 0 and 3.7, with 0 of 25921 vertices moved away from the holes. Now exact to 1e-12 at every mesh density. |
| `stocksFormula` | Green's theorem | 🟢 A | Curl computed analytically, exact: ∂Q/∂x − ∂P/∂y for F = (−cos(fx)cos(fz), +cos(fx)cos(fz)), which collapses to −f·sin(f(x+z)) and matches the shipped kernel to 1.06e-16 at the factory sliders and 1.09e-14 at the maxima over 6561 grid-81 vertices. Round 9: what the entry draws is the point-wise INTEGRAND of Green's theorem, not either side of the identity its caption states, and the caption now says so. The row comment claiming "curl of F = (−y,x) → constant 2" was false of the shipped code by 0.629998 at the factory sliders — three times the field's own height, and a constant field cannot have the measured range [−0.209998, +0.209998] — and is replaced in the same commit. |
| `poissonIntegral` | Poisson formula | 🔵 B | Trapezoid on 96 nodes, radius capped at 0.9. Round 6: for boundary data cos(3φ+s) the integral is exactly r³cos(3θ+s), and a trapezoid on N nodes also picks up the modes N±3 with weights r^(N∓3) — at N = 16 and r = 0.95 that is a worst absolute error of 1.52 on a quantity bounded by 1. Now 8.1e-5. |
| `continuousWavelet` | CWT scalogram | 🔵 B | Integrated in ξ = (τ−b)/a over \|ξ\| ≤ 5, 64 nodes, so the window follows the scale. Round 6: the grid was fixed at twenty samples of step 0.3 while the scale runs down to a = 0.1, whose oscillation has period 0.126 in τ — the wavelet simply did not land on it (error 0.39 at a = 0.1, 0.62 at a = 0.35, on a quantity of order 1) — and at the wide end the same fixed window cut the wavelet off. Worst error now 1e-6 over the whole scale range. |
| `fourierSlice` | Slice theorem | 🟡 C | Decorative; doesn't actually compute slice. |

### 10. Topology & Geometry (16) — 4 A · 4 B · 8 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `mobiusStrip` | Möbius strip | 🔵 B | Simplified parametrization, correct topology — the drawn quantity is the height coordinate of exactly the r the caption prints, up to the display gain (residual 1.39e-16 at the factory sliders, 4.44e-16 at the slider tops, after dividing by it) and it survives the Möbius gluing (u,v) ~ (u+2π, −v) to 1.94e-16, so a round-9 claim that the wrong component was drawn was raised and killed. What does stand: v = clamp((r−0.6)·3, −1, 1) is pinned at ±1 on 95.0 % of the grid-81 plate at the factory sliders, 97.1 % at the audio envelope and 99.7 % at the slider tops, so almost the whole picture sits on the strip's edge and is a function of the azimuth alone, with no width variation — now stated in the caption. Control: the widened ramp clamp((r−0.6)/1.6, ±1) reads 69.6 % through the same counter. |
| `kleinBottle` | Klein bottle figure-8 | 🔵 B | Approximate immersion. |
| `torusKnot` | Torus knot | 🟡 C | A phase field with a Gaussian ring envelope, not a (p,q) parametrisation: the winding measured on r = 1 gives p = 2 toroidal cycles, but the "poloidal" winding is in radius rather than around the tube, and at the default slider the (p,q) pair names a link rather than a knot. Round 8: tier B asserts a bounded 1e-3…1e-7 approximation to something, and there is no parametrisation here to approximate. |
| `boysSurface` | Boy's surface | 🔵 B | Third coordinate of the Bryant–Kusner immersion of RP², evaluated over the sphere in the homogeneous stereographic w = p/q; agrees with the immersion at 50 digits to 4.8, 5.5 and 6.2·10⁻¹⁶ on grids 41, 81 and 161 at the factory sliders **at t = 0** — an instant, and near the quiet end of its own clock: θ carries 0.2t, and over one turn of it (31.4 s) the grid-41 figure runs 4.1·10⁻¹⁶…1.9·10⁻¹⁵, against the 4.8·10⁻¹⁶ that grid reads at boot. That is the double `entry.f` returns, against the immersion evaluated at the *same double* abscissa the kernel was handed, which is bitwise the abscissa `THREE.PlaneGeometry(7, 7, N−1, N−1)` gives the vertex (control: a 1·10⁻¹⁰-shifted origin reads 1.000·10⁻¹⁰). Evaluate the immersion instead at the exact rational −7/2 + 7i/(N−1) that the lattice denotes and the same kernel reads 1.07, 1.15 and 1.40·10⁻¹⁵ — twice the figure, and not a disagreement but a different quantity, because that comparison charges the kernel for the plate's own coordinate rounding: a *perfect* kernel measured that way still reads 1.091·10⁻¹⁵ on all three grids, so it cannot certify an implementation. Over the reachable box — amp ≤ 2.25, freq ≤ 4.55, t ≤ 100, 100 random settings — the worst absolute is 1.78·10⁻¹⁴ at grid 161, and no address is quoted for it because none survives being printed: at the amp 1.92 / freq 4.37 / t 38.9 of the previous revision the sweep reads 1.06·10⁻¹⁴, while the three-significant-figure cell around that address spans 5.4·10⁻¹⁵ to 1.54·10⁻¹⁴ and contains the 1.2·10⁻¹⁴ it was given. The 4.6·10⁻¹⁶ this row once quoted alone was a factory figure at one grid, and the box maximum is thirty-eight times it. **Round 8 correction — neither corner the previous revision named produces the figure it was given.** amp 1.68 / freq 2.43 / t 98.9 reads 5.6·10⁻¹⁵ absolute at grid 81, not 1.5·10⁻¹⁴, and the amp 2.25 / freq 4.55 corner reads 6.3·10⁻¹⁵ at grid 81 and 1.05·10⁻¹⁴ at 161, not 1.4·10⁻¹⁴. That split is real and not a sampling accident: grids 41 ⊂ 81 ⊂ 161 bitwise (control: grid 90, which the app never draws, shares 2 of its 90 abscissae with 161), so every sup is monotone in the grid, and grid 161's worst vertex sits at x = −2.40625, which is not a grid-81 abscissa — grid 321 then finds nothing worse, still 1.05·10⁻¹⁴. Measured against the exact rational lattice instead, the same corner reads 1.946·10⁻¹⁴ on *both* grids and genuinely does not move, because that quantity's worst vertex is the shared double (2.4499999999999993, −3.2375), index (68,3) on grid 81 and (136,6) on 161. Both readings are right about the lattice; they are the maxima of two different quantities, and two-thirds of the larger one is plate coordinates (its floor at this corner is 1.31·10⁻¹⁴ at grid 81 and 1.36·10⁻¹⁴ at 161). The magnitude claim survives a full-grid sweep; the addresses did not — including this revision's own, which is why the box figure above is quoted without one, θ = x·freq·2π/7 + 0.2t being sensitive enough that a 10⁻⁶ change in freq reshuffles which vertex is worst. Every figure in this paragraph is the double `entry.f` returns; the drawn height goes through `generateSurfaceFromFormula`'s Float32Array and then a Float32 position attribute, where the same comparison reads 1.49·10⁻⁸, so these are claims about the expression's arithmetic and not about the rendered mesh. The comparison has to be absolute because the drawn height has a zero set on the plate: at the factory sliders the smallest height any vertex takes is 3.97·10⁻⁵ — grid 81 and grid 161 alike, the same shared vertex at x = 0.0875, z = −1.925 — and an absolute error of 4.1·10⁻¹⁷ there is a relative error of 1.0·10⁻¹². The 6·10⁻⁶ and 4·10⁻¹¹ this sentence used to quote reproduce on no grid tried: 41, 81, 90 and 161 give smallest heights 9.6·10⁻⁵, 4.0·10⁻⁵, 5.9·10⁻⁵ and 4.0·10⁻⁵, at relative errors 8.0·10⁻¹³, 1.0·10⁻¹², 1.4·10⁻¹² and 1.0·10⁻¹². 2.53·10⁻¹² after a day of uptime (t = 86 400 s, factory sliders, grid 81), from reducing θ = 0.2t in double — and already 7.5·10⁻¹⁴ after a single hour, which is four times anything the whole slider box reaches at t ≤ 100. Uptime, not the sliders, is what sets this row's worst case, and the box maximum above is only the worst case of a freshly loaded page. Round 8: the old height was a doubly periodic trig sum whose invariance group was the torus's — under the two antipodal identifications that make RP², (u+π, π−v) and (u+π, −v), it moved by 0.733 and 0.799 against its own sup of 0.696, so it was two-valued on RP² and not a function on it. The plate is now the sphere, the antipodal map is the glide (x,z) → (x + 3.5/freq, −z) — the 3.5 is the shift at freq 1 and it has to follow freq, because θ = x·freq·2π/7 and the half turn needs Δx·freq = 3.5; applying the literal +3.5 at freq 1.5, 2, 3.36 or 4.55 moves the height by 0.38–0.42 against a sup of 0.47, i.e. by most of the surface. Under the glide written correctly the drawn height is invariant **to 0.0 in the Float32 the vertex buffer stores** at the factory sliders, at amp 2.25, at amp 2.25 / freq 4.55 and after an hour of uptime — which is the sense in which the picture has the symmetry. **Round 8, second pass — "no mismatch anywhere" was one case short; round 9 — and the one case was quoted at its best.** After a *day* of uptime the Float32 glide does mismatch, at grid 161 and never at 81, and what the mismatch weighs is a property of the vertex it lands on rather than of the error: **every mismatch at every named setting is exactly one Float32 ulp** — adjacent representable values, the smallest nonzero difference there can be at that height. So it reads **3.64·10⁻¹²** at the factory sliders, where the vertex it lands on has height −5.16·10⁻⁵ (1 pair of 13 041, and this is the 3.6·10⁻¹² the row used to quote alone), and **5.96·10⁻⁸** at the reachable over-drive amp 2.25 / freq 4.55, where the worst-hit vertex has height 0.737 (3 pairs of 23 023 — also the slider-maxima reading, this kernel taking no comp). In between: 3 pairs of 13 041 at 7.45·10⁻⁹ at the suite's amp-1 baseline, 2 at 1.86·10⁻⁹ at the amplitude floor, and none at all at the audio envelope. Relative, the four readings are 7.0·10⁻⁸ (factory), 9.7 (amp-1), 8.1 (over-drive) and 8.2 (floor) — one ulp each — so the statement that reproduces is *one ulp, never more*, and 3.64·10⁻¹² is only the smallest absolute value that statement can take. Grid 81 shows none at any of the five settings, at t = 0, after an hour or after a day. The pair count is not a constant either: 3 321 at grid 81 and 13 041 at 161 hold at freq 1, but the glide is 3.5/freq, so at freq 4.55 it is 5 832 and 23 023. Controls on that comparison: the literal +3.5 at freq 4.55 mismatches all 3 321 pairs by up to 1.32, and a 10⁻⁹ relative nudge on one side — thirty times finer than the Float32 step at this height — still shows 40 mismatches, so a zero here is a measurement and not a blind probe. In double the same residual is 6.4·10⁻¹⁶ at the factory sliders on grid 81 at t = 0 and 6.7·10⁻¹⁶ at 161, running 5.0·10⁻¹⁶…1.9·10⁻¹⁵ and 5.6·10⁻¹⁶…2.0·10⁻¹⁵ over one turn of the clock; 2.0·10⁻¹⁵ and 2.3·10⁻¹⁵ at amp 2.25; 9.8·10⁻¹⁵ on both grids at amp 2.25 / freq 4.55 / t 11.3 — 9.769963·10⁻¹⁵ to the digit on 41, 81 and 161 alike, the grid-41 argmax (1.925, −3.15) being an abscissa all three carry, so the bitwise nesting above leaves no room for a smaller sup at 81 — inside a range of 3.6·10⁻¹⁵…1.9·10⁻¹⁴ over one turn at those sliders; and 3.2·10⁻¹² after a day of uptime, where the same argument reduction that costs the immersion its digits costs the symmetry its digits too. The 5.6·10⁻¹⁶ / 7.2·10⁻¹⁶ / 1.8·10⁻¹⁵ the previous revision gave reproduce under neither pairing tried — comparing a vertex against the *coordinate* x + 3.5/freq gives 6.4·10⁻¹⁶ / 6.7·10⁻¹⁶ / 2.0·10⁻¹⁵, comparing it against the *lattice index* half a plate along gives 1.5 / 1.5 / 4.9·10⁻¹⁵ — so the figures above are the coordinate pairing, named, and re-measured. (The old height moved 0.5625 on a sup of 0.4928, which is the number this is replacing.) the three roots of p⁶+√5p³q³−q⁶ are the triple point, which is what tells this from `romanSurface`. Apéry's parametrisation was rejected after measurement, not preference: it collapses the circle v = π/2 to the origin with ∂r/∂u = 0, so it is not an immersion, and neither the published-value control nor the RP² invariance control can see that. B and not A because only one coordinate of the immersion is drawn, and the plate's two z edges are the chart's poles — a whole row at the single point (0,0,−2), which is **5.0 % of the mesh within 1 % of the peak at grid 81 and 3.8 % at grid 161**. The 4.6 % this row used to quote is the grid-90 count, and 90 is not a lattice the app lays down; the share is a boundary-row count and falls roughly as 1/N, while the peak it is measured against is identical (0.470685) on grids 41, 81 and 161 at t = 0, so the movement is in the count and not in the peak (control: the same counter on a plate with no plateau at all, y = x at grid 81, reads 2.47 %). Those two shares are among the few figures in this row that are not instants: over one turn of the clock they run 5.00…5.04 % and 3.81…3.84 %, while the peak itself breathes 0.4698…0.4707 at grid 81 and 0.4705…0.4707 at 161 — so "identical on all three grids" is the t = 0 statement, and the plateau count is effectively the whole clock. Bounded by |B| ≤ 2, so no clamp and no fold; 0.37 ms per 90-plate against the collection's 0.12–1.72. |
| `romanSurface` | Steiner Roman | 🟢 A | Exact root of the surface's own equation. Round 8: nothing solved — the height was the guess xz/(2a+\|x\|+\|z\|), which meets x²y²+y²z²+z²x² = r²xyz only as x, z → 0 (0.2162 drawn where the surface is at 0.6439, at (−1.05, −1.05)), and since the surface is compact — the image of a sphere, so \|x\|, \|z\| ≤ r²/2 — 85.6 % of the plate had no real height at all and was given one anyway (that is the area fraction; on the lattices the app lays down it reads 85.8 % at grid 81 and 85.7 % at grid 161). The equation is quadratic in y, so the branch is closed form: 2xz/(r² + √(r⁴−4x²−4z²)), the sheet through the origin, in the form that does not cancel near the axis. Residual of the quoted equation at the drawn point, evaluated in exact arithmetic on the double `entry.f` returns, is ≤ **4.03·10⁻¹⁶ of \|r²xyz\|** — the worst of ~6.4·10⁸ (vertex, setting) pairs over grids 81 and 161 and the whole slider box — and the kernel's nine roundings cannot put it above 4·2⁻⁵³ = 4.44·10⁻¹⁶, a ceiling the sweep climbs towards and never crosses (3.07 → 3.46 → 3.59 ulp across three decades of sampling, 3.63 ulp with r² walked over 4·10⁶ consecutive doubles at one vertex). That is a bound with margin. **Round 8, second pass — the 7.4·10⁻¹⁶ this row gave as the bound is a different quantity, and it is not a bound either.** Written out and summed in floating point instead of exactly, the residual carries up to ~3.5 ulp of the *checker's* own rounding on top of the kernel's, because the four terms cancel to nothing; that is where this row's 6.1·10⁻¹⁶ for the factory sliders comes from — reproduced to the digit, both grids, t = 0 — and 7.4·10⁻¹⁶ is simply that quantity's sample maximum over a 5·10³-setting sweep, which keeps rising with density: 7.73·10⁻¹⁶ at 2·10⁵ settings on grid 81, 8.17·10⁻¹⁶ at 5·10⁴ on grid 161. It is a measured worst over a stated sweep and has to be quoted as one. (The ≤ 2.9·10⁻¹⁶ this row stated before that is below even the exact figure, so it was never a bound under any reading.) A reviewer was right to beat 7.4·10⁻¹⁶, but the address does not reproduce: at amp 2.25 / freq 1.008 / t 5.236 on grid 81 the worst is 5.49·10⁻¹⁶ under that same floating-point checker and 6.06·10⁻¹⁶ under the loudest of seven natural ways of writing the sum (2.71·10⁻¹⁶ exact), and the maximum of the 13×13×12 setting lattice that node belongs to sits at amp 1.908 / freq 1.008 / t 3.491 instead. All of this is at the double: the Float32 the vertex buffer stores sits 5.25·10⁻⁸ from the equation, eight decades further out, so the figure is about the arithmetic and not about the picture. Control: the shipped height multiplied by (1 + 10⁻¹²) reads 9.99·10⁻¹³ through the identical probe, against 2.18·10⁻¹⁶ untouched. **Round 8 — three of the figures this row quoted were measured wrongly, and one of its two "checks" was not a second check at all.** (i) The preimage residual \|yz/x + zx/y + xy/z − r²\| is not bounded by 3·10⁻¹⁵. It carries the scale of r², which runs 0.35…90 over the reachable box, so it reads **1.78·10⁻¹⁵** at the factory sliders and **2.84·10⁻¹⁴** wherever r² passes 64 — amp 2.25 / freq 0.3 gives r² = 75 at t = 0 and 90 at t = 5.236, and every corner with a comparable r² reads the same — worst of 364 528 drawn vertices on grid 81. Both readings are exactly 2 ulp of r², which is the point: the quantity is a scale, not a bound. Divided by r² it is ≤ 4.2·10⁻¹⁶ everywhere over the same sweep, and that is the figure that does not move with the sliders. **Round 8, second pass — the two corrected figures were themselves wrong.** 2.4·10⁻¹⁴ is not a value this residual can take: ulp(86.23) = 1.42·10⁻¹⁴ and every reading over the box is an integer multiple of it, so the corner the row named produces 2.84·10⁻¹⁴, 18 % above what was written for it; and the factory half was 25 % low, 1.78·10⁻¹⁵ rather than 1.4·10⁻¹⁵, which is the *relative* figure quoted elsewhere in this row. Controls for both: the pre-repair kernel — the xz/(2a+\|x\|+\|z\|) guess — through the identical probe reads 9.00 absolute and 1.29 relative to r², and the shipped height multiplied by (1 + 10⁻¹²) reads 7.0·10⁻¹² absolute against 1.78·10⁻¹⁵ untouched. (ii) That preimage was described as "the check that it is on Steiner's surface and not merely on the quartic containing it". It cannot separate those: sympy simplifies (yz/x + zx/y + xy/z − r²) − (x²y²+y²z²+z²x² − r²xyz)/(xyz) to 0 identically, and on the shipped doubles the two agree to the last bit; (yz/x)(zx/y) = z², (zx/y)(xy/z) = x², (xy/z)(yz/x) = y² are identities, so every real point of the quartic off the coordinate planes has a real sphere preimage — 38 070 sampled points on both sheets, all three terms non-negative in every one. The quartic residual is the whole of the evidence, and it is the number above. (iii) Against mpmath's roots the height is not exact to 5.8·10⁻¹⁶, and it is not exact to a single number at all. **Round 8, second pass — the 1.4·10⁻¹⁵ this row replaced it with is another sample maximum, and a denser sweep beats it.** Write σ = √D/r² for how far a vertex sits from the fold circle; the kernel's error is the loss in D = r⁴ − 4ρ², which is a difference of nearly equal numbers exactly where σ is small, so the relative error runs at **≈ 4.4·10⁻¹⁶/σ**. Measured worst × σ stays between 0.7 and 4.0 ulp across every decade of σ from 8·10⁻³ to 1 — thirteen bins, 205 572 vertices, ten settings on grids 41/81/161 — so that is a law and not a sweep, and any "away from the fold" figure is a statement about where the filter is put. At this row's own filter, σ > 0.05, **round 9 re-measures it over settings this document actually names** — factory, the suite's amp-1 baseline, the audio envelope, the reachable over-drive, the amplitude floor — each vertex against the exact root at the same doubles the kernel is handed: the worst is **1.36·10⁻¹⁵** at t = 0 over 101 970 drawn vertices on grids 81 and 161 (at the over-drive on 161, x = 0.30625 — the double 0.3062499999999999 — z = −2.45, σ = 0.053), and **2.41·10⁻¹⁵** once one turn of the clock is swept at 512 instants (also at the over-drive on grid 161, t ≈ 3.60, σ = 0.058). The law allows 8.8·10⁻¹⁵ there, and neither figure is a bound: both are sample maxima over a named set, and the ≈ 4.4·10⁻¹⁶/σ law above is what stands in for one. The 2.6·10⁻¹⁵ this replaces was the worst of 205 328 vertices over *ten settings the row never named*, so nobody but its author could re-measure it; 1.4·10⁻¹⁵ before that. The oracle was checked before it was used: 60-digit `Decimal` and a binary128 sweep agree on all ten setting/grid readings to four significant figures, the closed form leaves a quartic residual of exactly 0 in 60-digit arithmetic at (1, 1) with r² = 7 and matches `mpmath.polyroots` to 5·10⁻⁴², and numpy float64 reproduces the shipped kernel bitwise on all 162 410 vertices first. Control: the same probe on the shipped height multiplied by (1 + 10⁻¹²) reads 1.00·10⁻¹² in every bin, a floor the law cannot hide. And a vertex that lands *on* the rim pays what a double root costs: at (−2.8, 2.1) on the grid-41 lattice at the factory sliders at t = 0, where D is zero in exact arithmetic, the relative error is 2.8·10⁻⁹ against the root at the double abscissa (4.7·10⁻⁹ absolute on a height of 1.68) and 1.7·10⁻⁸ against the root at the exact rational the lattice denotes (2.9·10⁻⁸ absolute) — the 2.8·10⁻⁹ this row gave first is the double-abscissa reading and reproduces exactly (2.7817·10⁻⁹), and the 5.3·10⁻⁹ that briefly replaced it is neither convention's. Which convention is meant still has to be said, because at a rim vertex it is worth a factor of six — and so does how the oracle is handed the abscissa: parse it from the double's decimal `repr` rather than the double itself and the same probe reads 5.32·10⁻⁹, a 10⁻¹⁷ displacement that the vanishing √D turns into a factor of two. All of this sits under the Float32 the vertex buffer stores, whose own relative step is ≈6·10⁻⁸; it is quoted because the tier claim rests on it, not because it is visible. Outside the fold circle ρ = r²/2 the plate is flat because the surface ends there. At the factory sliders that circle is the plate's own inscribed circle, so the empty fraction tends to 1 − π/4 = 21.46 % and each mesh reads what its vertices land on. Counting the vertices that lie outside the fold circle — the ones with no surface above them — that is **23.5 % at grid 81 and 22.6 % at grid 161**, the two lattices the app lays down; counting instead every vertex the kernel leaves at exactly zero it is 25.9 % and 23.8 %, and the difference is the two double lines x = 0 and z = 0, honestly zero rather than empty, which only an odd grid carries at all (157 vertices of 6561 at 81, 317 of 25921 at 161 — the even grid 90 has no axis row and reads 23.7 % under both countings, which is the figure this row used to quote). As r breathes ±20 % over the clock the outside-the-circle share — the same counting as the two headline figures — runs 5.8…51.1 % at grid 81 and 5.3…50.4 % at 161, against a continuum band of 4.9…49.7 %; under the every-zero counting the same band is 8.2…53.1 % and 6.5…51.4 %. Controlled by a Monte-Carlo over 8·10⁶ uniform points, which reads 21.45 % against the closed form 21.460 % at ρ = 3.5 and tracks both ends of the breath to better than 0.01 points — no third digit is quoted, because a reseed moves it, and the control needed a control of its own: the plain LCG tried first read 22.30 % and could not have caught a 0.8-point error. The outside-the-circle counter reads 0.00 % at amp 2.25 / freq 0.3, where ρ = 37.5 puts the circle off the plate entirely, while the every-zero counter still reads 2.45 % at grid 81 and 1.24 % at 161 — the axis lines, which never leave. **Round 8 — the low-amplitude end of the box, which this row did not reach; and its second pass, where a lattice reading was given a continuum name.** Because amp and freq move r² rather than the height, the entry shrinks instead of clipping: at amp 0.2 — the amplitude slider's own floor — with freq 4.55, r² = 0.4396 at t = 0, and the surface is the whole of a disc r² = 0.44 world units across and r²/2 = 0.22 tall, peaking on its own rim at r²/4 = **0.1099**, 3.7 % of the ~3-unit frame; over the clock r² breathes 0.3516…0.5275, so that continuum peak runs 0.0879…0.1319, or 2.9…4.4 %. What the meshes carry is a different number and cannot be quoted without its grid and its instant: at t = 0 grid 81 peaks at **0.0479** (1.6 %) on 12 of its 6561 vertices and grid 161 at **0.0953** (3.2 %) on 60 of 25921, while grids 25 and 41 have no vertex off the two axis lines inside the disc and draw a flat plate. The 0.048 and 1.6 % this paragraph called r²/4 were that grid-81 lattice reading at t = 0; the 0.118 and 3.9 % it called the top of the breath name nothing at all — r²/4 there is 0.1319, the meshes read 0.0863 at 81 and 0.0868 at 161, and 0.118 is only a value the moving lattice peak passes through in transit. The largest any mesh reaches at this corner at any instant is a supremum rather than a maximum: 0.1237 (4.1 %), approached as r² falls to 2ρ = 0.4950 and the fold circle crosses the vertex (0.175, 0.175), which grids 41, 81 and 161 all carry — so a 2·10⁵-instant clock sweep lands on 0.1235 and a 2·10⁶-instant one on 0.1237, and no finite sweep reaches it. Grid 25 draws nothing here at any instant of the clock, and grid 41 is blank for 71.7 % of it and carries four vertices at its busiest. What a viewer meets at that corner is invisibility, not emptiness. amp and freq scale r² instead of the height, since a vertically stretched Steiner surface satisfies no equation of this form for any r — the best-fit r² at amp's top still leaves 0.75 of scale at the median — and that is also what keeps it in frame with no clamp: peak \|y\| = r²/4 until the plate corner cuts the fold off. Both peak figures are lattice figures and need their grid *and their instant* named: the continuum sup is r²/4, which is 1.75 at the factory sliders at t = 0 and breathes 1.40…2.10 over one turn of the clock, and 2.475 over the whole reachable range (attained at r² = 7√2 ≈ 9.9, where the rim reaches the plate's corner diagonal), while a mesh only sees what its vertices land on — at t = 0, 1.68 at grid 81 and 1.71 at grid 161 at the factory sliders, running 1.19…2.05 and 1.27…2.07 over the 2 618 frames of one turn. The largest lattice peak anywhere in the box is not 2.45 either: that was a 175-setting sample maximum, and a 13 230-setting sweep at grid 81 reaches 2.4712, converging on the continuum 2.4749 rather than on a figure of its own. The 1.52 this row used to quote is the grid-90 value at t = 0, and 90 is not a lattice the app lays down. |
| `enneperSurface` | Enneper z-Coordinate (u²−v²) | 🔵 B | u²-v² is the z-coordinate, exact — drawn ÷ (u²−v²) is 0.140000 at the factory sliders, exactly amp·0.2 once the (1 + 0.3·comp·sin 0.3t) gain is divided out. Round 9: it is plotted over the PARAMETER plane, because the plate axes carry u = 0.8·freq·x and v = 0.8·freq·z directly, while the caption said, until round 9, that the surface's own x and y are cubic in (u,v) — so what is drawn is a plain quadratic saddle where the Enneper surface is minimal: mean \|H\| — central differences at h = 10⁻³ over a 61×61 sampling of the cell \|x\|,\|z\| < 0.6, the cell and the stencil the `scherkSurface` and `catenoid` rows use — is 0.000351 (max 0.001018) at the factory sliders, 0.001010 at amp 1 and 0.668938 (max 2.294687) at the reachable slider tops (2.25/4.55/0.90), all at t = 0; over the whole plate instead of the cell it is 0.007795 (max 0.020805), 0.016874 and 0.146835 (max 2.150350) at the interior vertices of grid 81, and 0.007865, 0.016982 and 0.146240 at 161. Naming the cell is not a formality here: at the factory sliders the small cell reads twenty times BELOW the plate, and at the slider tops four times ABOVE it (0.668938 against 0.146835), because at c = 5.96 the curvature concentrates near the origin — one unnamed cell can therefore carry either verdict. **Round 9 re-measured this row.** The 0.006614 / 0.017679 / 0.014942 / 0.160928 / 1.389460 it used to print named no cell and reproduce on none: the closest square cell I could find is a 21×21 sampling of half = 3.0, which still leaves the five figures 0.1 % to 7.2 % out. The stencil is not what carries the numbers — the drawn field is exactly c·(x²−z²) with c = amp·0.2·(0.8·freq)²·(1 + 0.3·comp·sin 0.3t), a quadratic that central differences differentiate exactly, so h of 10⁻³, 10⁻² and 0.05 return the same six digits on grid 81 and only the cell moves them; the closed form \|H\| = 4c³\|z²−x²\|/(1+4c²(x²+z²))^{3/2} reproduces the numeric max on the 0.6 cell to six digits, 0.00101812 against 0.001018. The clock adds about half again: the worst instant over one turn of 0.3t is 0.000532 (max 0.001540) at the factory sliders, at t = 5.236. Controls through the same routine: y = x·z matches its closed-form H to 2.4·10⁻¹⁰ and its K to 1.0·10⁻¹¹, the unit hemisphere returns K = +1 to 3.3·10⁻⁶, an exact minimal Scherk graph y = (1/a)·log\|cos(a·x)/cos(a·z)\| over the same 0.6 cell reads mean \|H\| = 6.3·10⁻⁷ at h = 10⁻³ and 7.2·10⁻⁹ at h = 10⁻⁴ — the 3.9·10⁻⁹ this row used to quote is of that order but names no h — and the same graph made non-minimal with k = 0.7 reads 0.139185. The gain also breathes with comp (max/min 1.3529 at comp 0.5 and 1.7397 at comp 0.9), unlike the fixed ±20 % display factor its siblings carry, so the caption states it. **Round 11 — renamed.** The caption said "the Enneper z-coordinate" and the label said "Enneper Surface"; only the label reaches the screen. What the plate draws is that coordinate over the parameter plane — the quadric y = 0.0896·(x²−z²) at the factory sliders, matched to 5.8×10⁻⁸ at grid 90 and 5.9×10⁻⁸ at 161, i.e. to Float32 — where Enneper's surface is minimal and this one carries max \|H\| = 0.0214 with 90.1 % of its vertices past 10⁻³. |
| `scherkSurface` | Scherk surface | 🟡 C | y = (1/a)·log\|cos(a·x)/cos(a·z)\|, a = 2·freq — Scherk's first surface. Round 8: the prefactor was a flat 0.25, and a graph of this form is minimal only when it equals 1/a, so the surface shipped was a vertically compressed Scherk graph with mean curvature reaching 1.05 where minimality requires 0. Sympy now confirms 2H·(1+\|∇y\|²)^{3/2} ≡ 0 for the shipped prefactor **at amp = 1, and only there** — round 9: `amp` multiplies the whole graph, and a vertical stretch is not a conformal operation, so mean \|H\| over the central cell (\|x\|,\|z\| under 0.6, central differences at 1e-3) measures 1.0·10⁻⁶ at amp 1 against 0.135631 at the app's boot amp 0.70 (max 0.280333), 0.023218 at the audio envelope and 0.989844 at the slider tops; the caption now writes the equation with its amp-dependent prefactor and the name drops "Minimal". Controls through the same stencil: an exact Scherk graph with k = 1/a reads H = −1.0·10⁻⁸, and the same graph with k = 0.7 reads −0.012271, and the plateau share at the fold is 0.1 %. **Round 8 — the 10⁻³ guard band around the asymptotic walls is gone**, and only the simultaneous exact zero is branched, the way `sinc`, `fejerKernel`, `dirichletKernel` and `blaschke` branch. The band was 2·10⁻³ rad wide while the phase 0.2·t advances 0.0016 rad per rendered frame, so it swept a whole mesh row to exactly 0 on 5.6 % of consecutive frames at grid 90 and 9.4 % at 161 — measured 111 of 2000 and 47 of 500 with the band in place against 0 of both without it — each one dropping that row by up to 1.60 world units, the entire half height. The static half, cos(2·freq·x), left a permanently dead column with no clock involved for 2.45 % of the freq slider at grid 90 and 4.50 % at 161; both are 0 % now. Nothing outside the old band moved (0 differences over 399 508 random reachable points), and inside it the kernel agrees with an mpmath oracle at 40 dps to 4·10⁻¹⁵ where it used to return 0 against +1.5955. **Round 8 — Tier A withdrawn, and this is the entry whose figures depend most on the lattice.** The fold covers a measured **11.5 %** of the plate at the factory sliders and 3.4 % at the slider maxima, counted at grid 90 — and neither is a number the app draws: at grid 81 it is 9.39 % and 3.84 %, at 161 10.74 % and 5.66 %, at grid 41 0.00 % and 5.47 %, all at t = 0, and the clock moves them further, 9.18…12.10 % and 3.75…6.11 % over one turn at grid 81. The audio envelope sits at 13.7…15.1 %. The peak is a different object per mesh for the same reason — the tall points are the log poles at cos(z·freq·2) = 0, and whether a vertex lands near one is a property of the lattice: at t = 0, 0.92 at grid 90 against 1.49 at both 81 and 161 at the factory sliders, 62 % higher on the meshes the app lays down — but that gap is one frame and not the shape of the clock. Over one turn (10π, 3 927 rendered frames) grid 81 runs 1.4864…1.5995 with a median of 1.4866 and grid 161 1.4866…1.5995, against grid 90's 0.9163…1.5994 with a median of 1.1617: every mesh touches the fold ceiling 1.6 at some frame, and on a typical frame the app's own grids stand 28 % above grid 90 rather than 62 %. The 62 % is worth keeping as the boot instant, because boot is what a viewer meets first. Measured against the same kernel with `soften()` replaced by the identity, 9.39 % of the drawn plate at grid 81 and 6.84 % at 161 is further than 10⁻³ from the un-compressed value at the factory sliders, rising to 10.38 % and 7.62 % over the clock, and the deviation at t = 0 is 0.4285 world units at grid 81 and at 161 alike — the two app meshes agree at that instant, 81's vertices being a subset of 161's — while over one turn of the clock the worst runs to 2.7806 on both, reached at the same frame t = 0.896, against 0.0043…2.6802 at grid 90. That is four hundred times the 10⁻³ tier B allows at boot and two thousand eight hundred times it at the worst frame, and twelve orders past tier A's 10⁻¹² throughout — so the only bullet left is Tier C's envelope clause. The 0.44 and 1.59 this row used to quote are not a pair: they are the maxima of a clock scanned at a step of about one time unit, read at t = 6.00 on grid 81 and t = 3.00 on grid 161 — two different instants of one breathing plate, and both below what the frames the app actually renders contain. What argues against moving it: the row already measured a fold statistic and found it clean, and at grid 41 at t = 0 the entry folds nothing at all. What does **not** argue against moving it, though this row used to say it did, is mildness — measured at the factory sliders on grid 81 at t = 0 (grid 161 gives the same twelve numbers) this is the sixth-mildest of the twelve entries that fold, not the first: `manifoldCurvature` and `complexSin` are at exactly 0, their folds never engaging at these sliders; `dragon` is at 1.80×10⁻³, `lyapunov` at 5.38×10⁻² and `breatherSurface` at 6.36×10⁻². And none of those three moves — `dragon` and `lyapunov` take no clock at all, `breatherSurface` holds 6.36×10⁻² across every one of the 2 618 frames of its turn — while this entry's own deviation climbs from 0.4285 to 2.7806 over its. It does not win. The plateau share at the fold, 0.1 %, is not the quantity that decides whether the drawn height is the named function; and the grid-41 exoneration is a t = 0 artefact — let the clock turn and grid 41 reaches 11.24 % at t = 20. The ceiling 1.6 is never approached: 0.00 % of the plate lies within 10⁻³ of it at either setting on any of the three grids. |
| `catenoid` | Catenoid | 🟡 C | a·cosh(z/a) − \|x\|·freq exact — round 9: the height is that DIFFERENCE and not the catenary radius, and the −\|x\| term appears nowhere in the row or in the caption; its zero level set is the catenary, and it is what puts 23.3 % of the grid-81 plate below zero at the factory sliders under a caption naming a strictly positive r. Drawn ÷ (a·cosh(freq·z/a) − freq·\|x\|) is a flat amp·0.3 on every unfolded vertex at every setting measured — 0.210000 at the factory sliders, 0.300000 at amp 1, 0.315000 at the audio envelope, 0.675000 at the slider tops, 0.060000 at the amplitude floor. Also: the plate radius here is \|x\| and not √(x²+z²), so this is a half-plane profile mirrored rather than a surface of revolution (90-degree rotation residual 2.530000), and mean \|H\| over a cell right of the \|x\| kink is 0.248195 at the factory sliders against 2.1·10⁻⁸ for `helicoid` through the identical stencil, so the name's old "(Minimal)" was unearned and is gone. Round 5 clamped it to ±1.5 because the far field reaches 82 units at the default slider; round 6 replaced that with `soften(1.2, 1.9)` after measuring what the clamp cost — 49.6 % of the mesh flat at the default slider, 95.8 % at the maximum. The fold brings that to 27.1 %, and the remainder is intrinsic: cosh(2·z·freq) is so steep that over half the plate is far field at any display bound. The neck and the zero level set are inside the identity region and are bit-identical to before. **Round 8 — Tier A withdrawn, and the 27.1 % above is not the fold coverage.** 27.1 % is the plateau share; the coverage is a different quantity, and it is the one that answers the tier question. The fold covers a measured **52.1 %** of the plate at the factory sliders and 97.5 % at the slider maxima, counted at grid 90; on the meshes the app draws it is 52.16 % and 97.33 % at grid 81 and 51.90 % and 97.43 % at 161, with the audio envelope at 66.0 % and no clock at all — the plate is bit-identical across a full sweep of it. Measured against the same kernel with `soften()` replaced by the identity, 51.33 % of the drawn plate at grid 81 and 50.91 % at 161 is further than 10⁻³ from the un-compressed value at the factory sliders (64.93 % at the audio envelope, 97.12 % at the maxima), and the worst such deviation is 55.7 world units at boot and 1.1·10¹³ at the maxima. Half the plate is not a·cosh(z/a) at boot, on every mesh the app lays down. The argument this row already makes is the strongest in the catalogue against moving the letter — the remainder is intrinsic, cosh(2·z·freq) is so steep that over half the plate is far field at any display bound, so no repair exists that would restore A, and the neck and the zero level set, which is where a catenoid is legible, are bit-identical to the unfolded surface. It does not win: that is a statement that the entry can do no better, not that the plate is the named surface, and this document has an established way to say precisely that — Tier C, "honest artistic interpretations", with the exactness of the neck stated in the row. The tabletop the fold replaced is also the largest quiet one in the catalogue at the factory sliders: 29.72 % of the plate at grid 81 and 29.76 % at 161 sits bit-exactly on `fround(1.9)` and 38.5 % within 10⁻³ of it, rising to 89.2 % and 93.3 % at the maxima. The round-6 note above records the old clamp pinning 49.6 % and 95.8 %, so the fold moved that number rather than removing it (control: the same counter against the un-folded kernel reads 0.00 % at both settings). |
| `helicoid` | Helicoid | 🟢 A | c·θ exact height. The animation rotates the azimuth and folds it back into (−π, π], so the surface spins about its axis; it used to add an unwrapped t·0.3 to θ, which translated the whole mesh out of the framed volume over the length of a set. |
| `hyperbolicParaboloid` | x²/a-z²/b | 🟢 A | Exact saddle. |
| `torusSection` | Torus implicit | 🟢 A | (√(x²+z²)−R)²+y²=r², solved for y; the residual is 5.6e-17 across the tube at amp = 1. Round 9: `amp` multiplies the height, so the equation as written above held at that one slider value and nowhere else — the residual of (√(x²+z²)·freq − R)² + y² − r² is 0.353561 at the app's boot amp 0.70 (0.420310 at grid 161), 0.581091 at the audio envelope and 2.391734 at the reachable 2.25/4.55/0.90 (2.407614 at grid 161), carried at those settings by the sign-zero pinholes rather than by amp — with the four (grid 81) and eight (grid 161) hole vertices excluded it is the algebraic (1 − amp²)r² to five digits. The caption now reads (√(x²+z²)−R)² + (y/amp)² = r², and that residual is 5.6·10⁻¹⁶ at the factory sliders and 6.7·10⁻¹⁶ at the slider tops on both meshes the app draws, so the letter is no longer a claim about one amp (control: dividing by 1.1·amp instead of amp reads 0.073326). Separately, `Math.sign(x+z)` returned exactly 0 on the anti-diagonal, drawing 4 of 1600 in-tube grid-81 vertices and 8 of 6388 grid-161 vertices at height 0 — pinholes through the tube, not a branch — and is now `Math.sign(x+z) || 1`, which moves those vertices and no others and leaves the plate's peak bit-identical at every setting. Round 8: an extra factor of 0.5 halved the vertical semi-axis, so the section drawn was an ellipse of aspect 2:1 and the equation above held only on the tube's boundary curve. |
| `breatherSurface` | Breather pseudosphere | 🟡 C | **Known open item.** Round 5 found the a² factor missing from the denominator a[(1−a²)cosh²(aT) + a²sin²(√(1−a²)P)], and the scalar built on it is not a coordinate of the breather surface either. Round 6 could not verify a replacement: the test a pseudospherical surface must pass is Gaussian curvature identically −1, and both parametrisations tried came back with K running +0.21…−1.23 (the curvature routine itself was checked on a sphere to 8 digits and on a tractricoid to −1.0000000). Downgraded to C, which is what an unverified decorative scalar deserves, rather than left at B on a claim nobody has checked. What round 6 did fix is the ±0.6 clamp, which was pinning 66.3 % of the mesh flat at the default sliders and 100 % at the top of the range; it is now `soften(0.45, 0.95)`. **Round 8 — the fold, stated with its lattice and its clock.** The fold covers a measured **82.4 %** of the plate at the factory sliders and 100.0 % at the slider maxima, counted at grid 90; on the meshes the app draws it is 82.66 % and 100.00 % at grid 81 and 82.78 % and 100.00 % at 161, and t = 0 is the minimum of the cycle — over one turn the factory figure runs 82.66…84.26 % at grid 81 and 82.74…83.92 % at 161, with the audio envelope at 97.8 %. At the top of the sliders the entire plate is above the knee on every grid and at every t, so nothing of the surface is drawn unrescaled. Measured against the same kernel with `soften()` replaced by the identity, 73.60 % of the drawn plate at grid 81 and 73.34 % at 161 is further than 10⁻³ from the un-compressed value at the factory sliders, rising to 75.28 % over the clock and 100.00 % at the top. The letter does not move on that: this entry is already C for an unverified curvature claim, and the fold is a second independent reason rather than a first — recorded so that a later round which does verify the curvature does not read this C as free to lift. One caveat on the tabletop question, which splits here more sharply than anywhere else in the catalogue: the peak is 0.949877 against a ceiling of 0.95, so 0.00 % of the plate is bit-exactly pinned and 0.00 % is within 10⁻⁶ of the ceiling — yet 90.25 % of the plate at grid 81 and 90.20 % at 161 is within 10⁻³ of it with the sliders at their tops. By the strict measure it is a clean fold; by any visual measure it is a flat disc. Both figures, or neither. |
| `pseudosphere` | Tractrix profile | 🟡 C | log(tan(T/2)) + cos(T), the tractrix profile, revolved. Round 8 corrected two claims. The profile was log(tan(T/2)) + **sech**(T): sech and cos agree to first order at T = 0 and diverge at once, by 0.571 over the drawn range, so the curve this row called "the tractrix" was not one. And the radial coordinate is the plate radius under a monotone map, not arcsin of it, so what is drawn is that profile revolved and **not** a surface of constant negative curvature — the tier drops to C for the same reason `breatherSurface` did rather than assert a curvature nobody verified. Round 6: the parameter is defined only on (0, π), and clamping the plate radius into that interval gave every vertex past radius π/freq one shared value — a flat ring over 38.4 % of the mesh. A monotone map of [0, ∞) onto (0, π) shows the whole trumpet instead of cutting it off. Both ends of the profile run to infinity, so the fold rather than a clamp keeps it in frame. **Round 8 — the fold, and a grid parity a coverage figure hides.** The fold covers a measured **0.1 %** of the plate at the factory sliders and 53.7 % at the slider maxima, counted at grid 90; on the meshes the app draws it is 0.14 % and 53.73 % at grid 81 and 0.14 % and 53.24 % at 161, with the audio envelope at 0.3 % and no clock — the plate is bit-identical across a full sweep. The 0.2 % recorded for the factory sliders in round 8's first pass was stale by one decimal: the measurement is 0.1481 %, twelve vertices of 8100. The real hazard here is parity rather than density. log(tan(T/2)) is −∞ at ρ = 0, so wherever the mesh carries the centre vertex the plate returns the ceiling 0.8 exactly. **Round 9 tail — the grid list was wrong and the 30 % understates the loss.** 41 and 91 are grids the app cannot produce; the odd ones it could then are 3, 5, 9, 13, 15, 43, 45, 63, 65, 81, 83 and 161 — round 10 added **21** and retired 20, 42 and 82, making thirteen — and 81 and 161 are one lattice and not two, since 7/80 is exactly 2·(7/160) and every grid-81 vertex is a grid-161 vertex. Peak reads 0.800000 on all thirteen at all five reference settings — grid 21 included, re-measured in round 10 — the sole exception being grid 83 at amp 0.20, which reads 0.799906 — because grid 83 is odd and still has NO vertex at x = 0: `−3.5 + 41·(7/82)` is 4.44·10⁻¹⁶, and thirteen of the hundred odd grids in 3…201 miss zero the same way. The even side is not 0.5639 either; that is grid 90, which the app never draws. What it draws on even grids is 0.06117 at 8, 0.09565 at 10, 0.20898 at 18, 0.27173 at 24, 0.44938 at 52, 0.52561 at 74, 0.58305 at 100, 0.60541 at 114 and 0.68129 at 198, so the loss against the odd meshes runs from 92 % down to 15 % rather than sitting at one 30 %. **Round 10 moved the boot grids out of this list.** They were 42 (0.40032) and 82 (0.54609), a 50 % and a 32 % loss; they are now 43 and 83, both odd, both reading the ceiling — so the parity hazard this paragraph describes no longer applies to what either platform draws at boot. It still applies to the nine even grids above, but to fewer shapes than an earlier revision of this sentence claimed: exactly six of the twenty reach an even grid, and they are `icosahedron` (8), `dodecahedron` (10), `hex` (18 mobile, 24 desktop), `torusknot` (52, 74), `box` (100, 198) and `torus` (114 on desktop only — on mobile it draws at 81, odd). `ring` was named here and does not belong: it draws at 45 on mobile and 63 on desktop, both odd, and reaches no even grid on either platform. Measured against the same kernel with `soften()` replaced by the identity, 0.08 % of the plate is further than 10⁻³ from the un-compressed value at the factory sliders and 36.23 % at the slider tops, so the fold does not decide this letter — it stays C for what this row already says, that the profile revolved is not a surface of constant negative curvature and nobody has verified one (control: halving the knee lifts the factory figure to 0.32 %, so the comparator is not blind). One vertex of that 0.08 % is an artefact of the comparison and not of the fold: at ρ = 0 the un-compressed kernel is −∞ and stores 0 while the shipped kernel stores the ceiling, so it counts as misdrawn by 0.8 with no fold involved. |
| `crossCap` | Cross-Cap Coordinate (x·z saddle) | 🟡 C | Just x·z product — not actual cross-cap parametrization. **Round 8 — the display gain is 0.27, not 0.30.** At 0.30 the corner of the plate reached 3.087 world units at the top of the (1 + 0.2·sin 0.3t) breathing: the only entry in the catalogue standing outside the ~3-unit frame at the factory sliders, and it did it away from t = 0, where the frame guard could not see it. The height is largest at the plate corner (\|u·v\| = 12.25 at freq 1), which every mesh samples, so the figure does not depend on the lattice — measured peak over one full breathing period **at the factory sliders**, 3.0870 at gain 0.30 against **2.7783** at 0.27, identical to four places at grids 25, 81, 90, 161 and 201 — and the factory sliders are the only corner where that is a frame-sized number. The peak is 12.25·freq²·amp·gain·(1 + 0.2·sin 0.3t), so it scales as amp·freq²: the same measurement is **7.043** at the audio envelope the app reaches with nobody touching a slider, **184.88** at the slider maxima, and 16.43 even at the amplitude slider's own floor of 0.2 with freq at 4.55. The audio figure is not this row's alone — the suite's own envelope record carries 7.8255 for the pre-repair gain, which is 7.043 × 30/27. What the gain change buys is the ~3-unit frame at boot, which is what the frame guard asks for; it does not put this entry inside the frame. The change is a pure scale: every level set, the saddle and the breathing are the object that was there before, 10 % shorter, and 2.778 sits in the band of the tallest entries in the catalogue (`hydrogenS` 2.856, `determinant` 2.755). **Round 11 — renamed.** The label a viewer picks from is the <option> text, which mirrors `name`; the caption that carried the disclaimer is never rendered, because `bindMathCollectionUI()` is called by nothing. The claim needed no oracle to refute — the kernel returns the graph of a smooth function, a graph over the plane is embedded and orientable, and RP² admits no embedding in R³ at any parametrisation. Measured for the record: the plate is y = 0.189·x·z to 1.1×10⁻⁷ at grid 90 and 1.2×10⁻⁷ at 161, and its first fundamental form has EG−F² ≥ 1 everywhere with the minimum exactly 1 at the centre, where the Whitney model r(u,v) = (u, uv, v²) has EG−F² = 0 at both pinch points. The name now states the coordinate it draws: uv is that model's middle coordinate. |
| `alexanderHorned` | Radial fold iteration | 🟡 C | 2–5 iteration schematic — not actual wild embedding. Round 9: the atan2, the fork, the angle-doubling and the clock term are all computed and then discarded, because each iteration takes only the modulus of the rotated pair, so the drawn field is sin(5·r_d) with r_{n+1} = \|r_n − 0.5\| from r₀ = freq·√(x²+z²) — a radial ripple. Measured: the 90-degree rotation residual is 1.9·10⁻¹⁵ in float64 at the factory sliders and 0.000e+0 in the Float32 the vertex buffer stores, and max\|y(t) − y(0)\| over t ∈ {0.5, 3, 7, 40, 120} is 1.5·10⁻¹⁵ in float64 and 0.000e+0 in Float32 — exactly rotationally symmetric and completely clock-blind, where an Alexander horned sphere is the least rotationally symmetric object in topology. Controls in the same process, against the round-9 kernels: `kleinBottle` reads 0.680576 and 0.636427, `crossCap` 4.630500 and 0.459243. The caption and the name now say what is drawn; the entry was listed as "Alexander Horned (approximation)" until round 9, and this row is the only place that name survives. |
| `hopfFibration` | Phase-Coupled Ring Wave | 🟡 C | **Round 9 — B → C.** Tier B asserts a bounded approximation *to something*, and there is nothing here to approximate: the kernel is `sin(2θ + 4r − 0.5t)·amp·0.4·e^{−4(r−1)²}` and nothing else, with no S³, no S², no fibre and no fibration anywhere in it. Measured: drawn ÷ that expression is exactly amp·0.4 — 0.280000000 at the factory sliders over 3 960 grid-81 vertices, 0.080000000 at the amplitude floor, 0.420000000 at the audio envelope, 0.900000000 at the slider maxima, flat over 291 836 vertex-instants across grids 81 and 161 and t ∈ {0, 3.7, 11, 29}. Control that the comparator can fail: dropping the ring envelope from the named quantity spreads the same ratio to [2.2×10⁻²⁸, 0.279994]. The Gaussian confines the field so tightly that only 21.4 % of grid-81 vertices carry \|y\| above 1 % of amp at the factory sliders, 12.7 % at the audio envelope and 0.9 % at the maxima. This is verbatim the argument that took `torusKnot` from B to C in round 8. The previous words here — "phase visualization, structurally faithful" — *were* the B claim, and the caption the app now renders says "not the fibration"; the two could not both stand. **Round 11 — renamed.** This row has said since round 9 that there is no fibration in the kernel, and the label still promised one. Fitting the drawn plate to any coordinate of the Hopf map on any natural slice leaves 96.3–100 % relative residual, and the property that defines a fibration — linking number 1 between any two fibres — is not obtainable from this surface at all. Control that the linking probe can fail: −1.000002 on a genuine Hopf link, 0.000000 on two separated circles. |

### 11. Cellular Automata (16) — 13 A · 1 B · 2 C · 0 D

The cleanest collection: integer-valued automata with discrete rules — these are **exact by construction** on the simulation grid. Eleven of the sixteen go through `createCachedHeavySampler` and are bilinearly interpolated onto the adaptive display mesh (up to 160×160 segments, scaled to GPU capability); five are not, and are evaluated directly at every vertex — `rule30`, `rule90`, `rule110`, `rule184` call `cellularRule` per vertex, and `voronoiCA` takes an argmin per vertex.

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `rule30` | Rule 30 (Wolfram) | 🟢 A | Exact 8-bit lookup on 64-cell grid. |
| `rule90` | Rule 90 (XOR) | 🟢 A | Exact. |
| `rule110` | Rule 110 (Turing complete) | 🟢 A | Exact. |
| `rule184` | Rule 184 (Traffic) | 🟢 A | Exact. |
| `gameOfLifeDensity` | Game of Life B3/S23 | 🟢 A | Exact CA on 48×48 internal grid. Round 6 replaced the seed: `(i·2654435761) >>> 0` is a Weyl sequence, equidistributed rather than independent (χ² = 7.9 over 100 buckets where chance gives 99 ± 14), so almost every live cell was isolated and generation 1 killed 97 % of the population — the plate measured a peak of exactly 0 at t = 1.5. The rule was never wrong; it was being fed a lattice instead of a soup. On a murmur3-finalised hash the same rule settles around 5 % alive, as Life does. |
| `briansBrain` | Brian's Brain | 🟢 A | Exact 3-state CA on 48×48 internal grid. |
| `langtonAnt` | Langton's Ant | 🟢 A | Exact deterministic ant on 64×64 internal grid. |
| `cyclicCA` | Cyclic CA | 🟢 A | Exact on 48×48 internal grid. Round 6 replaced the seed for the same reason, and here it was degenerate rather than merely poor: `(i·2246822519) >>> 0 % N` depends only on the column whenever N divides a power of two, so at N = 4 and N = 8 the variance down every column was exactly 0 — vertical stripes instead of spirals. |
| `wiredFire` | Wireworld | 🟢 A | Exact 4-state CA on 50×50 internal grid. |
| `sandpile` | Abelian Sandpile | 🟢 A | Exact toppling rule on 40×40 internal grid. |
| `voronoiCA` | Voronoi tessellation | 🟢 A | Exact nearest-seed, and exact is the right word — the kernel matched an independent implementation to 0 over 33 640 points and to 0 violations of the perpendicular-bisector characterisation. It is not an automaton and nothing grows: there is no lattice, no neighbourhood and no transition rule, only argmin over 5+round(8·comp) moving seeds, recomputed from scratch at every vertex of every frame. The plate shows exactly N flat levels at any t. |
| `excitableMedia` | FitzHugh-Nagumo | 🟡 C | Real FHN PDE on 64×64 internal grid, explicit Euler at dt = 0.1, bilinearly interpolated to the display mesh. Round 6 corrected the error claim rather than the code: the time-stepping error alone is 3.9e-3 in u, whose display range is [0, 1] — about four times the ~10⁻³ this row used to assert, before any spatial error on a front two cells wide. The number stated is now the measured one. Round 8: explicit Euler at dt = 0.1 contributes 3.9e-3 against solve_ivp on a field displayed over [0, 1] — above the 10⁻³ ceiling tier B sets. The number was already stated correctly in this row; only the letter was wrong. |
| `reactionDiffusion` | Gray-Scott | 🟡 C | Real Gray-Scott PDE on 64×64 internal grid, configurable F/k regimes, bilinearly interpolated to the display mesh. Round 6 corrected the error claim rather than the code: explicit Euler at dt = 1.0 contributes 5.5e-2 to 8.5e-2 in the displayed clamp(4v, 0, 1), whose range is exactly [0, 1], and 1.7e-2 in v itself — seventeen to eighty-five times the ~10⁻³ this row used to assert. Reducing dt would cost a proportional number of iterations on a device that already carries this entry as the heaviest in the catalogue, so the number is stated instead of the tier being pretended. Round 8: explicit Euler at dt = 1.0 contributes 2.5e-2 to 8.5e-2 against solve_ivp on a field displayed over [0, 1] — one to two orders past tier B's ceiling. Reducing dt would cost a proportional number of iterations on the heaviest entry in the catalogue, so the letter moves rather than the code. |
| `forestFire` | Forest Fire CA | 🟢 A | Exact tree/fire/ash CA on 50×50 internal grid. |
| `conway3D` | Conway 3D | 🔵 B | Real 3D B5-7/S6 simulation on 18³ grid, 3–5 generations, bilinearly interpolated to the display mesh. Round 6 corrected the description: what is extracted is not a mid-y slice but the MEAN of three neighbouring slices, which is a projection and smooths the rule's own structure. Replaced a 1D Wolfram rule. Round 9: the initial condition is re-randomised twice per unit of clock — seed = floor(2t) — so what a viewer watches is a slideshow of generation 4 of unrelated soups rather than one evolution. Measured inside one process at grid 81, factory: t 0.30 → 0.49 changes 0.0000 % of vertices and t 0.49 → 0.51 changes 71.5897 %; t 0.51 → 0.99 changes 0.0000 % and t 0.99 → 1.01 changes 70.0198 %. At `main.js`'s 0.008 per frame and 60 fps that is a complete reseed every 1.0417 s — 62.5 frames — and nothing at all happens in between. The caption now says so, and the picker name says "3-layer mean" instead of "(slice)" — this row has described the mean since round 6 while the name did not. **Round 9 tail — measured against what would replace it, and kept.** `t` reaches this simulator only through `Math.floor(t*2)`, so the plate is a step function of the clock. At the app's own rebuild cadence — `createCachedHeavySampler` rebuilds when \|Δt\| > 0.016 and the clock advances 0.008 a frame, so every third frame, Δt = 0.024 — 40 of 40 ordinary rebuilds inside one bucket change 0.0000 % of the plate, at grids 81 and 161 and at all five settings, while the one rebuild in ~21 that crosses a bucket boundary changes 69.97–74.40 %, its largest single step spanning the entry's whole height range (0.42 at the factory amp, 0.60 at amp 1, 0.63 at the audio envelope, 1.35 at the reachable over-drive, 0.12 at the floor). Control: `julia` over the same Δt = 0.024 moves 0.0914 % of its plate, so the counter is not blind to small motion. What settles it is the comparison with the rule's own step, on this entry's own 18³ torus: one B5-7/S6 generation changes 52–80 % of the drawn slice and never stops doing it — five soups run 300 generations with no state repeat and population never leaving 22–33 % of 5832, so one continuing evolution would **not** go still. It would also not look like an evolution. Over 40 soups the drawn 18² three-layer-mean field correlates −0.152 between generations 4 and 5 (−0.203 between 5 and 6) with 38.3 % (32.2 %) of cells identical, while the same field against a **different** soup at the same generation correlates −0.017 (+0.006) with 37.6 % (36.5 %) identical, and a shuffled copy of the field itself gives −0.001 (+0.009) with 40.0 % (36.8 %) — so the reseed is no more of a discontinuity than the rule's own step (control: identical fields correlate 1.0000 at 100 % identical, one flipped cell 0.9975). The autocorrelation of one evolution against its own generation 4 is −0.211 at +1 and then 0.055, 0.046, 0.111, 0.055 at +2, +3, +5, +10 — inside the shuffle baseline from the second generation on, i.e. generation g+2 of one soup is already as unrelated to generation g as a fresh soup is. Driving the generation from `t` instead would multiply the churn about twentyfold (every rebuild changing 52–80 % instead of one in twenty-one changing 70 %) and buy no continuity a viewer could see, and it carries two independent obstacles besides. The CA has no inverse, so every rebuild would replay from the soup: 0.34 ms a generation on this VM, unbounded in `t`, which at the reseed's own floor(2t) rate is 576 generations ≈ 196 ms a rebuild after ten minutes of playback and at the rebuild cadence 12 000 generations, against a 16.7 ms frame budget. And `t` is not monotone — `math-visualizer.js` passes `t = time + beatInt·0.3` while `beatInt` decays 0.04 a frame against the clock's 0.008, so `t` runs backwards 0.004 a frame for ~25 frames after every beat, and a generation counter would step the evolution backwards. **Kernel unchanged: this stays a documented choice, and the caption states the reseed and its period, which is what the display contract asks of it.** |
| `turmite` | Turmite | 🟢 A | 2-state 2-colour turmite on a 56×56 internal grid. Round 6 replaced the transition table: both state-0 rows of the old one wrote state 0, so the state-1 rows were unreachable and the machine degenerated into a one-state ant of period 8 — 4 raised cells out of 3136. The new table is the survivor of an exhaustive run over all 65 536 rules of the family, scored on both states being used, the pattern still growing at the end of the run, and fill; it reaches 7.7 % of the plate at 700 steps and 39.3 % at 6000, where the runner-up saturates at 7.0 %. The step count rose to 1500 + comp·3000 to make the complexity slider visible (19 % → 35 %). |

### 12. Quantum Mechanics (16) — 14 A · 1 B · 1 C · 0 D

| Key | Name | Tier | Rationale |
|-----|------|:----:|-----------|
| `particleBox1D` | Particle in box | 🟢 A | (2/L)·sin²(nπξ/L) exact, ξ = clamp((freq·x+3.5)/7). Round 9: the drawn height also carries ×(0.8+0.2cos(n²t·0.015)), which the caption now names — a stationary state must not breathe, the ruling recorded at `src/math-collections.js:554-566` when cos²(0.3t) was cut from `hydrogenS` — and this one breathes ×1.666665 over t = 0…420 at the factory sliders, against the algebraic ceiling 1.0/0.6 = 1.666667. The density itself is untouched: dividing the drawn field by the caption's own quantity gives 0.5·amp flat to 1.1×10⁻¹⁵ over grid 81 at all six settings. **Round 11 — the pulse is gone.** Round 9 recorded that a stationary state must not breathe and left the kernel breathing; ×(0.8+0.2cos(0.015n²t)) is now removed. Measured before the cut: peak 1.000000 at t = 0 against 0.600000 at t = 13.09 on grid 161 and 0.999225 against 0.599535 on grid 90 — ×1.666667, the algebraic ceiling, with a period of 26.18 units, 54.5 s at 60 fps. The factor was exactly 1.0 at t = 0, so the boot plate is unchanged and the entry is now clock-blind, as an eigenstate density has to be. |
| `harmonicOscillator` | QM harmonic \|ψ_n\|² | 🟢 A | Hermite recurrence + Gaussian envelope, exact. **Round 9.** What is drawn is the density [H_n(ξ)e^{−ξ²/2}]²/(2ⁿn!√π) at **ξ = 2·freq·x** — the argument is compressed by two, and freq cannot absorb the two, because the neighbouring e^{−0.3z²} in the same expression is drawn at the raw plate z. The first round-9 caption wrote H_n(x) and was false of the plate: drawn ÷ that reading spreads over [2.2×10⁻¹⁴, 5.8×10⁶] at the factory sliders, while drawn ÷ the ξ reading is a flat 3.6·amp at every setting and both grids. Measured decisively at the zeros of H₃: the plate reads 0.8156 where H₃(x) vanishes at x = 1.2247 and 3.0×10⁻³² where H₃(2x) vanishes at x = 0.6124. Carried by ×(0.8+0.2cos(0.02nt)), which the caption now names, and n = round(6·comp) renders n = 3…5 over the reachable comp band. **Round 11 — the pulse is gone,** for the same reason and by the same measurement: ×(0.8+0.2cos(0.02nt)) took the peak from 1.242542 at t = 0 to 0.745525 at t = 52.36 on grid 161 (1.226839 → 0.736103 on grid 90), ×1.666667 over a period of 104.72 units, 218 s at 60 fps. Its phase advanced as n rather than n+½, so it did not even match E_n = ħω(n+½) as a decoration. The boot plate is unchanged. |
| `hydrogenS` | H 1s orbital | 🟢 A | 4·exp(−2r) radial part exact (Bohr units), measured **2.9×10⁻¹⁵** against `sympy.physics.hydrogen.R_nl(1,0)`. Round 8 removed the +0.01 added to r in the shared `hydrogenPsi` helper — the largest guard epsilon in the file, and under this row the only error in the entry. Nothing in the helper divides by r and θ comes from `atan2`, which is defined at the origin, so it guarded nothing; it drew the field of radius r+0.01 at radius r, which for 1s is the flat factor e^{−0.02} = 0.9802 (i.e. 3.9208·e^{−2r} under a row that says 4·e^{−2r}), worth 0.214 on a peak of 10.59. Its display constant went 2 → 1.7 in the same edit, because the entry was standing on the frame limit and the offset was part of what held it there: |ψ|² peaks at the origin and only odd grids have a vertex there, so at the factory sliders the plate read 2.947 at grid 90 against 3.293 at 25 and 161, i.e. already out of frame at the resolutions the suite's guard does not sample. At 1.7 the true factory peak is 4·0.6·0.7·1.7 = 2.856 **wherever a vertex lands on the origin, which is every odd grid** — 25, 41, 81, 161 all read 2.8560 — and the plane the app lays down is 81 (mobile) or 161 (desktop), both odd, so that is the peak a viewer gets. An even grid samples off the peak and reads less: 2.5554 at 90, 2.6836 at 160, 2.5196 at 80, 2.2157 at 40. **Round 8 also measured the headroom, which is the part this row was silent about.** 2.856 against the ~3.0 frame is 4.8 % of headroom, and the frame is crossed at amp = 3.0/(4·0.6·1.7) = 0.7353 — measured on grid 161: 3.0000 at amp 0.7353, 3.0600 at 0.75, 3.2640 at 0.80. The lift that crosses the frame is 5.04 % **on amp**, and the audio path is amp·(1 + bass·0.5), so it is delivered by bass at 0.10 of full scale — at bass 0.05 the peak is 2.927 and stays in frame. The entry sits on the limit rather than under it, and only the shortness of the drop keeps that from being visible. |
| `hydrogen2p` | H 2p orbital | 🔵 B | Radial part exact — measured **2.4×10⁻¹⁷ … 2.4×10⁻¹⁶** against `sympy.physics.hydrogen.R_nl(2,1)`, the low end at the factory sliders (1.9×10⁻¹⁷) and the high end over the reachable box (2.4×10⁻¹⁶ at amp 2.11 / freq 1.44 / t 27.3); the 8.8×10⁻¹⁷ this row used to quote is a factory-slider figure and the box exceeds it by 2.7× — angular part still approximated as cos(l·θ) instead of the full Y₁₀ spherical harmonic, which is what holds this at B. Round 8 removed the same +0.01 in `hydrogenPsi`; here it was not a constant factor but a 0.01 a₀ inward shift that filled in the node at the origin, worth 1.04×10⁻³, over the top of tier B. |
| `tunneling` | Quantum tunneling | 🟡 C | Schematic piecewise wavefunction: a pure decaying exponential inside the barrier and T = e^{−2κL} for the transmission. Round 8 measured it against the exact rectangular-barrier solution (four matching conditions solved symbolically in sympy and again numerically in mpmath; control: \|t\|²+\|r\|² = 1 exactly, and the closed form reproduced to 2.2e-19). The exact in-barrier envelope carries a growing e^{+κx} piece this does not, and e^{−2κL} omits the 16E(V₀−E)/V₀² prefactor: the exact T is 0.39× to 3.97× the drawn one across E/V₀, and no choice of E brings the envelope closer than 8.5e-3. The code's own comment says "schematic"; the tier now agrees with it. |
| `wavePacket` | Gaussian wave packet | 🟢 A | exp(−(x−vt)²/4σ²)·cos(kx−ωt), squared, and the arithmetic matches to 8.6e-16. Round 8: v, k and ω are set independently, so no ħ and m make them a solution of the free Schrödinger equation simultaneously — what is drawn is the stated expression, not a propagating solution of that equation, and the row no longer implies otherwise. The clock is folded to a 16-unit period (round 4), so the packet is still at 0.44–0.49 of its boot peak after four hours. |
| `spinorVisualization` | Bloch sphere | 🟢 A | ⟨σx⟩ = sin θ·cos φ, formed from the state in the caption; exact to 1.1e-16 against explicit Pauli algebra. Round 8: this row used to describe the population difference cos²(θ/2) − sin²(θ/2) = cos θ, which round 6 had already replaced — and cos θ is φ-independent, so the two differ by 70.7 % of full scale. |
| `doubleSlitProbability` | Two-slit \|ψ\|² | 🟢 A | Round 6: 2I₀(1 + cos δ) with δ = k(r₁ − r₂), which is the formula the caption states. The 1/r amplitudes it carried are a near-field detail the caption never claimed, and both sources sit inside the plate, so near them r fell to the 1e-3 regulariser and the intensity went as 1/r²: peak 58.6 world units against a ~3-unit frame, and because the spike lives between vertices its height depended on the mesh — 1.9 / 324 / 84 / 3706 across grids 25 / 49 / 90 / 161, a spread of ×1926. Now bounded in [0, 2], identical at every density, fringe visibility 1. |
| `densityMatrix` | ρ diagonal | 🟢 A | Σ pₖ·\|ψₖ\|² over box states ψₖ = sin(kπ(x·freq + ½)), k = 1…round(1 + 4·comp), evaluated exactly. Round 8 struck the word *thermal*, which was the one thing here that was not measured. The weights are pₖ = e^{−k/2}: the ratio pₖ₊₁/pₖ is a constant 0.606531 at every k, so they are geometric — Boltzmann for a ladder whose energies are linear in k. The states drawn are particle-in-a-box states, and sympy solving the well gives Eₙ = π²ħ²n²/(2mL²) with E₂/E₁ = 4, so a thermal mixture of *these* states needs e^{−βk²}. Fitting β to the drawn p₂/p₁ gives β = 1/6, and the drawn weights then overshoot the box-thermal ones by 39.6 % at k = 3, 171.8 % at k = 4, 638.9 % at k = 5. The kernel was measured before being left alone: `exp(-k*k/6)` is a one-line swap but not a free one — it pins 5.5 % (amp 2.25, freq 1, comp 1) to 5.8 % (amp 2.25, freq 4.55, comp 1) of the plate at this entry's 0.7 ceiling, which the present weights never touch (0.00 % everywhere in the slider box, peak 0.615), and it drops the top step of the comp slider from 0.0172 to 0.0033 world units, i.e. the fifth state stops being visible. The tier is about what is drawn, and what is drawn is an exactly-evaluated mixture; A stands with the honest word. One further measured fact: the well is tiled, not shown once — the surface repeats with period 1/freq in x to 1.7×10⁻¹⁶, so the plate carries 7.00 wells at freq 1 and 31.85 at freq 4.55. |
| `landauLevels` | Landau levels | 🟢 A | Generalized Laguerre L_n^0 recurrence, exact to 4.7e-16 against mpmath and scipy for every reachable n; replaced a hardcoded n=1 approximation. Round 8: the caption's claim that this is the m = ±n pair was withdrawn — [L_n^0(r²)]²e^{−r²} is the m = 0 radial density, and the cos²(nθ) factor over it is a turning pattern, not an eigenstate (the m = ±n pair vanishes at the origin; this surface is brightest there). **Round 9 — the peak was decided by float noise, and the row's own "brightest there" is false on the desktop boot grid.** θ = atan2(z, x) is undefined at r = 0 and the kernel evaluated cos(n·θ) there anyway. The app's lattice puts the centre column at exactly 0 on grids 81 and 161, but 13 of the 100 odd grids in 3…201 miss it by 4.44·10⁻¹⁶, where atan2 returns π/4 rather than 0: measured peak at the reachable over-drive was 0.90000 on grid 81 against 0.45000 on grid 83, both odd and both selectable. θ is now pinned to 0 inside the singular vertex (r² < 10⁻¹²); grids 81, 83 and 161 now agree at 0.28000 (factory) and 0.90000 (over-drive), and the boot grids do not move by any amount. The even grids still differ — 0.17434 at grid 82, 0.24918 at 162 — but that is parity rather than noise: they carry no vertex at the origin at all. **Round 10:** grid 82 is no longer in the catalogue. The desktop boot grid is now 83, which reads 0.280000 at the factory sliders, the same as 81 and 161, so the clause this row used to carry — that "brightest there" is false of what a desktop draws — no longer holds; a desktop now draws the peak. The physics half of FIX(r8) — that the \|m\| = n state must vanish at the origin while this surface peaks there — is unchanged and still open. |
| `schrodingerSoliton` | NLS soliton | 🟢 A | \|ψ\|² = A²·sech²(A(x−vt)), the soliton density; the NLS residual computed symbolically is zero. Round 8: the kernel, this row and the in-app caption named three different quantities — A²sech², A·sech² and A·sech — of which only the first is what is drawn. |
| `coherentState` | Wigner of coherent | 🟢 A | 2·exp(-2\|α-β\|²) exact. |
| `atomicOrbitals` | sp² hybrid (xz-plane) | 🟢 A | The **density** \|ψ_sp²\|² of the hybrid ψ_sp² = (1/√3)(2s + √2(p_x cos φ + p_z sin φ)), with 2s and 2p radial parts, machine-exact against a symbolic reference. Renamed from a misleading "sp³". **Round 8 — the label named the amplitude and the kernel draws its square.** The caption read `sp² = (1/√3)(s + √2(p_x cosφ + p_z sinφ))` and this row opened the same way, while the last line of the kernel is `return psi*psi * amp * 4` — so what is on screen is a probability density, everywhere ≥ 0 (measured range 0…0.0371 at the factory sliders), and the caption named a signed amplitude that changes sign across the 2s node. Both now say \|ψ\|², which is the convention `hydrogenS` (\|ψ₁₀₀\|²) and `hydrogen2p` (\|ψ_{2p}\|²) already use next door. The "3-lobe geometry at 120°" this row used to credit never reaches the screen — one hybrid is one lobe with its small back lobe, and only one φ is drawn at a time. Measured **1.1×10⁻¹⁶** against `sympy.physics.hydrogen.R_nl(2,0)` and `R_nl(2,1)` with the standard Y₀₀ = 1/2√π and √(3/4π) constants, over the reachable box. Round 8 removed the +10⁻⁶ on r: ψ_s, ψ_px and ψ_pz are all polynomials times e^{−r/2} and θ comes from `atan2`, so nothing needed guarding; the shift cost 4.08×10⁻⁷ against a peak of 0.12. |
| `bellState` | Bell correlation | 🟢 A | E(a,b) = -cos(a-b) exact. |
| `feynmanPath` | Free propagator | 🟢 A | (m/2πiħt)^½·exp(imx²/2ħt) — real part. Round 6 restored the (1/i)^{1/2} = e^{−iπ/4} that was dropped: the real part is cos(x²/2T − π/4), and forty-five degrees moves every fringe, so this was a change of pattern and not of scale. The clock is folded to a 24-unit period (round 5) so the 1/√T amplitude replays instead of fading. |
| `quantumZeno` | Zeno survival | 🟢 A | cos²ᴺ(ωT/2N) exact — and the caption's t is the **x axis**: T = 4(x+3.5)/7 ∈ [0, 4], not the session clock. Round 9 measured dClock = 0.000000 at all five settings with t swept to 137 inside one process, and dFreq = 0.000000 as well, since x is never multiplied by freq — one of the three sliders is inert on this entry alone. |

---

## GPU displacement ladder (38 branches)

`computeMode()` in `src/shaders.js` is a 38-way if/else on `uMode`, and `main()`
**assigns** `pos.y` from it rather than accumulating, so a branch that returns
zero is a flat plate in a flat colour — `vH` feeds the palette too.

These branches are GLSL and cannot be evaluated in the Node test suite. They
were measured by transliterating each branch into JS (the ladder uses only
`length`, `atan`, `exp`, `sin`, `cos`, `pow`, `abs`, `tanh`, all of which match
JS semantics; the one exception is `mod`, added to mode 16 in round 6 and
transliterated as `x − y·floor(x/y)`, which is what GLSL means by it and is
**not** JS `%` for a negative first argument — that difference is the whole
point of the branch, whose argument goes negative on every wrap) and taking the surface
span over [−3.5, 3.5]² in three uniform states: silence, mid-level music, and
loud. The regression tests that guard the two repairs read the shader source.

**Two branches drew nothing.**

| Mode | Label then | Span | Cause |
|-----:|------------|------|-------|
| 10 | 11. τ(n) Ramanujan Tau | 7.9·10⁻⁶ | every term carried `sin(fn*3.14159)` — that is sin(nπ) = 0 for integer n |
| 30 | 31. Dragon Curve | 1.1·10⁻¹⁶ | summand `sin(5n·x)cos(5n·z)` is odd in n, the unrolled weight `exp(-4.*.3)` was even, so every ±n pair cancelled exactly |

Mode 10 now carries the tau coefficients its label names (1, −24, 252, −1472,
4830, −6048, −16744, scaled by 10⁻³); mode 30 carries the sign of n in its
exponent, which is what the loop it was unrolled from would have produced.
Mode 8 is unrolled the same way and is unaffected — its summand `cos(ang·n)` is
even, so its pairs add. The difference is the parity of the summand.

**Two more had no floor or no ears.**

- Mode 11 multiplied its whole sum by `sin(fn*t*2.)`, where `t` is `uTreble`,
  not time — span 0.0 in silence. Given an offset, like every other branch has.
- Modes 35 and 36, labelled "EQ 3D" and "Vocoder", read neither `uBass` nor
  `uMid` nor `uTreble`: their span was identical to the digit in silence and
  under loud music. The three bands now drive the harmonics they name.

**Nineteen labels named mathematics that was not in the branch.** Δ(τ) is
q∏(1−qⁿ)²⁴ and mode 8 is a theta sum; η(τ) is q^{1/24}∏(1−qⁿ) and mode 14 is
`sin(8r)·cos(4θ)`; j(τ) has a pole at q → 0 and mode 15 is a sum of sines;
E₄ needs the divisor sum σ₃; none of the eight branches under "FRACTALS &
CHAOS" iterates anything. Those labels are now descriptive of what the branch
computes. The three that were accurate keep their names: mode 6 (Ramanujan
theta), mode 9 (θ₃(q)), mode 37 (spectral centroid, which really is
(treble+ε)/(treble+bass+2ε)), and mode 10 now joins them.

Presets store the numeric `uMode`, not the label, so no saved preset moved.

**Second pass — two of the round-5 repairs needed repairing.** Both were found
by re-deriving the fixes independently rather than by reading their rationales.

- Mode 11's floor was written as `sin(fn*(0.35+t*2.))`, with the offset *inside*
  the harmonic index. That phases all seven harmonics together and they add
  constructively: span went 2.046 → 3.290 under loud audio and 3.070 → 4.935
  with the sliders up, against a camera half-frame of about 3.26. The branch was
  in frame everywhere before the repair and was not after it. Written as
  `sin(fn*t*2.+0.6)` the offset shifts every harmonic by the same amount:
  silence 0.680, loud 1.998, sliders up 2.997 — a floor, and a ceiling slightly
  below what the branch had before either repair.
- `gamma`'s overflow fix was applied to the n ≥ 0.5 branch only. The reflection
  branch computes Γ(1−n) with the same `Math.pow` and π/Infinity is 0, so
  `gamma(n)` returned **exactly zero** for every n ≲ −141.5 — Γ(−141.5) is
  1.39·10⁻²⁴⁴ and even Γ(−170.5) ≈ −3.3·10⁻³⁰⁸ still fits a double. Half a fix
  under a comment claiming a whole one. Both branches now fall back to logs.

---

## Previously Fixed Defects (Tier D → resolved)

All three Tier D defects identified in Round 1 have been fixed and verified by automated tests. Listed here for historical reference.

### `tinkerbell` (Fractals) — fixed in Round 1 → Tier B
`isFinite` guard was inside the loop body, missing final-iteration overflow. Post-loop guard added: `if (!isFinite(py)) return 0;`. Now Tier B — stable 12-iteration Tinkerbell map.

### `dragon` (Fractals) — fixed in Round 1 → Tier B
Was using shader-noise hash instead of Heighway IFS. Replaced with deterministic IFS via complex rotation and bit-pattern branch selection. Now Tier B.

### `jacobian` (Linear Algebra) — fixed in Round 1 → Tier A
Operator precedence bug: `amp*0.1` was only scaling the second product term, not the full determinant expression. Parentheses added: `((ux/(2*h))*(vz/(2*h)) - (uz/(2*h))*(vx/(2*h))) * amp * 0.1`. Now Tier A.

---

## Marketing-Defensible Claims

### ✓ Defensible (current state — defects fixed + Tier C upgrades)

> **157 mathematical formulas with verifiable numerical accuracy.**
> 122 closed-form analytic expressions evaluated at IEEE 754 double precision.
> 35 well-validated approximations, each with its error documented in its own row — typically 10⁻³ to 10⁻⁷ at the factory sliders, and stated where it is larger — including real PDE simulations on adaptive internal grids with bilinear interpolation to the full-resolution display mesh.
>
> (The "≤ 10⁻³ to 10⁻⁷" this claim used to state as a single ceiling over all 35 is not one. Measured 2026-08-17 against the same kernels with `soften()` replaced by the identity: of the 35, exactly one folds at all and it exceeds it — `manifoldCurvature` by 3.24 world units over 34.6 % of the plate at the audio envelope. (`dragon` was the second until round 11 rebuilt it around a normalised density, which needs no fold; through this probe it now reads exactly 0.) The other 34 read exactly 0 through that probe, which is the control that makes the two readings mean something. Two further rows document non-fold errors above 10⁻³ on purpose: `ornsteinUhlenbeck` at 17 % on stationary variance and lag-1 autocorrelation, `juliaPotential` at 6.4×10⁻⁴…1.46×10⁻³ over one turn of its clock at the audio envelope — though that one is inside the ceiling everywhere the rule reads it, running 5.3…8.6×10⁻⁴ over the whole of the same clock at the factory sliders.)
> Source-available, open test suite (the validation file, including regression tests for previously identified defects and validation tests against canonical mpmath/NIST DLMF reference values).

### ✓ Defensible (alternative — domain-coverage emphasis)

> **Implements 192 canonical mathematical models across 12 domains** — special functions, statistical distributions, complex analysis, Fourier theory, dynamical systems, integral transforms, topology, quantum mechanics, cellular automata. 82% achieve numerical accuracy verifiable against scipy/Wolfram references; 18% are visualization-grade for systems where exact real-time evaluation is computationally prohibitive at 60 fps.

### ✓ Defensible (slogan)

> **157 formulas. Verifiable accuracy. Open tests.**

### ✗ Not defensible without major rework

> ~~"100% scientific accuracy"~~ — too vague; will be challenged on first audit.
> ~~"All 192 formulas mathematically exact"~~ — false (Tier C is 35 formulas).
> ~~"Real-time numerical solutions"~~ — implies simulation fidelity that 8-step Euler does not provide.

---

## How to Verify

<!-- This is the count for this file alone, same as the "Test
     suite" line above — move both together. `npm test` reports 208. -->
The companion file `tests/math-validation.test.js` is executable and covers:
- All 122 Tier A formulas at canonical reference points (boundary values, known special-function values, identity tests).
- Sanity checks for Tier B formulas (PDF integration, convergence behaviour, polynomial fit boundary error, PDE simulation stability).
- Qualitative checks for Tier C formulas (peak location, sign changes, energy bounds, determinism).
- Regression tests for all three previously fixed Tier D defects.
- Schema integrity: every formula has `name`, `formula`, and `f`. Catalog count, finite output, bounded output across all formulas.

Run with:
```bash
node --test tests/math-validation.test.js
```

All of them currently pass against the live `math-collections.js`.

---

## Methodology Notes

**Reference implementations used for Tier A verification**:
- scipy.special (gamma, bessel, erf, hyp2f1, lambertw, etc.)
- mpmath (high-precision arbitrary-accuracy reference)
- Wolfram Alpha for spot-checks
- NIST DLMF (Digital Library of Mathematical Functions) for canonical values

**Tolerances**:
- Tier A: 10⁻¹² absolute (limited by float64 round-trip noise)
- Tier B: 10⁻³ absolute or stated polynomial bound, read at the factory sliders — see "The settings a letter is read at" under Tier Definitions. Where an entry exceeds it, its row states the figure; the letter does not move for it.
- Tier C: qualitative (peak position, sign, monotonicity, asymptotic limit)

**Audio modulation handling**:
For all formulas, validation uses `{amp: 1, freq: 1, comp: 0.5, time: 0}` — the unmodulated baseline. Audio-modulated outputs are correctly viewed as **scaled visualizations** of the underlying baseline, not separate mathematical objects. This is documented in the user-facing UI as "audio-reactive parameters modulate canonical formulas — set defaults for unmodulated reference."

**The `time` argument is a session clock, not a physical time**:
`time` starts at 0 when the page loads and advances 0.008 per animation frame for as long as the tab is open; nothing in the UI rewinds it. Validating at `time: 0` is therefore validating at the only instant a decaying solution is guaranteed to be alive, and eight entries were found rendering a flat plate — or, for `helicoid`, a mesh translated clean out of the framed volume — one or two minutes into a set. Those eight (`heat2D`, `dampedOscillator`, `heatEquation`, `fishersEquation`, `wavePacket`, `schrodingerSoliton`, `complexHeat`, `helicoid`) now fold the clock back into a per-entry period before using it, so the solution replays instead of running out. The mathematics of each is unchanged and `time: 0` still evaluates exactly as this document describes; what changed is which physical time a given session age maps to. `tests/math-validation.test.js` asserts each of them is still drawing after thirty minutes of uptime.

**Grid resolution note**:
Internal simulation grids for heavy formulas (cellular automata, PDEs) use fixed sizes of 40×40 to 64×64. These are bilinearly interpolated onto the adaptive display mesh (60–200 segments depending on GPU capability). Validation tests use the internal grid resolution, not the display resolution. Accuracy figures for Tier B formulas (e.g. "~10⁻³ accuracy") are measured at the internal grid level; interpolation to higher display resolutions does not improve numerical accuracy but produces visually smoother output.

---

*This document is the authoritative source for VIMATHIC's mathematical accuracy claims. Update when formulas are added or modified.*
